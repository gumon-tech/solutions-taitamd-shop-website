# 🔒 LEAD.md — ล็อก Lead ข้ามเครื่องของรีโป `solutions-taitamd-shop-website`

> กติกาต้นทาง: `gumon-workspace/docs/charter.md` §3.5 *"`LEAD.md` — ล็อก Lead ข้ามเครื่อง"*
> สร้างไฟล์นี้ 2026-08-22T18:55Z ตามมติเจ้าของที่ `KWS` ถ่ายทอดมาในใบ `Q-KWS-007`

**ไฟล์นี้แก้ปัญหาเดียว**: `SendMessage` ส่งได้เฉพาะ session **บนเครื่องเดียวกัน** ⇒ กันสอง Lead
คนละเครื่องรันคิวของรีโปนี้พร้อมกัน **ด้วยข้อความไม่ได้ ต้องใช้ไฟล์**
🔑 ***เส้นแบ่งที่พูดกันด้วยข้อความ หายไปพร้อม context — เส้นที่อยู่ในไฟล์เท่านั้นที่อีกเครื่องเห็น***

---

## สถานะปัจจุบัน

```
holder:   KWEB
machine:  komphet-air
acquired: 2026-08-23T01:55+07:00
expires:  2026-08-23T07:55+07:00      # +6 ชม. · ต่ออายุเองได้ · เขียนเวลานี้ลงช่อง next ของกระดานด้วย
scope:    งานเว็บใหม่ตั้งแต่ 2026-08-23
          ไม่รวม Q-LAW-045 ซึ่งยังรอเจ้าของกับนักกฎหมายจริงเคาะฐานทางกฎหมาย สถานะเดิมทุกประการ
```

**ที่มาของล็อกนี้ — มติเจ้าของ 2026-08-23** (ห้องนี้ไม่ได้ตัดสินเอง)

เจ้าของพิมพ์ในห้อง `KWS` verbatim ว่า **"ก ทุกสาย"** ⇒ ทุกสาย (Video · Concept · Legal · Web)
ให้เครื่องที่เจ้าของนั่งอยู่รับงานใหม่ ส่วนเครื่องเก่าปิดของค้างแล้วหยุด
📌 **ใบต้นทาง**: `gumon-workspace/machines/komphet-air/queue/ALL/Q-KWS-007-owner-ruling-all-lines-option-a.md`
(commit `82faa60c`) · ฝั่ง `komphet-mac` ได้รับแจ้งด้วยใบ `Q-KWS-008`

⚠️ ไฟล์นี้เป็น **ป้ายชี้ทาง** ตาม `Q-KWS-004` ข้อ 2 **ไม่ใช่แหล่งความจริงคู่ขนาน**
ถ้าขัดกับใบต้นทาง ให้เชื่อใบต้นทาง แล้วกลับมาแก้ที่นี่
🔑 ***คนที่ต้องทำตามมติ กับ คนที่อ่านที่ที่มติถูกเขียน ไม่ใช่คนเดียวกันเสมอไป***

## สิ่งที่ห้อง `SHOP` @`komphet-mac` ต้องรู้เมื่อ pull เจอไฟล์นี้

```
ท่านไม่ต้องส่งมอบอะไร  งานที่ท่านทำไว้ทั้งหมดอยู่ใน main แล้ว ห้องนี้อ่านต่อจาก HANDOFF ได้เลย
ท่านหยุดรับงานเว็บใหม่  ตามมติเจ้าของ ไม่ใช่เพราะห้องนี้ขอ
ของค้างที่เป็นของท่าน   ปิดให้จบแล้วเงียบ ตามใบ Q-KWS-008
ไฟล์ร่วมที่ขยับไปแล้ว   CLAUDE.md · docs/MEMORY-shop.md · docs/plans/HANDOFF.md (แจ้งไว้ในใบ Q-KWEB-001)
```

## วิธีถือ · ต่ออายุ · ปล่อยล็อก

แก้บล็อก **สถานะปัจจุบัน** ข้างบน แล้ว commit และ push ทันที
🔴 **ล็อกที่หมดอายุแล้วถือว่าปล่อย** — ห้องอื่นหยิบต่อได้โดยไม่ต้องถาม
⚠️ `KMKT` ปล่อยล็อกขาดไปประมาณ 3.5 ชั่วโมงโดยไม่มีอะไรเตือน (2026-08-22)
⇒ กติกา `Q-KWS-004` ข้อ 1 จึงบังคับให้เขียน `expires` ลงช่อง `next` ของกระดาน `~/.claude/lead-status/KWEB.json`
🔑 *ล็อกที่ต่ออายุด้วยความจำของคนถือ จะขาดตอนคนถือกำลังยุ่ง ซึ่งคือตอนที่มันสำคัญที่สุด*
