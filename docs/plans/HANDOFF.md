# HANDOFF — ห้อง Taitam-D Shop Lead

## 🔑 `WORKSPACE-RESUME` — ทำสามข้อนี้ก่อนทำอะไรทั้งสิ้น

> 🔴 **นิยามฉบับจริงอยู่ที่ `~/dev/gumon-workspace/docs/workspace-resume.md` (ขั้น 0-6 · `Q-KWS-022` ย้ายมาไว้ในรีโป 2026-08-27)**
> **`~/.claude/CLAUDE.md` เหลือเป็นตัวชี้ · ไฟล์นี้เป็นแค่สำเนาย่อ ถ้าขัดกัน ฉบับกลางชนะ**
> 🔑 **ขั้น 6 = เคลียร์งานค้างที่ทำได้จนจบ ไม่ต้องถามก่อนเริ่ม — resume จบเมื่อกอง "ทำได้เอง" หมด ไม่ใช่เมื่อรายงานเสร็จ**
> **พิมพ์ `WORKSPACE-RESUME` เมื่อไร ห้องนี้ต้อง:**
>
> 0️⃣ **ตั้งชื่อ session ตัวเองก่อนทุกอย่าง** (`Q-WS-270` · เพิ่ม 2026-08-27) — `mcp__ccd_session_mgmt__set_session_title`
>    `session_id: "self"` · title = `[SHOP] Taitam-D Shop Website Lead` (รหัสจากทะเบียนเครื่องในขั้นถัดไป ห้ามเดา)
>    เหตุ: ทะเบียนข้ามเครื่อง **snapshot ชื่อตอน session เริ่ม** ⇒ ตั้งทีหลังเครื่องอื่นยังเห็นชื่อเก่า
>    และเมื่อ 7 ห้อง resume พร้อมกัน ทุกห้องขึ้นชื่อ "Workspace resume" เหมือนกันจนจ่าหน้าข้อความไม่ได้
>
> 0. 🔴 **อ่าน `machines/$(cat ~/dev/gumon-workspace/.machine-id)/queue/README.md` ก่อนอ่านอะไรทั้งสิ้น**
>    เพื่อรู้ว่า **บนเครื่องที่คุณยืนอยู่ ห้องนี้ใช้รหัสอะไร** (`SHOP` @komphet-mac · `KWEB` @komphet-air)
>    ⚠️ **ขั้นนี้ต้องมาก่อนขั้นที่ 1 เสมอ** — ไฟล์นี้ยาวและพูดคำว่า `SHOP` ตลอดทั้งไฟล์
>    ⇒ อ่านจบแล้วความมั่นใจเรื่องรหัสก่อตัวเสร็จ ก่อนจะได้เห็นทะเบียนของเครื่องแม้แต่ครั้งเดียว
>    🔑 ***ขั้นที่มาทีหลังต้องออกแรงแย้งความมั่นใจที่ก่อตัวไปแล้ว ขั้นที่มาก่อนแค่บอกข้อเท็จจริงก็พอ***
>    (`Q-KWS-004` ข้อ 3.2 · `KLORE` เสนอ · เกิดจริงกับห้องนี้ 2026-08-23)
> 1. **อ่านไฟล์นี้ให้จบก่อน**
> 2. **รันบล็อก §0 "ตรวจก่อนเชื่อ" ข้างล่างจริง ๆ** แล้วรายงานส่วนที่ไม่ตรงกับเอกสาร อย่าเชื่อตามที่เขียน
> 3. **เช็คตู้รับใบคิวกลางของห้องนี้**: `ls ~/dev/gumon-workspace/machines/<เครื่องนี้>/queue/<รหัสจากขั้น 0>/`
>    (รหัสห้องนี้คือ `SHOP` · ออกใบใหม่เป็น `queue/<รหัสผู้รับ>/Q-SHOP-<nnn>-<เรื่อง>.md` เดินเลขในเขต `SHOP` เท่านั้น)
> 4. **อ่าน `MEMORY.md`** ของโปรเจกต์ แล้ว **ตรวจว่าที่จดไว้ยังจริงกับโค้ดปัจจุบัน** — ข้อไหนไม่จริงแล้วให้แก้/ลบทันที
> 5. **แนะนำตัวกลับไปที่ห้อง Gumon Workspace Lead** (`~/dev/gumon-workspace`) ด้วย `ListAgents` สดแล้วต่อด้วย `SendMessage`
>    โดยใส่ `[ref]` ที่คัดลอกจากรายการสด ห้ามใช้ `[ref]` ที่จดไว้ เพราะมันเปลี่ยนทุกครั้งที่ห้องปลายทางเริ่มใหม่
>    🔴 **แก้ 2026-08-18 — บรรทัดนี้เคยเขียนว่าให้ใช้ `mcp__ccd_session_mgmt__send_message` ซึ่งถูกห้ามไปแล้ว**
>    `QA` วัดเมื่อ 2026-08-15 ว่ามันคืน `Session not found` ทั้งที่ห้องปลายทางเปิดอยู่ ⇒ ทั้งเครื่องเชื่อกันว่าท่อพัง
>    ทั้งที่ของจริงคือเรียกผิดเครื่องมือ · กติกาเต็มอยู่ที่ `gumon-workspace/docs/cross-session-messaging.md`
>    🔑 ***เครื่องมือที่ล้มเหลวโดยบอกแค่ว่าไม่พบ จะถูกอ่านว่าปลายทางผิด ไม่ใช่ผู้เรียกผิด***
>    (เดิมเขียนว่า "Local Dev Lead / `~/dev/gumon-localdev`" — รีโปถูก rename เป็น `gumon-workspace` เมื่อ 2026-08-09
>    path เก่าเป็น symlink ยังใช้ได้ แต่ **ชื่อห้องที่ถูกต้องคือ Gumon Workspace Lead**)
>
> 🔴 **context ในห้องอาจไม่รอดการเปลี่ยนบัญชี — ของที่ไม่ได้ลงดิสก์ถือว่าหาย** ⇒ เอกสารนี้คือความจำทั้งหมดที่เหลือ

---

> **อัปเดตล่าสุด 2026-08-14** (เปลี่ยนบัญชี Claude เป็น `app@taitam-d.com` · ลงทะเบียนห้องใหม่กับ `WS` แล้ว)
>
> 🔴 **เอกสารนี้คือ snapshot ไม่ใช่สถานะสด** — ทุกข้อด้านล่างจริง ณ เวลาที่เขียน
> **session ถัดไป: รันบล็อก `## Verify` ให้จบก่อนลงมือ** ข้อไหนพิสูจน์ว่าไม่จริงแล้ว **ลบทิ้งทันที อย่าปล่อยให้ค้าง**

---

## 🛑 เปลี่ยนบัญชี 14 ส.ค. + คำสั่งยืน — **อ่านบล็อกนี้ก่อนแตะอะไรทั้งสิ้น**

**① การพักตาม `Q-WS-101` หมดอายุแล้ว แต่มีคำสั่งหยุดใบใหม่มาทับ อ่านต่อที่ ⑤**
`~/dev/gumon-workspace/machines/komphet-mac/queue/WS/Q-WS-101-focus-a1-learner-instructor-flow.md`
ตารางข้อ 8 เขียนว่า `SHOP` `MKT` `LAW` พัก **คืนสถานะหลัง 16 ส.ค.** ซึ่งถึงแล้ว
วัด 2026-08-16T01:2xZ: ห้องนี้ **ไม่มี** `queue/SHOP/_PAUSED.md` · ที่ยังพักคือ 11 ห้องอื่น
และเจ้าของปลด `MKT` `SIG` `LAW` เองแล้วใน `Q-WS-191` ภาคผนวกข้อ 4
⇒ ตาม `Q-WS-165` ข้อ 3 คำสั่งที่มีเวลากำกับเลยเวลาแล้วให้ถือว่าปลดเอง **การพักของห้องนี้จบแล้ว**
เงื่อนไขคืนสถานะรายแถวยังอยู่ใน [`QUEUE-WEB.md`](QUEUE-WEB.md) ตามเดิม

**② ข้อยกเว้น "ของที่ขวางสายผู้เรียนโดยตรง" — ตรวจแล้ว เว็บร้านไม่ขวาง (14 ส.ค.)**
ทางเข้ากรวยมีค่าเดียว `SITE.academy` ([`lib/site.ts:38`](../../lib/site.ts)) ไหลไป 9 จุด · วัดสดได้ 200 ทั้งหมด
(`academy.taitam-d.com/` → 200 redirect `/en` · cookie-policy → 200 · apex → 200 · `/signature/` → 200)
⇒ **อย่าวัดซ้ำโดยไม่มีเหตุ** · 📌 เว็บร้านไม่มี deep link ไปคอร์ส `A1` — บันทึกไว้เฉย ๆ ถ้าใครอยากได้ให้ส่งใบมา

**③ ที่ค้างจากรอบเปลี่ยนบัญชีครั้งก่อน (13 ส.ค.)** — บล็อก "🔁 กลับมาเดินต่อ" ใน [`QUEUE-WEB.md`](QUEUE-WEB.md)
มีของสำคัญที่**อยู่นอกรีโป**: รูปต้นทาง 24 ใบใน `~/Downloads/Taitam-D Shop/` และเสียงครูนก 4 คลิป
⚠️ เครื่องถูกล้างเมื่อไร **ต้องขอครูนกใหม่ทั้งชุด** (ต้นฉบับอยู่ Google Drive)

**④ สี่ใบที่เคยค้าง ตอบครบแล้ว เลิกทวงได้** — `WS` ตอบใน `queue/SHOP/Q-WS-146` (14 ส.ค. ยัง `open`)
`001` เบอร์บ้าน ฝั่ง platform แก้ไปแล้วตั้งแต่ `Q289` โดยไม่มีใครบอกเรา ที่ `grep` ยังเจอคือไฟล์เทสต์กับคอมเมนต์ที่บันทึกว่าเคยผิด
`002` นามสกุล `Pomnongsan` ยืน ข้อสังเกตของ `LAW` ตกไป `WS` รับว่าตัวเอง route มาโดยไม่วัดก่อน
`003` รูปครูนก ยกให้เจ้าของ เพราะเป็นคำถามเรื่องสิทธิ์ ไม่ใช่เรื่องข้อมูลไม่พอ **ปิดไปแล้วใน `Q-SHOP-013`**
`004` ไฟล์กำพร้า เคาะให้เอาออกจาก production **ทำไปแล้ว `Q-SHOP-012`**
ใบ `Q-WS-146` เองยัง `open` เพราะห้องนี้ยังไม่ได้ ack ลงไป

**⑤ คำสั่งเจ้าของ `Q-WS-190` สั่งหยุดทุกห้อง ออกเมื่อ 2026-08-15T23:49Z อายุประมาณ 1 ชั่วโมงครึ่ง ณ ที่เขียนนี้**
`queue/ALL/Q-WS-190-STOP-NOW-owner-order-at-99-percent.md` เหตุผลคือโควตาบัญชี 99 เปอร์เซ็นต์
สั่งสี่ข้อ ปิดของในมือ push ทุกอย่าง เขียน HANDOFF สามข้อ แจ้งงานพื้นหลัง
**ห้ามเริ่ม deploy ห้ามเริ่มนำเข้า ห้ามเริ่มเรนเดอร์ ห้ามเริ่ม apply สคริปต์กวาดทั้งทรี**
⇒ ห้องนี้ปลดพักแล้วก็จริง แต่ **ยังไม่หยิบ W-24 ขึ้นมาทำ** จนกว่าเจ้าของจะสั่ง เพราะนั่นคือการเริ่มของใหม่
ถามไปในใบ `queue/WS/Q-SHOP-017` แล้ว

---

## Verify — ตรวจก่อนเชื่อ (รันก่อนทำอะไรทั้งสิ้น · charter ข้อ 7)

> หัวข้อนี้ชื่อ `## Verify` ตรงตัวโดยตั้งใจ — ขั้นที่ 2 ของ `WORKSPACE-RESUME` และ verify block
> ของใบคิวกลางค้นด้วย `grep '^## Verify'` ⇒ **เปลี่ยนชื่อหัวข้อเมื่อไร ขั้นวัดของจริงจะถูกข้ามเงียบ**
> (เดิมชื่อ "0 · ตรวจก่อนเชื่อ" ซึ่งมีเนื้อครบแต่ grep หาไม่เจอ — แก้ 2026-08-10)

```bash
git -C ~/dev/solutions-taitamd-shop-website status -sb   # คาดหวัง: clean · ตรง origin/main
git -C ~/dev/solutions-taitamd-shop-website log --oneline -3
# 📌 `gh` ลงแล้ว 2026-08-23 (v2.98.0 ที่ `~/.local/bin/gh`) แต่ **shell ของ agent ไม่ source โปรไฟล์**
#    ⇒ ต้อง export PATH เองก่อน ไม่งั้นได้ command not found แล้วบล็อกเดินต่อเหมือนผ่าน
export PATH="$HOME/.local/bin:$HOME/.nvm/versions/node/v24.19.0/bin:$PATH"   # gh + npm/node
gh auth status >/dev/null 2>&1 && gh run list --limit 3 \
  || echo "⚠️ gh ยังไม่ได้ล็อกอิน (เจ้าของรัน `gh auth login` เอง) — ข้ามไปวัด deploy ด้วยของจริงข้างล่าง"
#    รีโปเป็น private ⇒ GitHub API แบบไม่ล็อกอินคืน 404 · **ตัววัด deploy ที่ไม่ต้องพึ่ง gh เลยคือเทียบ production กับ HEAD**
IMG=$(grep -o '"/images/[a-z0-9/_-]*\.\(webp\|jpg\|png\)"' ~/dev/solutions-taitamd-shop-website/lib/catalog.ts | head -1 | tr -d '"')
curl -s https://taitam-d.com/services/ | grep -c "$IMG"          # คาดหวัง: 1 (หน้าจริงอ้างรูปเดียวกับ HEAD)
curl -s -o /dev/null -w "%{http_code}\n" "https://taitam-d.com$IMG"   # คาดหวัง: 200
lsof -nP -iTCP:3300 -iTCP:3301 -sTCP:LISTEN               # คาดหวัง: ว่าง
# 🔴 อ่าน size_download คู่กับ http_code เสมอ (เพิ่ม 2026-08-27 · `MKT` เจอกับตัว)
#    วัดจริงวันนี้: /book (ไม่มีสแลชท้าย) คืน `301 162` ⇒ ตัวหน้า 162 ไบต์นั้นหน้าตาเหมือนหน้าเว็บที่ยังไม่ได้แก้
#    ⇒ ด่านที่ตามด้วย -L หรือที่ grep เนื้อหาจากผลลัพธ์นั้น จะอ่านว่า "ของยังไม่ขึ้น" ทั้งที่ของขึ้นแล้ว
#    🔑 สแลชท้ายไม่ใช่รายละเอียดความสวยงามของ URL — ที่นี่มันคือความต่างระหว่างหน้าจริงกับหน้าเปลี่ยนทาง
curl -s -o /dev/null -w "apex   %{http_code} %{size_download}\n" https://taitam-d.com/    # คาดหวัง: 200 · ราว 164 KB
curl -s -o /dev/null -w "/book/ %{http_code} %{size_download}\n" https://taitam-d.com/book/  # คาดหวัง: 200 · ราว 48 KB
#    เลขไบต์ขยับได้ทุกครั้งที่แก้เนื้อหา ⇒ เกณฑ์คือ **หลักหมื่นขึ้นไป** ⛔ ไม่ใช่ตรงเป๊ะ · หลักร้อย = หน้าเปลี่ยนทาง
grep -H '^status:' ~/dev/gumon-workspace/machines/*/queue/SHOP/Q-*.md   # คาดหวัง: ไม่มีใบไหน open
# 🔴 บรรทัดข้างบน **ไม่พอ** — ใบที่จ่าหน้าหลายห้องถูกเก็บใน**ตู้ของผู้ออก** ⇒ ตู้ SHOP/ มองไม่เห็น
# และ **ห้ามใช้ `git pull`** ที่นี่ — working tree ของ workspace เป็นของกลาง ห้องอื่นมีงานค้างได้ตลอด
#    ⇒ pull ล้ม แต่ grep รันต่อบนสำเนาเก่า **โดยคืนผลลัพธ์หน้าตาปกติ** (วัดเจอ 14 ส.ค. 09:37Z · Q-SHOP-008)
cd ~/dev/gumon-workspace && { git fetch -q origin || echo "🔴 FETCH ล้ม — ผลข้างล่างเชื่อไม่ได้"; }
# 🔴 แก้ 2026-08-23 — เดิม grep แค่ `\bSHOP\b` ⇒ **ใบที่จ่าหน้า `to: [ALL]` หายไปทั้งหมด**
#    วัดเจอของจริง: `Q-WS-256` (กติกาใหม่ priority high ถึงทุกห้อง) ไม่โผล่ในผลสแกนเดิมเลย
# ⚠️ และ **ห้ามยุบสองบรรทัดนี้เป็น `git grep -lE '(SHOP|ALL)'`** — วัด 2026-08-23: `git grep -E` **ไม่รองรับ `\b`**
#    คืน 0 ไฟล์เงียบ ๆ ไม่มี error (ท่า `-l` ธรรมดารองรับ ⇒ 86 ไฟล์) ⇒ สแกนตาบอดโดยหน้าตาปกติ
# 📌 ขอบเขตเครื่อง: สแกนเฉพาะ `machines/komphet-mac/` — ห้องนี้อยู่บนเครื่องนี้
#    ใบ broadcast ในตู้ `ALL` ของเครื่องหนึ่ง **ผูกพันเฉพาะห้องของเครื่องนั้น** (`WS` เคาะใน `Q-TWS-001` ข้อ 4)
#    วัด 2026-08-23: กว้างเป็น `machines/*/` แล้วได้ `Q-KWS-004` (komphet-air) กับ `Q-TWS-002` (tongo) ซึ่งไม่ใช่ของเรา
# 🔴 แก้ 2026-08-27 — เพิ่มขา GLOBAL เป็นขาที่ 3 (`Q-WS-269` ข้อ 4)
#    ตู้ `queue/GLOBAL/` อยู่ที่ **root ของรีโป workspace ไม่ใช่ใต้ `machines/`** ⇒ pathspec เดิมมองไม่เห็นทั้งตู้
#    วัดเจอ 2026-08-27: สแกน 2 ขารายงาน "ไม่มีใบใหม่หลัง 25 ส.ค." ขณะที่ตู้ GLOBAL มีใบ open 8 ใบ
#    (2 ใบ priority high · 1 ใบคือ `Q-KWS-024` มติมอบอำนาจของเจ้าของ) ⇒ hook เป็นตัวบอก ไม่ใช่ด่านนี้
# 🔴 แก้รอบสอง 2026-09-08T17:5xZ — **เลิกเขียนคำสั่งสแกนเอง ใช้สคริปต์กลาง** (เหตุผลใต้บล็อก)
~/dev/gumon-workspace/bin/queue-open-to SHOP     # อ่าน frontmatter เท่านั้น · ข้าม archive/ · ครอบ 3 ตู้
#    บรรทัดหัวผลลัพธ์บอก 2 เลข: "จ่าหน้าตรง n · ประกาศทุกห้อง m"
#    วัด 2026-09-08T17:5xZ ได้ จ่าหน้าตรง 3 · ประกาศทุกห้อง 14
#    ⚠️ ใบ "ประกาศทุกห้อง" ปิดได้เฉพาะผู้ออก · ของเราคือ **อ่านแล้วเขียน ack** ⛔ ไม่ใช่ปิดใบ
~/dev/gumon-workspace/bin/queue-open-to --selftest   # ✅ ตัวนี้แทน positive control ที่ห้องเขียนเอง
```

🔬 **บรรทัด positive control ไม่ใช่ของประดับ — มันคือด่านที่ทำให้ผลลัพธ์ "ว่าง" อ่านออก**

🔴 **แก้นิยามของ control 2026-09-08 — control ล้มครบ 3 ขาในรอบเดียว และทั้ง 3 ขาล้มโดยชอบ**
```
Q-WS-262   status: done   ย้ายเข้า machines/komphet-mac/queue/ALL/archive/   pathspec เดิม `*/*.md` ไม่ลงชั้นลูก
Q-WS-200   status: done   ย้ายเข้าชั้นเดียวกัน                                เหตุเดียวกัน
Q-KWS-024  อยู่ที่เดิม     status เปลี่ยน open → standing เมื่อ 2026-09-04    เกณฑ์ `^status: open` มองไม่เห็นทั้งชั้น
```
ต้นเหตุคือกติกากลาง 2 ใบที่ออกหลังบล็อกนี้: `Q-WS-343` (ใบที่จบย้ายลง `archive/` · มติถาวรเป็นชั้น `standing`)
และ `Q-WS-362` (`WS` ไล่จัดชั้นใบเก่าตามนั้นเมื่อ 2026-09-04) ⇒ **ตู้เปลี่ยนรูป แต่ด่านที่ส่องตู้ไม่ได้เปลี่ยนตาม**

🔑 ***control เดิมผูกกับ "ใบนั้นยัง `open` ไหม" ซึ่งเป็นสถานะที่ห้องอื่นแก้ได้ทุกวัน ⇒ มันหมดอายุเองโดยไม่มีใครแตะ***
⇒ ฉบับนี้ย้าย control ไปยืนบน **"ขานั้นยังเห็นไฟล์ไหม"** ซึ่งเป็นคุณสมบัติของ `git grep` เอง ไม่ใช่ของสถานะใบ
📌 ที่แลกไป: control จะไม่ส่งเสียงอีกแล้วตอนใบถูกปิด (เช่นตอนปลดพัก) ⇒ **หน้าที่นั้นย้ายไปอยู่กับบรรทัด `$OUT`**
ซึ่งพิมพ์ทุกใบที่ยังไม่ `done`/`closed`/`superseded` ออกมาให้อ่าน ไม่ใช่แค่นับ

<details><summary>ถ้อยคำเดิมของ control ก่อน 2026-09-08 — เก็บไว้เพราะเหตุผลของมันยังอ่านได้</summary>

`Q-WS-262` (ขา `SHOP`) และ `Q-WS-200` (ขา `ALL`) เป็นใบ `open` ที่อายุยืน ⇒ **คำสั่งที่ทำงานถูกต้องต้องเห็นทั้งคู่เสมอ**
⚠️ **ตัวเดิมคือ `Q-WS-101` และมันล้มจริงเมื่อ 2026-08-23** — ไม่ใช่คำสั่งพัง แต่ `WS` กวาดใบหมดอายุ
ปิดมันไปเมื่อ 2026-08-21T11:0xZ (แทนด้วย `Q-WS-238` + `Q-WS-239`) ⇒ **เคสบวกของกลไกนี้ ไม่ใช่ false alarm**
⇒ มันหายเมื่อไร = pathspec เพี้ยน · ชื่อฟิลด์เปลี่ยน · หรือ `WS` เพิ่งปิดใบนั้น (**= ปลดพักแล้ว!**)
**ทั้งสามกรณีต้องเอาตาดู ห้ามอ่านว่าตู้ว่าง** · ทดสอบทางที่ล้มแล้ว 14 ส.ค.: path ผิด → ล้ม ✅ · `status`→`state` → ล้ม ✅
🔑 *control ที่หมดอายุพร้อมกับเงื่อนไขที่มันเฝ้า เป็น**คุณสมบัติ** ไม่ใช่บั๊ก — มันบังคับให้มีคนมองตอนสถานะเปลี่ยนพอดี*

</details>

⚠️ **คุณสมบัติข้อสุดท้ายนั้นจริง แต่มันจ่ายด้วยราคาที่วัดได้แล้ว 2026-09-08** — เมื่อ control ทั้ง 3 ขาล้มพร้อมกัน
สิ่งที่ห้องอ่านได้จากมันคือ "ไปดูเอง" เท่านั้น **ซึ่งเป็นข้อมูลปริมาณเท่ากับไม่มี control เลย**
⇒ แยกหน้าที่กันชัด ๆ ดีกว่า: control เฝ้า**กลไก** · `$OUT` เฝ้า**สถานะ**

⚠️ **อย่าใช้ `git rev-parse HEAD origin/main` เป็นตัววัดความสดของบล็อกนี้** (`WS` เสนอมา · ผมแย้งใน `Q-SHOP-009`)
มันวัด **working copy** ซึ่งบล็อกนี้ ไม่ได้อ่านเลย ⇒ จะเตือนผิดทุกครั้งที่ห้องมี commit ที่ยังไม่ push
🔑 *ตัวเตือนที่เตือนผิดเป็นประจำ อันตรายกว่าไม่มีตัวเตือน — ห้องจะเรียนรู้ที่จะเพิกเฉยมัน*

📌 **`git` pathspec ไม่เหมือน shell glob — `*` ข้าม `/` ได้** (วัดแล้ว 14 ส.ค.)
⇒ แปลง glob ระหว่างสองที่โดยคิดว่าความหมายเหมือนกัน จะได้ผลคนละอย่าง **โดย ไม่มี error**

⚠️ **บรรทัด `grep -rl` เพิ่ม 2026-08-14 เพราะมันจับของจริงได้ทันทีที่เพิ่ม** — ตอนนั้นมีใบ `open`
ถึงห้องนี้ **2 ใบที่ `ls SHOP/` มองไม่เห็นเลย** (`WS/Q-WS-110` channel test · `WS/Q-WS-101` คำสั่งยืน)
🔑 ***การตรวจที่มองเฉพาะตู้ของตัวเอง ให้ผล "ไม่มีงานเข้า" ที่**อ่านไม่ออกว่าแปลว่าไม่มีงาน
หรือแปลว่ามองไม่เห็น*** — และสองอย่างนี้หน้าตาเหมือนกันทุกประการ

🔔 **ห้องนี้ปลุกตัวเองด้วย cron** (ใบ `SETUP-self-wake` · ตอบแล้ว 14 ส.ค.) — `CronCreate`
**`37 * * * *` (รอบ 60 นาที เพราะห้องนี้ถูกพัก · `Q-WS-116` กำหนด)** รันบล็อก **fetch-based** ข้างบน
⚠️ **cron ตายพร้อม session และหมดอายุเองใน 7 วัน** ⇒ **เปิดห้องใหม่เมื่อไร ต้องตั้งใหม่ทุกครั้ง**

🔴 **พรอมป์ cron ต้องมีเงื่อนไขพักเขียนอยู่ในตัวมันเอง** (`Q-WS-116` · ข้อสังเกตของ `SUN`)
🔑 ***นาฬิกาปลุกที่ ไม่รู้ว่าห้องถูกสั่งพัก จะปลุกห้องกลับมาทำงานโดยที่ ไม่มีใครสั่ง*** —
และมันจะดูเหมือนห้องตัดสินใจเอง ไม่ใช่เหมือนบั๊ก ⇒ ห้ามตั้ง cron เปล่า ๆ ที่แค่ "ไปดูคิว"

**เหตุผลที่ต้องมี cron:** `send_message` **ไม่ถึง** — วัดครบ 11 ห้องสองรอบ (`Q-WS-110` · `Q-WS-116`)
**9 ห้องตอบ `NO-TOKEN` ทั้งหมด** · ห้องนี้ยืนยันทั้งสองทิศ (ขาเข้า ไม่ได้รับ · ขาออก `queued` แล้วเงียบ)
🔴 **ห้ามใช้ `sent` หรือ `queued` เป็นหลักฐานว่าถึง — ทั้งคู่โกหกได้**
🔑 *ข้อความคือกริ่งที่ดังไม่ครบ — **ไฟล์ที่ commit คือเนื้อ** ⇒ ตอบห้องอื่นด้วยไฟล์เสมอ*

🟢 **แก้ 14 ส.ค. 10:1xZ — กริ่งดังได้จริงเมื่อห้องปลายทาง `idle`** (`Q-SHOP-009`)
`WS`→ห้องนี้ส่งถึงครบทั้งฉบับ **และปลุกให้เดินเทิร์นทันที** ตอนห้องนี้ live+idle
⇒ เคสบวกตัวแรกของทิศนี้ · **แก้ถ้อยคำเดิมที่ว่า "ไม่ปลุก idle" ซึ่งไม่จริง**
🔴 **แต่ ไม่ใช่เหตุถอด cron** — ห้องที่ **ยุ่ง** ยังได้ `queued` แล้วหายเงียบ 0 เคสบวก
🔑 *กริ่งใช้ได้กับห้องที่ว่าง = ใช้ได้ตอนที่เราต้องการมันน้อยที่สุด*

⚠️ **แก้บรรทัดสุดท้าย 2026-08-14** — เดิมเขียนว่า `ls … SHOP/` แล้วคาดหวัง *"มีแค่ `README.md`"*
ซึ่ง **เป็นเท็จตั้งแต่ 10 ส.ค.** · ตู้รับมีใบสะสมอยู่ **5 ใบ** (`Q-WS-008/016/023/080` · `Q-MKT-002`) และ **ปิดครบทุกใบ**
🔑 **ใบที่ตอบแล้วไม่ได้ถูกลบทิ้ง มันอยู่ในตู้ต่อไปเป็นบันทึก** ⇒ เกณฑ์ที่ถูกคือ **"มีใบไหน `open` ไหม"** ไม่ใช่ **"ตู้ว่างไหม"**
⇒ ถ้าปล่อยไว้ ทุก session จะเห็นตู้ไม่ว่างแล้วรายงานว่า "มีงานเข้า" ทั้งที่ไม่มี

**ถ้าไม่ตรง แปลว่าอะไร:**

| อาการ | แปลว่า | ทำอะไรต่อ |
|---|---|---|
| tree ไม่ clean · มี `next-env.d.ts` / `tsconfig.json` โผล่ | มี session ที่รัน dev server แล้วไม่ได้คืนไฟล์ | `git checkout -- next-env.d.ts tsconfig.json` **ก่อน** ทำอะไรทั้งสิ้น — ถ้าหลุดขึ้นไป build ฝั่ง human พัง (กับดักข้อ 1) |
| ahead `origin/main` | มี commit ที่ verify แล้วแต่ **ยังไม่ขึ้นเว็บ** | ตรวจว่า gate เขียวแล้วค่อย push · ที่นี่ push = deploy |
| `gh run list` มี run ล้มเหลว หรือ 2 run ต่อ 1 push | deploy พัง หรือ workflow ซ้ำกลับมา (เคยเป็น ดู W-4) | ดู log จริง **อย่าเดาจากไฟล์ workflow** |
| พอร์ต 3300/3301 ไม่ว่าง | **สันนิษฐานว่าเป็น session อื่นของเรา — ห้าม kill** | ใช้เลขถัดไปใน band หรือ attach ของเดิม |
| เว็บไม่ 200 | ของจริงพัง สำคัญกว่าทุกงานในคิว | หยุดคิว แล้วไล่จาก run ล่าสุดที่ deploy |
| ตู้ `SHOP/` มีใบใหม่ | มีงานเข้า จบ standby | อ่านใบ → ตอบในใบ + commit (state เดินทางด้วย commit ไม่ใช่ข้อความ) |

**ณ เวลาที่วัดล่าสุด (2026-08-16T01:2xZ):** tree clean · ตรง `origin/main` · พอร์ต 3300/3301 ว่าง ·
ไม่มี `next-env.d.ts`/`tsconfig.json` หลุด · apex 200 · `gh run list` 3 run ล่าสุด **success ทั้งหมด 1 run ต่อ 1 push**
(ล่าสุด `31874632170`) · positive control ผ่าน
**ตู้ `SHOP/` มีใบ `open` 1 ใบ** คือ `Q-WS-146` ซึ่งเป็น**คำตอบ**ที่รอ ack ไม่ใช่งานใหม่
⚠️ เกณฑ์ "ไม่มีใบไหน open" ในบล็อกข้างบน **อ่านผิดได้** เมื่อใบที่ open เป็นคำตอบของเราเอง
⇒ เจอใบ open แล้วต้องเปิดอ่านว่ามันขออะไร ไม่ใช่นับจำนวนแล้วสรุปว่ามีงานเข้า
⚠️ **หน้าเว็บมี 7 หน้าแล้วไม่ใช่ 6** — `/signature/` ขึ้น production ตั้งแต่ 13 ส.ค. (`216ec3a`) โดยเอกสารฉบับ 10 ส.ค. ไม่รู้เรื่อง

> ไม่ปักหมายเลข commit ไว้ที่นี่อีกแล้ว — ปักเมื่อไรก็ล้าสมัยทันทีที่ commit ถัดไปลง
> (รอบแรกล้าสมัยตั้งแต่ commit ตัวเอกสารนี้เอง) · ดูของจริงจาก `git log` ในบล็อกข้างบน

---

## กระดานเป้าหมาย (มติถาวร A8 · วัดสด 2026-09-12T05:1xZ · HEAD `9899440`)

### (1) ครั้งที่แล้วทำถึงไหน
รอบนี้ปิด 3 อย่าง **ขึ้น production ครบ**: 3 จุด AA (`7695aad`) · ระยะ 4 มือถือ (`d9f5f7d`) · แก้วิธีวัดของ probe (`9899440`)
apex 200 · 181,878 ไบต์ · run success ทุกตัว

🔴 **ของที่เอกสารรอบก่อนไม่รู้ และทุก resume ต้องกวาดซ้ำ** — 2026-09-11T14:56Z เจ้าของ merge PR #4
`codex/optimize-seo-for-taitam-d-website` (`f3bd5e3` + `63cb7fb`) ตรงเข้า `main` โดยห้องนี้ไม่ได้ออกและไม่ได้ verify
วัดแล้วดี WS รับตามสภาพ · **WS สั่งว่าห้ามตั้งด่านที่ต้องมือ SHOP ก่อน merge** (รีโปของเจ้าของ เขาเลือก Codex เอง)
⇒ แทนด้วย job CI ที่ทุก PR รันเอง = G13
📌 **ทุก resume ต้องรัน `git log origin/main --not --author=<ห้องนี้>` และ `gh run list` หา deploy ที่ห้องไม่ได้ออก แล้ววัด live**

### (2) เป้าหมายทั้งหมดที่ห้องนี้ตามอยู่
```
Q-SHOP-033 งานหน้าตาเว็บ — ปิดครบทั้งใบแล้ว ไม่มีอะไรค้าง
  G1-G8  ระยะ 1 ถึง 3 + ด่าน check:css + รายงาน       ✅
  G12 3 จุด AA ที่มีมาก่อน                           ✅ 7695aad
  G9  ระยะ 4 มือถือ                                 ✅ d9f5f7d
  G13 job CI visual gate ทุก PR                     ✅ ae1ae3b · ค่าควบคุมตัวที่ 2 ff5db1c
  G14 คำเคาะ 4 ข้อรอบสอง + ลบโค้ดตาย                 ✅ 7cd9dab · 150b703 · ack 07eb4f4c
คิวจาก MKT
  G10 G11 Q-MKT-078 · Q-MKT-079 · Q-SHOP-034 รหัส SB  ✅ MKT ปิดครบ
แถวใหม่จากเจ้าของ
  W-40 ภาพประกอบบนหน้าที่ลูกค้าเห็น                  🟡 แถวถัดไป ทำได้เลย ไม่รอใคร
```

### (3) เสร็จแล้วเท่าไร
**14 จาก 14 ของ Q-SHOP-033 ปิดครบทั้งใบ** · เปิดแถวใหม่ W-40 จากเจ้าของ ⇒ ค้าง 1

### (4) เหลืออะไร ใครถือ รออะไร
```
W-40  SHOP ถือ · ไม่รอใคร · เจ้าของสั่งเอง 2026-09-12 และย้ำว่าเคยสั่งไปแล้วรอบหนึ่ง
      วัดแล้ว: 6 หน้า landing + /book/ มีภาพตกแต่ง 0 · เทียบ / 26 · /tour/ 15 · /services/ 12
      6 หน้านั้นคือหน้าที่ Google Ads ยิงเงินเข้าโดยตรง
      เจ้าของเคาะแล้วว่าภาพ Gemini มีหน้าคนได้ · รายละเอียดครบใน QUEUE-WEB.md แถว W-40
```

### สถานะความต่างสีหลังปิดใบ วัด 2026-09-12T06:5xZ
```
              ก่อนรอบสอง   ตอนนี้
1440              11          2     ทั้ง 2 ตัวเป็นข้อยกเว้นที่ WS เคาะเอง
 375              10          3     ยกเว้น 2 + ไม่ถึง 4.5 อีก 1 จดไว้แล้ว
```
🔑 **ตัวที่เหลือ ห้ามไล่ต่อโดยไม่ถาม** ชื่อร้าน 4.19 เป็นโลโก้ตัวอักษร ยกเว้นตาม WCAG ·
สร้อย 4.25 และป้าย `KING'S CROSS • LONDON` 4.35 เป็น ink เต็มแล้ว ไปต่อต้องแตะค่าสี ⇒ T11 สั่งให้หยุด ·
ทอง `King's Cross` 2.87 ยกเว้นเพราะข้อความเดียวกันอ่านได้อยู่เหนือมัน 1 บรรทัด
📌 **ทั้ง 3 ตัวเป็น 11 ถึง 14px ⇒ เข้าเกณฑ์ large text ไม่ได้** (ต้อง 24px หรือ 18.66px ที่หนา 700)
⇒ ใครคิดจะแก้ด้วยน้ำหนักตัวอักษร วัดขนาดก่อน **น้ำหนักเปลี่ยนค่าความต่างสีไม่ได้**

### ผลระยะ 4 ที่วัดได้ (วัดซ้ำบน production ตรงกับ dev ทุกค่า)
```
                     ก่อน       หลัง
หัวเว็บกินจอ          156px       86px
--nav-height          120px       96px      เคยจองน้อยกว่าหัวเว็บ 36px = เนื้อหาถูกทับจริง
ความสูงหน้าแรก 375  19,978px   19,666px
พื้นที่สว่าง 375      31,264     31,676     เฉลี่ย 22.1 → 22.7
พื้นที่สว่าง 1440      3,643      3,643     จอใหญ่ไม่ขยับเลย
น้ำหนัก apex        176,012    181,878     +3.3% เพดาน 15%
```

## 🔬 `scripts/contrast-probe.mjs` — อ่านให้จบก่อนใช้ และก่อนเชื่อเลขจากมัน

**วิธีที่ใช้ตอนนี้: พื้นอ่านจากพิกเซล (มัธยฐานความสว่างในกรอบ) · ตัวอักษรอ่านจาก CSS ผสมความโปร่งลงบนพื้นที่วัดได้**
`MATCH=<regex>` พิมพ์ค่าของจุดที่ระบุแม้มันผ่าน · ทุกรอบพิมพ์ค่าควบคุม `#6d5223` บน `#fffdf8` = 7.17 ก่อนรายการ (WS สั่ง)

**ค่าควบคุม 2 ตัว พิมพ์ทุกรอบ ตัวใดไม่ตรง = ออกโดยไม่พิมพ์ผลเลย**
`#6d5223` บน `#fffdf8` พื้นเรียบ = **7.17** · `#f2f4e8` บน `#2b5438` กลางไล่เฉด = **7.78** (วัดได้ 7.43)
📌 ตัวไล่เฉดเข้าใกล้ค่าคาดจาก**ด้านล่างเสมอ** เพราะตัวอักษรสว่างกว่าพื้นทุกพิกเซล เลยดันมัธยฐานขึ้นครึ่งสว่าง
🔑 ***ตัวที่ให้ฟันจริงคือด่านช่วงสี ไม่ใช่ค่าคลาดเคลื่อน*** — สีพื้นที่รายงานต้องเป็นสีที่อยู่ในไล่เฉดจริง
เพราะไม่มีค่าคลาดเคลื่อนไหนแยก "เทียบผิดคู่" ออกจาก "พลาดนิดเดียว" ได้

🔴 **เครื่องมือนี้ผิดมาแล้ว 5 รุ่น และทุกรุ่นพิมพ์รายการที่หน้าตาเหมือนงานที่ต้องทำ**
```
1  ถ่ายตอน section ยัง reveal ไม่จบ                          ฟ้อง 72 จุด
2  รีเซ็ต inline style แต่ framer-motion เขียนทับเฟรมถัดไป       ฟ้อง 42 จุด
3  วัดจากเศษของ element ที่ค้างขอบจอ                          ฟ้อง 36 จุด
4  scrollTo ขยับ rect ทันที แต่ภาพยัง repaint ไม่ทัน            ฟ้อง 33 จุด
5  อ่านสีตัวอักษรจากพิกเซลด้วย ⇒ บนพื้นไล่เฉดมันเอาสีตัวอักษร 2 สีมาเทียบกันเอง
```
🔑 ***รุ่น 5 ไม่ได้หยุดที่ตัวเลขผิด มันเดินจนถึงคำเคาะของ WS และเข้าคิวเป็นงานแล้ว***
รายงานว่าแถบ Academy ได้ 1.90 เป็นจุดที่ตกหนักสุดในหน้า WS เคาะให้เปลี่ยนสี · ของจริงคือ **6.49 และ 3.28 ผ่านทั้งคู่**
🔴 **และค่าควบคุม 7.17 จับรุ่น 5 ไม่ได้** เพราะค่าควบคุมเป็นพื้นเรียบทึบ = ทางที่เครื่องมือทำถูกอยู่แล้ว
🔑 ***ค่าควบคุมพิสูจน์ได้แค่ทางที่มันเดิน ไม่ได้พิสูจน์ทางที่มันไม่เดิน*** ⇒ เสนอ WS ให้เพิ่มค่าควบคุมตัวที่ 2 ที่เป็นตัวอักษรบนพื้นไล่เฉด

📌 **น้ำหนักตัวอักษรเปลี่ยนค่าความต่างสีไม่ได้** — ค่านี้คำนวณจากสี 2 สีเท่านั้น
น้ำหนักช่วยทางเดียวคือทำให้เข้าเกณฑ์ large text (18.66px ขึ้นไป และหนา 700) ⇒ ใช้กับลิงก์ 12-14px ไม่ได้
📌 **กับดักเดียวกับ Browser pane** แท็บถูกซ่อน rAF ถูกหรี่ ⇒ หน้าเว็บค้างเบลอครึ่งหน้า
⇒ **visual gate ของห้องนี้ใช้ `scripts/screenshot.mjs` เท่านั้น อย่าใช้ screenshot ของ Browser pane**

📮 **ตู้ GLOBAL ฟ้องใบ open 16 ใบว่าห้องนี้ยังไม่ ack — ตรวจรายใบแล้ว 2026-09-12T05:3xZ ไม่มีใบไหนจ่าหน้าถึง SHOP**
14 ใบจ่าหน้า ACAD EDU VID IMP OFFICE MKT PEL KWS KSYS KLORE · อีก 2 ใบเป็น `to: GLOBAL` ระดับเครื่อง
(`Q-PWS-001` ลงทะเบียนเครื่องที่ 4 · `Q-PWS-003` บั๊ก lease) ⇒ **อ่านเป็นข้อมูล ไม่ใช่งานของห้องนี้**
🔑 ***hook ตัวนี้ฟ้องทุกใบที่ไม่มีไฟล์ ack ของเรา ไม่ได้ดูว่าใบนั้นจ่าหน้าถึงใคร*** ⇒ รอบหน้าอย่าไล่ ack ทั้ง 16 ใบ
ให้ `grep -m1 "^to:"` รายใบก่อน ใช้เวลาไม่ถึงนาที · ถ้ามีใบใหม่ที่จ่าหน้า SHOP หรือ ALL จริง อันนั้นต้องตอบ

🔑 ***ทุกตัวเลขในกระดานนี้วัดสดตอนเขียน ไม่ได้คัดจากรอบก่อน*** รอบหน้าต้องวัดใหม่ก่อนเชื่อ

## 1 · ห้องนี้คือใคร (อ่าน [`../../CLAUDE.md`](../../CLAUDE.md) ให้ครบก่อน)

**Taitam-D Shop Lead** — เจ้าของ repo เว็บไซต์ taitam-d.com **คนเดียว**
ถืออำนาจ **Lead + Concept รวมกัน** (เจ้าของมอบ 2026-08-09) ⇒ สำรวจเอง · สร้าง/จัดลำดับคิวเอง · เคาะมติเชิงเทคนิคเองได้
**ยกเว้น** มติที่เป็นของเจ้าของธุรกิจ (ถ้อยคำแบรนด์ · ราคา · ขอบเขตธุรกิจ) → ขึ้น [`DECISIONS-BOARD.md`](DECISIONS-BOARD.md) รอเคาะ

ห้องอื่นส่ง request มาที่นี่ ไม่แก้ repo เอง — Marketing Lead ยืนยันรับทราบแล้ว

---

## 2 · ทำอะไรไปแล้วใน session นี้ (4 commit)

| commit | เรื่อง |
|---|---|
| `7199546` | วางกติกา Lead/Executor (delta ชี้ไปไฟล์กลาง ไม่ fork) + กฎ 4 ข้อที่ห้ามย้อน + สร้างคิวและบอร์ดมติ |
| `6585eaa` | รับ port band `3300–3399` + ทำ `distDir` ให้สลับได้ + `.gitignore` `.next-*` |
| `726e4d2` | ลบ `pages.yml` — เดิม 2 workflow แย่ง deploy กันจนผลขึ้นกับจังหวะ |
| — | เข้า Gumon Lore (`lore-add`) · ⚠️ **hook เต็มที่ต่อเมื่อเปิด session ใหม่** |

---

## 3 · งานค้าง

### 🔵 งานหน้าตาเว็บ (`Q-SHOP-033`) — ระยะ 1 ถึง 3 ขึ้นแล้ว · **ระยะ 4 ⛔ ยังไม่เปิด** (2026-09-11 · สถานะสดดูที่กระดานเป้าหมายข้างบน)

เจ้าของสั่งว่าเว็บ *"เหมือนเว็บงานวิชาการมากกว่าเป็นทำความสวยความงาม"* ⇒ แผน 4 ระยะอยู่ที่
[`DESIGN-REFRESH-2026-09-11.md`](DESIGN-REFRESH-2026-09-11.md) · **การเคาะปล่อยแต่ละระยะเป็นของ `WS` ⛔ ไม่ใช่ของเจ้าของ**
(เจ้าของสั่งเอง 2026-09-11 *"คุณสามารถส่งเรื่องให้ WS ตัดสินใจให้ได้เลยนะครับ"* เหตุ เขา ⛔ ไม่ได้อยู่หน้าจอตลอด งานจะสะดุด)

```
ระยะ 1 ฟอนต์กับที่ว่าง        live  commit 731561c  run 34573446566
ระยะ 2 ภาพที่เรามีอยู่แล้ว      live  commit a169305  run 34575029985 · ภาพคู่ 604c21b
ระยะ 3 พื้นครีมสลับจังหวะ      live  e0d52ae · fcb0b62 · c9f0b1c · bbc18b6 · ภาพคู่ docs/design-refresh/phase3/
ระยะ 4 มือถือ                ⛔ ยังไม่เปิด
```

✅ **ของค้างจากระยะ 2 ปิดแล้ว** การ์ด hero ทั้งใบ (4 ช่อง `d86ac05` + 4 บรรทัดบน `bbc18b6`) ผ่าน AA ทุกค่าจากพิกเซลจริง

📌 **T11 คือเส้นที่ห้ามข้ามในทุกงานหน้าตาหลังจากนี้** ⛔ ห้ามแก้ค่าสีใน `tailwind.config.ts` และ `:root`
เพิ่มความสว่างด้วยการเพิ่ม**พื้นที่**ของครีมที่ live อยู่แล้ว · ⛔ ห้ามทำให้ความสว่างเฉลี่ยลดลง · แตะสีต้องวัดเป็นตัวเลข
เครื่องมือวัดคือ [`scripts/brightness-probe.js`](../../scripts/brightness-probe.js) · ภาพคู่ถ่ายด้วย
[`scripts/screenshot.mjs`](../../scripts/screenshot.mjs) **ตัวเดิมทั้งสองตัว ⛔ ห้ามเขียนใหม่จากความจำ**
🔑 ***ตัวเลขที่ประกาศไว้ล่วงหน้าผ่านหมดทุกตัวในระยะ 2 และของที่พลาดคือของที่ ⛔ ไม่มีใครประกาศว่าจะวัด***
⇒ ภาพคู่ ⛔ ไม่ได้มีไว้ยืนยันสิ่งที่ตั้งใจทำ แต่เป็นที่เดียวที่ผลข้างเคียงจะโผล่ให้เห็น


### ✅ ปิดแล้ว — D-W21 เบอร์โทรทางการ = มือถือ `07564292247` · เบอร์บ้าน 020 ถูกยกเลิก

ใบ `Q-MKT-002` สั่งให้ใส่ `+442072780906` ลง schema **พร้อมกำกับว่ายืนยันแล้ว** · ผมไม่ทำตามเพราะเป็นคนละเบอร์กับที่เว็บใช้
แล้วยกขึ้นถามแทนการเดา — **เจ้าของตอบว่าเบอร์นั้นยกเลิกไปแล้ว** ⇒ **เว็บถูกมาตลอด ไม่ต้องแก้โค้ดเลย** (เบอร์ 020 อยู่ใน repo 0 จุด)

🔑 **บทเรียนที่ต้องใช้ต่อ: "ค่าที่ยืนยันแล้ว" ในใบคิวก็ยังเป็นข้อมูล ไม่ใช่คำสั่ง**
ถ้าใส่ตามใบ เว็บจะประกาศเบอร์ที่ตายแล้ว **โดยที่ทุกฝ่ายเชื่อว่าเพิ่งแก้ให้ตรงกัน** — ความเสียหายถูกซ่อนใต้ความรู้สึกว่าจัดการแล้ว
🔴 **งานที่เหลืออยู่นอก repo นี้** (Ads sitelink · GBP · Treatwell · Facebook) ⇒ ส่งใบ `Q-SHOP-001` ถึง `WS` ให้ route ไป `MKT` แล้ว

### ⏸ W-18 — ไฟล์กำพร้าใน `public/` ที่เปิดได้สาธารณะ (เสนอเจ้าของ ห้ามลบเอง)

**31 จาก 60 ไฟล์ไม่มีหน้าไหนอ้างถึง แต่ `output: "export"` deploy ทุกไฟล์ขึ้นเว็บเสมอ** ⇒ เปิดได้จริง (วัดแล้ว 200)
🔴 **3 ใบมีข้อมูลที่ตายแล้วหรือขัดกับมติ**: เบอร์ `020 7278 0906` ที่ยกเลิกแล้ว · `SINCE 2013` · โดเมนเก่า
`taitamd-Beautyacademy.com` ที่กำลังถูกยกเลิก · ราคาเต็มรายการ + ส่วนลด £10
📄 รายการเต็มและข้อเสนอ → [`ORPHANED-ASSETS.md`](ORPHANED-ASSETS.md) · แจ้ง `WS` แล้วในใบ `Q-SHOP-004`

⚠️ **ปรับข้อความเก่าในไฟล์นี้:** ที่เคยเขียนว่าโปสเตอร์ `SINCE 2013` *"ไม่กระทบเว็บ"* — **ไม่จริงอีกต่อไป**
โปสเตอร์ใบนั้นอยู่บนโดเมนเราและเปิดได้สาธารณะ · **ไม่ได้แปลว่า `D-W1` (ปีก่อตั้ง 2009) ถูกรื้อ** — มติยังยืน
สิ่งที่เปลี่ยนคือ **ขอบเขตของมัน** ไม่ใช่ข้อสรุป · 🔴 ห้ามใช้บรรทัดนี้เป็นเหตุตั้งข้อสงสัยกับเลข 2009 อีก

### 🛑 คิวงานเทคนิค — **ไม่ว่าง แต่ถูกพักทั้งคิว** (แก้ 2026-08-14)

⚠️ **หัวข้อนี้เคยเขียนว่า "ไม่มีงานเทคนิคอื่นค้าง · ตารางคิวปัจจุบัน *ว่าง*" (ยืนยัน 10 ส.ค.) — เท็จตั้งแต่ 13 ส.ค.**
ของจริงตอนนี้: **W-24** Gallery พร้อมคำบรรยาย (โจทย์ลูกค้า 13 ส.ค. · บรีฟครบใน [`GALLERY-BRIEF.md`](GALLERY-BRIEF.md)) ·
**W-23** เกณฑ์ `LAW` สองรายการต้องคำนวณเข้ากันได้ · **W-18** ไฟล์กำพร้าเหลือ 19 ใบ 3.3 MB
⇒ **ทั้งสามแถว ⏸ พักตาม `Q-WS-101` ข้อ 8** พร้อมเงื่อนไข `🔁 คืนสถานะเมื่อ:` รายแถวใน [`QUEUE-WEB.md`](QUEUE-WEB.md)

🔑 **บทเรียนของบรรทัดที่เพิ่งลบ:** ประโยค *"คิวว่าง"* ในเอกสารที่ไม่ได้อัปเดตตามคิว **อันตรายกว่าการไม่เขียนอะไรเลย** —
ห้องใหม่ที่อ่านแล้วเชื่อจะสรุปว่าตัวเองว่างงาน แล้วไปเริ่มของใหม่ทับงานที่ค้างอยู่จริง
⇒ **สถานะคิวมีแหล่งจริงแหล่งเดียวคือ [`QUEUE-WEB.md`](QUEUE-WEB.md)** · ไฟล์นี้ห้ามสรุปซ้ำ ให้ชี้ไปอย่างเดียว

**W-7 · W-8 · W-9 ปิดครบแล้วตั้งแต่ 2026-08-09** (ส่วนนี้ยังจริง)

- **W-7 · W-9 ปิดโดยการแก้จริง** — ยก action ทั้งชุด + `node-version 20→22` · ยืนยันจากล็อก CI ว่า
  annotation "Node 20 is being deprecated" เหลือ **0 ครั้ง**
- **W-8 ปิดโดยการเคาะว่าไม่ทำ ไม่ใช่การเลื่อน** — static export ไม่มี server runtime และ
  `images.unoptimized = true` ⇒ `sharp` ไม่ถูกเรียก · เงื่อนไข 3 ข้อที่จะทำให้กลับมาทบทวนอยู่ใน [D-W13](DECISIONS-BOARD.md)

**W-3 (ESLint CLI) เสร็จแล้ว** — `next lint` หายไปจาก repo แล้ว ตอนนี้ gate คือ `eslint . --max-warnings=0`
ตาม [D-W9](DECISIONS-BOARD.md) · 🔑 **warning = ตก gate** อย่าปล่อยผ่านเพราะเห็น exit code 0

### ✅ ปิดแล้ว — D-W10 นามสกุลผู้ก่อตั้ง = `Pomnongsan` · **ถูกยกมาถามซ้ำแล้ว 1 ครั้ง**

🔁 **2026-08-10: `LAW` ผ่าน `WS` แจ้งว่า "เว็บร้านสะกด `Phomnongsan` 3 จุด ให้ตรวจแล้วแก้ให้ตรงกัน" — วัดแล้วกลับด้าน**
ซอร์สและ production ทั้ง 6 หน้าสะกด `Pomnongsan` (`/story/` 6 ครั้ง) · **`Phomnongsan` เหลือ 0 จุด**
⇒ ตอบกลับด้วยผลวัดในใบ `queue/WS/Q-SHOP-002-*.md` · **ไม่แก้อะไร**
🔑 **คำสั่ง "ไปตรวจแล้วแก้ให้ตรงกัน" อันตรายกว่าคำสั่ง "แก้เป็น X" เมื่อข้อเท็จจริงตั้งต้นผิด** —
แบบหลังมีค่าให้เทียบแล้วสะดุด แบบแรกส่งต่อความมั่นใจโดยไม่ส่งค่าที่ตรวจสอบได้ไปด้วย
⇒ ห้องที่ทำตามอย่างซื่อสัตย์จะไปหาหลักฐานภาษาศาสตร์ ได้ `Phrom-` ที่ "ถูกหลัก" แล้วเขียนทับฉบับที่เจ้าตัวเลือก

ครูนกเคาะเอง เว็บแก้แล้ว (เดิมสะกด `Phomnongsan` ผิดมาตลอด)
🔴 **ห้ามไป "แก้ให้ถูกหลัก" ภายหลัง** — `พรหม` ถอดตามราชบัณฑิตยสภาได้ `Phrom` แต่ **ไม่ใช่ฉบับที่เจ้าตัวใช้**
ฉบับนี้มาจาก **เจ้าตัว** ไม่ใช่หลักการถอดเสียง ⇒ หลักภาษาไม่ใช่เหตุผลที่ใช้ค้านได้ · ดู [D-W10](DECISIONS-BOARD.md)

### ✅ ตรวจแล้ว — เว็บร้าน **ไม่มี** พื้นผิวชื่อนิติบุคคลเลย

ห้อง Concept เตือนว่า `D-71` คุมถ้อยคำที่เราผลิต **แต่เปลี่ยนชื่อที่ผูกกับใบเสร็จ/ใบกำกับไม่ได้** (บัญชี Google Ads
มีชื่อผู้จ่ายเป็น `TaiTam-D`) ⇒ ตรวจแล้ว **เว็บนี้ไม่มีหน้า terms · ไม่มีใบเสร็จ · ไม่มีเลขทะเบียนบริษัท/VAT เลย**
และ `legalName` ใน [`lib/site.ts`](../../lib/site.ts) **เป็น ASCII อยู่แล้วก่อนการกวาด** — ยืนยันด้วย `git show 2a1f557^`
⇒ **การกวาด U+2011 ไม่ได้ไปแตะชื่อนิติบุคคล** · ถ้าวันหนึ่งเว็บมีหน้า terms ขึ้นมา ชื่อตรงนั้นอยู่ใต้ `D-73` ข้อ 11 **ไม่ใช่ `D-71`**

### ✅ ปิดสนิท — D-W1 ปีก่อตั้ง = **2009** ตามที่เว็บเขียน (ครูนกยืนยันเอง 2026-08-09)

🔑 **หลักการที่ปิดเรื่อง: "ปีก่อตั้ง ≠ ปีเปิดกิจการ ≠ ปีจดทะเบียนบริษัท"**
⇒ วันจดทะเบียนใน Companies House (2015 · 2018 · 2019) **ไม่ใช่หลักฐานค้าน** เป็นคนละสิ่งกันตั้งแต่ต้น
⇒ `Founded in 2009` · `ESTABLISHED 2009` · `foundingDate: "2009"` **ถูกหมด ไม่ต้องแก้อะไรเลย**

🔴 **ห้ามเซสชันไหนหยิบวันจดทะเบียนบริษัทมาตั้งข้อสงสัยกับตัวเลขนี้อีก** — ยกมาแล้วและตอบแล้ว
โปสเตอร์หน้าร้าน SINCE 2013 ยังไม่มีคำอธิบาย **แต่ไม่กระทบเว็บ** และไม่ใช่เรื่องของห้องนี้
📄 ที่มาและขอบเขตการค้นอยู่ใน [`RESEARCH-2026-08-09-founding-year.md`](RESEARCH-2026-08-09-founding-year.md)

### ✅ แก้แล้ว — ถ้อยคำ Academy บนหน้า `/cookies` (เดิมเป็นเท็จอยู่บน production)

เดิมเขียนว่า Academy *"sets no cookies of its own"* — **เท็จ** (`curl -I` → `set-cookie: NEXT_LOCALE=en`)
ตอนนี้เขียนว่า **ไม่มีคุกกี้วิเคราะห์/โฆษณา มีแต่คุกกี้จำเป็นที่ไม่ต้องขอความยินยอม** ซึ่งตรงกับที่วัดได้

🔴 **ข้ามด่าน Legal ไปโดยเจ้าของให้สิทธิ์ตรง ๆ** ([D-W16](DECISIONS-BOARD.md)) — **เป็นการข้ามด่าน ไม่ใช่การยกเลิกด่าน**
⇒ **ครั้งหน้าที่ไม่มีคำสั่งตรงจากเจ้าของ ให้กลับไปรอ Legal ตามเดิม** · Legal ยังแก้ถ้อยคำนี้ทีหลังได้เต็มที่

🔑 **แก้สองรอบในวันเดียว และรอบแรกคือบทเรียน** — ฉบับแรกเขียนว่า Academy *"ไม่มีคุกกี้วิเคราะห์ ไม่ต้องขอ consent"*
ซึ่ง **จริงวันนี้แต่จะเท็จทันทีที่ GA4 ขึ้นฝั่ง Academy** ⇒ **ถอดระเบิดลูกหนึ่งแล้ววางลูกใหม่แทน**
ฉบับที่ใช้จริงตอนนี้ **เลิกบรรยายคุกกี้ของเว็บอื่น** เหลือแต่ข้อความที่เป็นของเราและจริงตลอดไป
แล้ว **ลิงก์ไปนโยบายของเขาเอง** (`academy.taitam-d.com/en/policies/cookie-policy` — ยืนยันแล้ว 200)

🔴 **หลักการที่ต้องใช้ต่อ: อย่ายืนยันข้อเท็จจริงเกี่ยวกับระบบที่เราไม่ได้ควบคุม ลงในเอกสารกฎหมายของเรา**
มันจะผิดโดยที่เราไม่มีทางรู้ · ชี้ไปแหล่งของเจ้าของระบบนั้นแทน แล้วเอกสารเราจะไม่ผิดตามเขา
⇒ **ผลพลอยได้: GA4 ขึ้นฝั่ง Academy เมื่อไรก็ได้ หน้าเราไม่ต้องแก้ตามอีกแล้ว**

🔑 **บทเรียนที่แพงที่สุดของรอบนี้:** Marketing วัด "ไม่มี GA4/GTM/pixel · `dataLayer` ว่าง" แล้วสรุปว่าถ้อยคำยังจริง
**แต่นั่นวัด tag ไม่ได้วัดคุกกี้** — `NEXT_LOCALE` ไม่โผล่ในการตรวจแบบนั้นเลย
⇒ **"วัดแล้ว" ต้องถามต่อว่า *วัดอะไร* — เครื่องมือที่ตอบว่าไม่มี อาจแค่ไม่ได้มองตรงนั้น**

### ✅ ปิดแล้ว — ลิงก์ Academy ชี้ `academy.taitam-d.com` (เจ้าของเคาะ 2026-08-09)

โดเมนเก่า `taitamd-beautyacademy.com` **เราถือเองบน Hostinger** (ไม่ใช่ทรัพย์สินหลุดมือ) **และจะถูกยกเลิกทั้งหมด**
⇒ ลิงก์เดิมจะพังแน่นอน ไม่ใช่แค่ชี้ผิดชื่อ · แก้ **ค่าเดียว** `SITE.academy` ครอบคลุมทั้ง 9 จุด

### 🔴 โดเมนแบรนด์ที่หลุดมือไปแล้วจริง — ตรวจแล้วเว็บเราไม่ได้ลิงก์ไป

`taitamdbeautyacademylondon.com` และ `hub.` ของมัน **ไม่ได้อยู่ในมือเราแล้ว** — Google ยัง index ไว้ว่า
*"TaiTamD Beauty Academy London | All Online Courses"* แต่ของจริงคือ **เนื้อหาสแปมที่เปลี่ยนไปตามครั้งที่เรียก**
(ครั้งหนึ่งได้ title เว็บพนันอินโดฯ · `hub.` เด้ง **20 ครั้ง** ไปจบที่ `barbaraross.de`)

✅ **ตรวจแล้วว่าไม่มีอะไรของเราชี้ไปที่นั่น** — รีโปนี้ 0 จุด · production ทั้ง 6 หน้า 0 จุด · แม้แต่ WordPress ตัวเก่าก็ 0
⇒ **ไม่ใช่งานของห้องนี้** ส่งให้ Marketing (แบรนด์/ผลค้นหา) และ Legal (เครื่องหมายการค้า) แล้ว

🔑 **แต่เก็บไว้เป็นหลักฐานว่าความเสี่ยงนี้ไม่ใช่ทฤษฎี** — ตอนคุยเรื่องแผนย้ายโดเมน ผมเตือนว่า *"ถ้าโดเมนหลุด
ลิงก์ของเราจะพาลูกค้าไปหน้าที่ถูกยึด"* · **มันเกิดขึ้นแล้วกับโดเมนพี่น้องของมันจริง ๆ**
⇒ ตอกย้ำว่า `taitamd-beautyacademy.com` ที่กำลังจะยกเลิก **ต้องวาง 301 ก่อนปล่อย ไม่ใช่ปล่อยหมดอายุเฉย ๆ**

### 🔴 ของที่เหลือจากการค้นครั้งนั้น — ส่ง Legal แล้ว ไม่ใช่งานเว็บ

**`TAI TAM-D ACADEMY LTD` เลิกกิจการตั้งแต่ 8 มี.ค. 2022** ⇒ `Taitam-D Academy London` เป็น **ชื่อทางการค้า
ไม่ใช่นิติบุคคลที่ยังมีชีวิต** · ตัวที่ active ตัวเดียวคือ `TAITAM-D MANAGEMENT LTD` (`11141046`)
ไม่กระทบสิ่งที่เว็บแสดง (ชื่อถูกตามที่เจ้าของสั่ง) แต่กระทบคำถามว่าใครออกใบรับรองให้ผู้เรียน — **เกินขอบเขตห้องนี้**

### ✅ ปิดแล้ว — D-W14 ชื่อสถาบันบนเว็บ = `Taitam-D Academy London`

เจ้าของยืนยันว่าเป็นหน่วยเดียวกับที่เว็บเคยเรียกว่า `Beauty Academy` · แก้ 4 จุดแล้ว
🔑 **ป้ายนำทางที่เขียนแค่ `Academy` ไม่ผิด อย่าไปไล่แก้** — เป็นป้ายนำทาง ไม่ใช่การระบุชื่อสถาบัน
　 ⚠️ ข้อนี้เป็น **การตีความเจตนา `D-L1` โดยห้อง Concept ไม่ใช่คำสั่งเจ้าของ** — ถ้าเจ้าของสั่งให้ขึ้นชื่อเต็มทุกที่ คำสั่งท่านชนะ
🔑 **URL เฟซบุ๊กที่มีคำว่า `BeautyAcademy` ห้ามแตะ** — เป็นที่อยู่ของทรัพยากรจริง แก้แล้วลิงก์พัง

### 🛑 ระดับความละเอียดที่เจ้าของกำหนด (2026-08-09) — อ่านก่อนจะเริ่มสืบอะไร

> *"ตอนนี้เราเริ่มละเอียดเกินกว่าที่จะทำ MVP แล้ว · ทำเท่าที่ทำได้ · ข้ามได้ข้าม อย่าเพิ่งหาหลักฐาน"*

**ปิดแล้วทั้งหมด ไม่ต้องสืบต่อ:** `Taitam-D Group` คงไว้ (ตั้งใจใช้ ครูนกมีหลายกิจการ · [D-W19](DECISIONS-BOARD.md)) ·
`VTCT-accredited` คงไว้ (เจ้าของยืนยันหนังสือปี 2019 ยังใช้อยู่) · `Certified NLP Master` คงไว้ **ข้ามการสืบ** ([D-W20](DECISIONS-BOARD.md))

🔑 **นี่ไม่ใช่แค่การปิดสามข้อ แต่เป็นการตั้งระดับของทั้งห้อง** — งานสืบสวนที่ทำมาทั้งวัน (ทะเบียนบริษัท · WHOIS ·
ทะเบียน VTCT) **มีประโยชน์จริงแต่เกินระดับที่โปรเจกต์ต้องการตอนนี้** ⇒ เจอของน่าสงสัย **บันทึกไว้ แล้วเดินต่อ**
อย่าไล่จนจบ เว้นแต่มันบล็อกงานที่ต้องส่งจริง หรือเจ้าของสั่ง

### ~~🔵 รอข้อเท็จจริง~~ — ปิดหมดแล้ว

**ห้องนี้มีอำนาจเคาะ แต่ไม่มีข้อมูล** — อำนาจตัดสินใจไม่ได้แปลว่ารู้ว่าใบ NLP ของครูนกใครออกให้
⇒ **สิ่งที่เคาะไปแล้วคือ "ระหว่างรอจะทำตัวอย่างไร"** (คงข้อความเดิม · ห้ามเพิ่มคำกล่าวอ้างใหม่ · ห้ามเก็บกวาดถ้อยคำตรงนั้น)

🔑 **แต่คำกล่าวอ้างสองข้อนี้น้ำหนักไม่เท่ากัน — แยกเป็น D-W12a/D-W12b แล้ว ห้ามคลุมด้วยเหตุผลเดียว**
`VTCT` **มีหนังสือรับรอง** (2019 · ศูนย์ `2547/001`) แค่เก่า ⇒ ถามว่า *"ยังใช้ได้ถึงเมื่อไร"*
`Certified NLP Master` **ไม่มีหลักฐานที่ไหนเลย** ⇒ ถามว่า *"ใครออกให้ ปีไหน"* · **ถ้าถามแล้วไม่มีคำตอบ น้ำหนักจะเอียงไปทางเอาออก**

### ❌ ไม่มีงานค้างจากห้องอื่น

Academy launch 16 ส.ค. ยังไม่มีทราฟฟิกมาฝั่งเว็บร้าน

**Marketing audit 2026-08-09 — ปิดครบแล้ว ไม่มีอะไรค้าง** แต่มีสองข้อที่ต้องรู้ไว้กันไล่ผี:

- 🔑 **`phone_click` / `whatsapp_click` ไม่ได้พัง ห้ามไป "แก้"** — Marketing ยิง synthesised click
  บน production แล้วทั้งคู่ยิง event ครบพร้อม parameter · ที่ไม่โผล่ในคอนโซล Google Ads
  คือ **ยังไม่มีใครกดจริง** ไม่ใช่บั๊กในโค้ด ([`components/Analytics.tsx:108`](../../components/Analytics.tsx))
- 🔑 **แอดหยุดวิ่งเพราะบัตรเครดิตถูก declined** (ค้าง £10.25 เจ้าของจัดการเอง) ไม่ใช่เรื่องของเว็บ ⇒
  ทราฟฟิกหายช่วงนี้ **ห้ามไปหาสาเหตุในโค้ด** (ก่อนหยุด: 242 impressions · 13 clicks · CTR 5.37% · £10.04)

**Meta Pixel — เจ้าของเคาะแล้วว่า "ยังไม่ยิง Meta ads" (2026-08-09 · Marketing Lead แจ้งต่อ)**
⇒ Marketing **ยกเลิกคำขอ** และสั่งว่า **ไม่ต้องแตะ tag layer เลย** · ดู [`DECISIONS-BOARD.md` D-W7](DECISIONS-BOARD.md)
เก็บผลประเมินไว้เผื่อวันหนึ่งกลับมาคุยใหม่ **แต่ตอนนี้ไม่ใช่งาน อย่าไปเริ่มเอง**:
`AW-` ~1 บรรทัดต่อจาก [`Analytics.tsx:74`](../../components/Analytics.tsx)
(ขี่ `gtag` เดิม เคารพ Consent Mode อัตโนมัติ) · **Meta Pixel ครึ่งวัน–1 วัน** เพราะ `fbq`
ไม่รู้จัก Consent Mode ต้องเดินสาย consent เองและกันไม่ให้ `_fbp` รั่วลง academy
🔴 **ต้นทุนจริงอยู่ที่เอกสาร ไม่ใช่โค้ด** — ทั้งคู่เพิ่มคุกกี้ ⇒ กฎข้อ 2 บังคับให้วัดทะเบียนใหม่บน
production แล้วแก้ [`app/cookies/page.tsx`](../../app/cookies/page.tsx) + ข้อความ banner (ตอนนี้เขียนแค่
"Google Analytics & Google Ads") ⇒ **ต้องผ่านห้อง Legal ก่อนขึ้นจริง**

---

## 4 · เรื่องที่ห้องอื่นค้างอยู่ ไม่ใช่ของเรา (อย่าไปทำแทน)

| เรื่อง | อยู่ที่ใคร |
|---|---|
| ให้ §4.1 ของ `solution-taitamd-platform` เปลี่ยนเป็น **ตัวชี้** มาที่ `gumon-localdev/registry.json` แทนการเป็นสำเนา | **Gumon Workspace Lead** (เดิมชื่อ Local Dev Lead) รับไปแล้ว — เขาบอกชัดว่า **เราไม่ต้องไปขอ Platform Lead เอง** |
| แจ้ง `solutions-c2-rice-flow-mobile` และ `vera-d` ว่า bind พอร์ตทับ band คนอื่น | **Gumon Workspace Lead** (เดิมชื่อ Local Dev Lead) |
| mark `whatsapp_click` เป็น Key event + import เป็น conversion (งานในคอนโซล ไม่แตะ repo) | **Marketing Lead** |

---

## 5 · กับดักที่จะเจอถ้าไม่รู้

1. **รัน dev server แล้ว Next เขียนทับ `next-env.d.ts` + `tsconfig.json`** ให้ชี้ `.next-claude`
   หลุด commit ขึ้นไป = **build ฝั่ง human พัง** ⇒ หยุด server แล้ว `git checkout -- next-env.d.ts tsconfig.json` **ทุกครั้ง** + ยืนยันพอร์ตปิดด้วย `lsof`
2. **push `main` = deploy ขึ้นเว็บจริงทันที** และมีงบ PMax ยิงมาที่ apex อยู่ ⇒ verify เขียว **ก่อน** merge เสมอ
3. **ไม่มี test runner** ⇒ `npm run build && npm run lint` + **visual gate** คือทั้งหมดที่มี ตาคือด่านสุดท้ายจริง
4. **ห้าม `git add -A`** — repo มี `.env` · `out/` · `.next*` อยู่ในเครื่อง stage เป็นราย path เสมอ
5. **ห้ามคัดตารางพอร์ตมาเก็บใน repo นี้** — แหล่งจริงคือ
   `gumon-workspace/machines/<machine-id>/registry.json → devServerPorts` (ยืนยัน path นี้ 2026-08-10)
6. **ใช้ `npm` เท่านั้น ห้าม `pnpm install` ในรีโปนี้** — CI ตรวจ package manager แบบ
   "มี `yarn.lock` ไหม ถ้าไม่มีก็ `npm ci`" ⇒ **มันไม่รู้จัก pnpm** · ถ้าใครลง dep ด้วย pnpm
   `package-lock.json` จะไม่ขยับตาม แล้ว CI จะ build ด้วยของคนละชุดกับที่เรา verify (เคยแตกมาแล้ว ดู D-W8)
7. 🔴 **แก้ 2026-09-09 — ข้อนี้กลับด้านแล้วตามคำสั่งเจ้าของ (`Q-WS-453`)**
   เจ้าของเปลี่ยนอีเมลหลักของบัญชีครูนกใน Google Workspace **จาก `thanyarat.po@` มาเป็น `info@taitam-d.com`**
   ⇒ **`info@taitam-d.com` เป็น *บัญชี* ที่ล็อกอินได้แล้ว** · ที่อยู่เดิมมักเหลือเป็น alias (WS ระบุ ห้องนี้ยังไม่ได้วัดเอง)
   ⇒ บริการที่ต้องล็อกอินแทนเว็บนี้ (Search Console · analytics · โฮสต์ · โดเมน) **ใช้ `info@` ได้เลย**

   <details><summary>ถ้อยคำเดิมก่อน 2026-09-09 — เก็บเป็นประวัติตามข้อห้ามเขียนทับของ <code>Q-WS-453</code></summary>

   **`info@taitam-d.com` เป็น *กลุ่ม* ไม่ใช่ *บัญชี*** (มติ komphet 2026-08-10) — **ส่งถึงได้ แต่ login ไม่ได้**
   บัญชีจริงที่ล็อกอินได้คือ `thanyarat.po@taitam-d.com` (Admin สูงสุดของ Google Workspace)
   ✅ **ที่เว็บใช้อยู่ 2 จุดถูกต้องแล้ว ไม่ต้องแก้** — [`lib/site.ts`](../../lib/site.ts) (`email` → ไหลเข้า JSON-LD)
   และ [`app/cookies/page.tsx`](../../app/cookies/page.tsx) (ที่อยู่ติดต่อเรื่องความเป็นส่วนตัว) — **ทั้งคู่เป็นการ *รับเมล* ล้วน**
   🔑 **ที่อยู่ติดต่อบนหน้านโยบาย *ควร* เป็นกลุ่ม ไม่ใช่เมลส่วนตัว** — กลุ่มอยู่ต่อได้เมื่อคนเปลี่ยน
   ⇒ ห้ามใครไป "แก้ให้เป็นเมลจริง" ภายหลัง · 🔴 **แต่ถ้าวันไหนต้องสมัคร/ล็อกอินบริการแทนเว็บนี้**
   (Search Console · analytics · โฮสต์ · โดเมน) **ต้องใช้ `thanyarat.po@` — `info@` จะล็อกอินไม่ได้**

   </details>

   ⚠️ **สิ่งที่ ⛔ ไม่เปลี่ยน และสิ่งที่เปลี่ยนความหมายไปโดยที่ตัวอักษรบนเว็บเท่าเดิม**
   ที่อยู่ 2 จุดบนเว็บยังเป็น `info@taitam-d.com` เหมือนเดิมทุกตัวอักษร ⇒ **⛔ ไม่มีอะไรต้องแก้บนหน้าเว็บ**
   **แต่เหตุผลที่เคยเขียนไว้ข้างบนตายแล้ว** — ตอนนั้นเลือกที่อยู่นี้เพราะมันเป็น *กลุ่ม* ซึ่งอยู่ต่อได้เมื่อคนเปลี่ยน
   วันนี้มันคือ **กล่องที่ผูกกับบัญชีล็อกอินของคนคนหนึ่ง**
   🔑 ***ตัวอักษรที่ไม่ขยับ ทำให้ไม่มีใครเห็นว่าเหตุผลใต้มันขยับไปแล้ว***
   ⇒ ห้องนี้รายงาน `WS` ไว้ใน ack ของ `Q-WS-453` แล้ว · **เป็นเรื่องของ `LAW` กับเจ้าของ ⛔ ไม่ใช่ของห้องนี้ตัดสิน**
8. **`NEXT_DIST_DIR` ทำให้ผลลัพธ์ static export ไม่ลง `out/`** — มันไปกองอยู่ใน distDir นั้นแทน
   (`.next-claude/index.html`, `.next-claude/sitemap.xml`, …) ⇒ **`out/` ในเครื่องจะเป็นของเก่าค้างและหลอกตา**
   เจอจริงตอน W-5: build เขียวแล้วแต่ `cat out/sitemap.xml` ยังโชว์ของเดิม เกือบสรุปว่าแก้ไม่ติด
   CI ไม่ตั้ง env นี้ (`next build` เปล่า ๆ แล้ว upload `./out`) ⇒ **ของขึ้นเว็บถูกต้อง** แต่เวลา verify ในเครื่อง
   ให้อ่านจาก distDir ที่ตัวเองตั้ง ไม่ใช่ `out/`

---

## 6 · เริ่มงานต่อยังไง

1. รันบล็อก [`## Verify`](#verify--ตรวจก่อนเชื่อ-รันก่อนทำอะไรทั้งสิ้น--charter-ข้อ-7) · ข้อไหนไม่ตรง **แก้เอกสารนี้ก่อน** แล้วค่อยทำงาน
2. อ่าน [`CLAUDE.md`](../../CLAUDE.md) → [`QUEUE-WEB.md`](QUEUE-WEB.md) → [`DECISIONS-BOARD.md`](DECISIONS-BOARD.md)
3. 🛑 **ตรวจคำสั่งยืนก่อนหยิบแถว** — `Q-WS-101` พักห้องนี้ไว้ถึง 16 ส.ค. (บล็อกบนสุดของไฟล์นี้)
   ⇒ **ยังพักอยู่** → อย่าหยิบแถวไหน · เดินได้เฉพาะของที่ชี้ได้ว่าขวางสายผู้เรียน A1 โดยตรง (วัดแล้วว่าไม่มี)
   ⇒ **พ้น 16 ส.ค. แล้ว หรือถูกปลด** → หยิบ **W-24 Gallery** เป็นแถวแรก อ่าน [`GALLERY-BRIEF.md`](GALLERY-BRIEF.md) ก่อน อย่าออกแบบใหม่จากศูนย์
4. **ห้ามจบเทิร์นด้วยคำว่า "พร้อมทำต่อ"** (`Q-WS-101` กติกาข้อ ②) — ปิดก้อนแล้วหยิบแถวถัดไปในเทิร์นเดียวกัน
   ติดจริง ⇒ เขียนว่าติดอะไร · ใครปลด · **วันหมดอายุของการรอ** แล้วไปทำแถวอื่น · 🔑 *"กำลังรอ" กับ "หยุดแล้ว" ต่างกันแค่มีวันหมดอายุเขียนไว้หรือเปล่า*

---

# สถานะ ณ 2026-08-15 03:5xZ

> แก้หัวข้อ 08:2xZ เดิมเขียนว่า "สถานะปิดห้อง เครื่องจะถูกปิด ~04:00Z"
> **เครื่องไม่ได้ถูกปิด** เจ้าของเตรียมปิดแล้วไม่ได้ปิด และ `WS` ไม่ได้แจ้งว่ามันไม่เกิด
> (ห้อง `ACAD` หยุดรอไป 273 นาทีโดยทำถูกทุกอย่าง) · กติกาใหม่ `Q-WS-165`
> **เลยเวลาที่คำสั่งระบุแล้ว ให้ถือว่าปลดเอง อย่ารอคนสั่ง**
>
> เนื้อข้างล่างยังจริงทั้งหมด เปลี่ยนแค่เหตุผลที่เขียนมัน จากปิดห้องเป็นบันทึกสถานะ

## วัดแล้ว ไม่ใช่เดา

```
working copy      สะอาด ทุกอย่าง push ขึ้น origin/main แล้ว
deploy ล่าสุด     31834852329 success · apex 200 · /tour/ 200
หน้าเว็บ          8 หน้า (เพิ่ม /tour/ วันนี้ อยู่ใน sitemap แล้ว)
ทีมบน /signature/ Nok · Key · Kanya · Tip · Sue Pranee Veronica
                  HCPC PH112704 · VTCT · ITEC · จำนวนปีของคนเก่า เหลือ 0 จุด
รูปในทัวร์         14 ใบ ทุกใบไม่เกิน 200 KB
team-board.jpg    ลบแล้ว ตอบ 404 บน production
```

## งานที่ทำวันนี้ ตาม feedback ครูนก

commit c09ff34, 02cb98d, 09dd02f, 873dd1b, 17e9aeb · deploy เขียวทุกครั้ง
มติที่บันทึกใหม่ D-W23 D-W24 D-W25 และ D-W22 ถูก amend

## ค้างและเป็นของ komphet ตัดสิน ไม่ใช่ของห้องนี้

```
1  ปิดแล้ว 2026-08-17  หน้า /tour/ อยู่ใน Navbar แล้ว ชื่อ Inside the Shop
                       WS เคาะแทนเจ้าของใน Q-SHOP-020  ยืนยันบน production
2  รูปกำพร้าใน public/ เหลือ 19 ใบ สิทธิ์เคลียร์แล้ว (D-W25) จะใช้หรือลบก็ได้
   หมายเหตุ Q-WS-209 จำกัดขอบเขตห้องนี้ไว้ที่งานที่รับใช้ Platform กับ A1
   แถวนี้ตอบไม่ได้ว่ารับใช้ A1 ตรงไหน จึงรออยู่ ไม่ใช่ค้างเพราะลืม
3  ปิดแล้ว 2026-08-17  เจ้าของเคาะให้คงสัญลักษณ์ทั้งสองตัวไว้ ไม่ต้องแตะเว็บ
   ใบมติ Q-DESK-010  เหตุผลคือลบออกแล้วความหมายไม่กลับด้าน จึงเป็นป้ายไม่ใช่คำ
   ตรงเกณฑ์ Q-WS-163 อยู่แล้ว  ดาวเป็นจุดคั่นรายการบริการ ลูกศรอยู่ท้ายปุ่มบัตรของขวัญ
```

## เลข 91 ที่เคยเขียนไว้ตรงนี้ เป็นเลขที่พอง อย่าเอาไปใช้ต่อ

```
ที่เคยรายงาน   91 จุด
วัดใหม่วันนี้   ทั้ง build 93 จุด   ไม่นับ payload ของเฟรมเวิร์ก 63 จุด
เลขที่ถูก      63 คือจำนวนที่คนเห็นจริง
```

**สาเหตุ** ด่าน `check-learner-artifacts-notation` อ่านไฟล์ HTML ทั้งไฟล์
และ Next.js ใส่ข้อความเดียวกันซ้ำลงในบล็อก `self.__next_f.push` ซึ่งเป็น payload ที่คนมองไม่เห็น
อ่านโค้ดด่านแล้วไม่มีบรรทัดไหนตัดบล็อกนั้นออก

**และสัดส่วนการซ้ำไม่ใช่ค่าคงที่ อย่าประมาณด้วยการคูณ**
```
out/index.html            51 เหลือ 26   ซ้ำ 49 เปอร์เซ็นต์
out/services/index.html   36 เหลือ 34   ซ้ำ  6 เปอร์เซ็นต์
```
🔑 ***มันขึ้นกับว่าเนื้อของหน้านั้นถูกใส่ลง payload มากแค่ไหน ไม่ใช่คุณสมบัติของเฟรมเวิร์ก***
📌 ถ้าเอา 49 เปอร์เซ็นต์ของหน้าแรกไปหารทั้งเว็บจะได้ 46 ต่ำกว่าของจริง 17 จุด **และดูน่าเชื่อเพราะมีที่มา**

**วิธีนับที่ถูก ตัดตั้งแต่ตำแหน่งแรกของ `self.__next_f.push` จนจบไฟล์ แล้วค่อยนับ**
ขาที่ต้องล้มเหลวคือสั่งตัดที่สตริงที่สะกดผิด ต้องได้ `-1`
⇒ ถ้าสตริงพิมพ์ผิด **โค้ดไม่ error มันคืนเลขทั้งไฟล์กลับมาอย่างสุภาพ**
รายละเอียดเต็มอยู่ในใบ `Q-SHOP-023`

## กับดักที่เจอวันนี้ อย่าเจอซ้ำ

```
เชื่อผลเครื่องมือโดยไม่ถามว่ามันตอบคำถามไหน เกิดสามครั้งในวันเดียว
  whisper คืนข้อความอ่านครบของ 12 วินาที จาก 72 วินาที ที่เหลือหายเงียบ
  perceptual hash บอกว่าไม่ซ้ำ เพราะมันเทียบพิกเซล ไม่ได้เทียบมุมกล้อง
  ข้อความจาก WS ที่ผมสรุปว่าพิสูจน์ช่องทางเก่า ทั้งที่มันมาทางช่องใหม่
ทั้งสามครั้งเครื่องมือตอบถูกในคำถามของมันเอง คนอ่านเป็นคนแปลผลผิด

โมเดล whisper large-v3 กินหน่วยความจำ 2.9 GB บนเครื่องที่ swap เต็ม
วัด sysctl vm.swapusage ก่อนเลือกโมเดล ไม่ใช่วัดแค่ load

ลูป until grep ที่เฝ้าไฟล์เอาต์พุตของ build ค้างได้ถ้า build ถูก pipe ผ่าน tail
เพราะคำที่รออยู่ถูกตัดทิ้ง วนเปล่า 1 ชั่วโมง 24 นาทีมาแล้วหนึ่งครั้ง

---

# WORKSPACE-RESUME 2026-08-16T01:2xZ

## วัดแล้ว ไม่ใช่เดา

```
working copy      สะอาด ตรง origin/main
deploy ล่าสุด     31874632170 success 15 ส.ค. 08:30Z  apex 200
พอร์ต 3300 3301   ว่างทั้งคู่
หน้าเว็บ          8 หน้า  app/page.tsx + 7 หน้าย่อย
positive control  ผ่าน  Q-WS-101 ยังโผล่ในผลสแกน
ตู้ SHOP          open 1 ใบ  Q-WS-146 เป็นคำตอบรอ ack
ใบถึงห้องนี้ทั้งหมด  32 ใบ open ส่วนใหญ่อยู่ตู้ ALL เป็นกติกากลาง ไม่ใช่งานเว็บ
MEMORY.md         ตรวจครบทุกข้อ ยังจริงกับโค้ดปัจจุบัน
                  cookie_domain none อยู่ที่ Analytics.tsx บรรทัด 74
                  2009 อยู่ 6 ไฟล์ ตรงกับรายการในกฎข้อ 4 ไม่ขาดไม่เกิน
                  images.unoptimized true  เบอร์ 020 ในรีโป 0 จุด
```

## ของใหม่ที่เอกสารเดิมไม่รู้

```
Q-WS-190  เจ้าของสั่งหยุดทุกห้องที่โควตา 99 เปอร์เซ็นต์  ออก 2026-08-15T23:49Z
Q-WS-191  ทะเบียนภาษา 2 ชั้น เข้า CLAUDE.md ส่วนตัวของเจ้าของแล้ว
          และปลดพัก MKT SIG LAW  ห้องนี้ไม่มี _PAUSED.md อยู่แล้ว
Q-WS-146  ตอบสี่ใบที่ห้องนี้ทวงมา 5 รอบ  ตอบช้าไป 4 วัน
เปิดขาย A1 16 ส.ค. 08:00Z อีกประมาณ 6 ชั่วโมงครึ่งนับจากที่เขียนนี้
```

## กับดักเรื่องเวลาที่เพิ่งเจอ อย่าเจอซ้ำ

```
เอกสารของ WS หลายฉบับปั๊มวันที่เกินจริงไป 1 วัน
  machines/komphet-mac/HANDOFF.md  เขียน  2026-08-16 ราว 23:5xZ
  Q-WS-188                        เขียน  2026-08-16 ราว 23:1xZ
  ของจริงคือ 2026-08-15 คืนนั้น  ยืนยันจาก author date ของ commit
    Q-WS-190 commit 2026-08-16T06:49:59+07:00 ซึ่งเท่ากับ 2026-08-15T23:49Z

สาเหตุ เอา**วันที่**จากนาฬิกาเครื่อง +07 มาต่อกับ**เวลา** UTC แล้วเติม Z
  +07 ข้ามวันเร็วกว่า UTC 7 ชั่วโมง ⇒ ทุกอย่างที่เขียนหลัง 17:00Z ได้วันที่ของพรุ่งนี้
  นี่คือสิ่งที่ Q-WS-181 สั่งห้ามไว้พอดี และมันเกิดในใบของคนที่ออกคำสั่งนั้นเอง

ทำไมมันอันตราย  วันที่ในอนาคตอ่านไม่ออกว่าผิด มันอ่านเหมือนของสดกว่าของจริง
  ห้องที่มาอ่านจะคิดว่าเอกสารใหม่กว่าผลวัดของตัวเอง แล้วเชื่อเอกสารแทนที่จะเชื่อของจริง
  ใช้ date -u ทุกครั้ง ห้ามประกอบวันกับเวลาจากคนละนาฬิกา
```

## ที่ห้องนี้ทำและไม่ทำในเทิร์นนี้ และทำไม

```
ทำ     WORKSPACE-RESUME ครบ 5 ขั้น  แก้ HANDOFF ส่วนที่พิสูจน์แล้วว่าเท็จ
       ออกใบ Q-SHOP-017 ถึง WS  ack Q-WS-146
ไม่ทำ  ไม่หยิบ W-24 Gallery ขึ้นมาเริ่ม
เหตุผล การพักตาม Q-WS-101 หมดอายุแล้วจริง แต่ Q-WS-190 สั่งห้ามเริ่มของใหม่
       และมันใหม่กว่า  การอ่านวัดและแก้เอกสารไม่ใช่การเริ่มของใหม่ จึงทำได้
       W-24 คือของใหม่ชัดเจน จึงรอเจ้าของสั่ง  ถามไปในใบแล้ว
```

---

# ปิดก้อน W-24 · 2026-08-16T01:4xZ

## W-24 ปิดแล้ว และมันเคยถูกรายงานผิดว่ายังไม่เริ่ม

```
ของจริง    หน้า /tour/ ขึ้น production ตั้งแต่ 15 ส.ค.  ไม่ใช่ app/gallery/ ตามชื่อในบรีฟ
คิวเขียน   "ยังไม่เริ่ม" อยู่เต็ม 1 วันหลังจากนั้น
ผลถ้าไม่แก้ ห้องถัดไปสร้างหน้า gallery ซ้ำ โดยเชื่อว่ากำลังเดินตามแผน
แก้แล้ว    QUEUE-WEB.md แถว W-24 ปิดด้วยชื่อไฟล์ที่ส่งจริง
```

## ที่ทำเพิ่มในเทิร์นนี้

```
ลิงก์เข้า /tour/ จากท้าย /services/   บรีฟ ข้อ 6 สั่งไว้ และเป็นทางเข้าเดียวที่ยังขาด
ผ่านด่าน  build + lint + check:assets เขียว
          visual gate ทำจริงสองขนาด desktop 1280 และ mobile 390
          production /services/ 200 และมีคำว่า Take the tour อยู่จริง
deploy    31920309201 success
```

## ทางเข้า /tour/ ตอนนี้มีสามทาง ขาดทางเดียว

```
การ์ดหน้าแรก ShopTour    มี
ท้ายหน้า /services/       มี เพิ่มวันนี้
sitemap                  มี
Navbar                   ไม่มี  ยังเป็นข้อที่รอ komphet เคาะ ไม่ใช่ของห้องนี้
```

## กับดักที่เจอในเทิร์นนี้ อย่าเจอซ้ำ

```
Browser pane ค้างทุกครั้งที่สั่ง scroll  ทั้ง computer scroll และ JS ตั้ง scrollTop
  อาการ  ภาพที่ถ่ายได้กลายเป็นสีเขียวเปล่า หรือ navbar ไปโผล่ล่างจอ
  แต่ DOM ตอบว่าองค์ประกอบอยู่ในจอ opacity 1  สองอย่างนี้ขัดกัน
  ทางที่ใช้ได้  ตั้ง viewport ให้สูงพอที่ของอยู่ในจอตั้งแต่แรก แล้ว reload
                1280x2700 สำหรับ desktop  390x3600 สำหรับ mobile  ไม่ต้อง scroll เลย
  บทเรียนเดิมซ้ำ  เครื่องมือตอบถูกในคำถามของมันเอง คนอ่านเป็นคนแปลผลผิด
                 ภาพเปล่าไม่ได้แปลว่าหน้าเว็บพัง มันแปลว่าตัวถ่ายภาพพัง

body เป็นตัว scroll ไม่ใช่ window  overflow ของ body คือ hidden auto
  window.scrollTo และ scrollY จึงคืน 0 เสมอ และอ่านเหมือนหน้าไม่ยอมเลื่อน

กับดัก next-env.d.ts tsconfig.json เกิดจริงทั้งสองไฟล์ในเทิร์นนี้
  คืนด้วย git checkout แล้ว และยืนยันพอร์ตปิดด้วย lsof
```

## แถวถัดไป

```
② เปลี่ยนภาพ AI ที่เหลือบนหน้าแรกและ /services/
   รูปจริง 14 ใบใน public/images/tour/ ผ่าน prep-shop-photos.py แล้ว หยิบไปใช้ได้เลย
   ยังไม่เริ่ม เพราะมันเปลี่ยนหน้าตาหน้าแรกที่มีงบ PMax ยิงอยู่
   ควรให้เจ้าของเลือกว่ารูปไหนไปแทนภาพไหน ไม่ใช่ห้องนี้เลือกเอง
```

---

# กฎการเลือก model ของ Executor 2026-08-16T02:3xZ

```
ใบ            Q-WS-204  machines/komphet-mac/queue/ALL/
ห้องนี้แตก     0 ตัว ทั้ง session นี้  จึงไม่มีอะไรต้องย้อนแก้
              และข้อ 5 ของใบก็สั่งไว้เองว่าไม่ต้องย้อน
บังคับเมื่อไร   ตั้งแต่การแตกครั้งถัดไป
ต้องเขียนที่ไหน ในใบคิวของงานนั้น หรือบรรทัดสถานะของห้อง
```

**ก่อนแตก Executor ตัวแรก ต้องเขียนหนึ่งบรรทัดว่าเลือกรุ่นอะไรเพราะอะไร**
ตัวอย่างงานของห้องนี้ที่เข้าเกณฑ์รุ่นใหญ่ คือถ้อยคำบนหน้าขาย ตารางทะเบียนคุกกี้
และการวินิจฉัยว่าสัญลักษณ์ตัวไหนทำหน้าที่เป็นคำ เพราะสามอย่างนี้ผิดแล้วกลับความหมาย

ส่วนงานอย่างกวาดสัญลักษณ์ตามรายการที่เขียนครบแล้ว หรือนับขนาดไฟล์รูป เข้าเกณฑ์รุ่นเล็ก

---

# ทำตามคำตัดสิน WS ในใบ Q-SHOP-020 · 2026-08-16T04:0xZ

## ทำอะไรไปแล้ว

```
Navbar        เพิ่ม Inside the Shop ชี้ /tour/  components/Navbar.tsx
              ตอนนี้มี 4 ลิงก์ services tour story contact
ภาพ AI        เปลี่ยนเฉพาะใบเสี่ยงใบเดียวตามที่ WS สั่ง
              promo-hair-spa-gemini-v2.jpg 756 KB  ออก  ลบไฟล์ทิ้งด้วย
              images/tour/salon-lavender-mural.jpg  เข้า  เป็นห้องทำผมจริง
              ถอด illustrative ออกจากการ์ดนั้น เพราะเป็นรูปจริงแล้ว
              ป้าย Illustrative image เหลือ 0 อัน  alt เปลี่ยนตามอัตโนมัติ
ภาพ AI เหลือ   3 ใบ 1620 KB  จากเดิม 4 ใบ 2376 KB
              เปลี่ยนเมื่อมีรูปจริง ไม่มีนาฬิกา ตามที่ WS สั่ง
แจ้ง MKT      กริ่งก่อนสลับ ตามที่ WS สั่ง เพราะหน้าแรกมีงบ PMax
```

## กับดักที่ทำให้ production เสียจริง 118 วินาที อย่าเจอซ้ำ

```
เกิดอะไร   git add ระบุ 3 path แต่หนึ่งใน 3 ถูก git rm ไปแล้ว
          git add ล้มทั้งคำสั่งเพราะ pathspec ไม่ตรง แล้ว ไม่ stage อะไรเลยสักไฟล์
          commit ถัดมาจึงมีแต่การลบไฟล์รูป ส่วนโค้ดที่ชี้ไปหารูปนั้นยังอยู่ในเครื่อง
ผลลัพธ์    production มีหน้าแรกที่อ้างรูปซึ่งถูกลบไปแล้ว รูปตอบ 404
ช่วงเวลา   deploy เสีย 03:55:39Z  deploy แก้ 03:57:31Z  รวม 118 วินาที
```

**และสิ่งที่อันตรายที่สุดคือทุกตัวชี้วัดขึ้นเขียว**
`git push` ผ่าน `echo pushed` พิมพ์ออกมา GitHub Actions ขึ้น success และ apex ตอบ 200
สิ่งเดียวที่จับได้คือเปิดดูว่า commit นั้นมีไฟล์อะไรบ้าง

**กติกาที่ออกมาจากเรื่องนี้ ใช้ทุกครั้งที่ commit**
```bash
git commit ... && git show --stat --oneline HEAD
git status --short          # ต้องว่าง ถ้าไม่ว่างแปลว่ามีของตกค้าง
```
อ่านรายชื่อไฟล์ใน commit เทียบกับที่ตั้งใจแก้ ก่อน push เสมอ
ห้ามใช้ผลของ git push หรือสีของ CI เป็นหลักฐานว่าของที่ตั้งใจแก้ขึ้นไปครบ

**เหตุที่ git add ล้มแล้วเงียบ** เพราะไฟล์ที่ถูก git rm ไปแล้ว ถูกลบออกจาก working tree
pathspec จึงหาไม่เจอ และ git add ถือว่าทั้งคำสั่งผิด ไม่ใช่ข้ามเฉพาะตัวที่หาไม่เจอ

---

# WORKSPACE-RESUME 2026-08-16T05:0xZ

## วัดแล้ว ไม่ใช่เดา

```
working copy      สะอาด ตรง origin/main
deploy 3 ตัวล่าสุด  success ทุกตัว 1 run ต่อ 1 push
apex              200
พอร์ต 3300 3301   ว่างทั้งคู่
ตู้ SHOP           ไม่มีใบ open เลย ทุกใบปิดครบ
positive control  ผ่าน Q-WS-101 ยังโผล่ในผลสแกน
MEMORY.md         ตรวจครบ ยังจริงทุกข้อ
                  cookie_domain none 1 จุด  dropWideCookies 2 จุด
                  2009 อยู่ 6 ไฟล์ ตรงกับกฎข้อ 4  images.unoptimized 1 จุด
                  เบอร์ 020 ในรีโป 0 จุด  มือถือ 07564292247 ยังใช้อยู่
                  ตาราง /cookies มีเลข 400 วัน 2 จุด
```

## ของใหม่ที่เปลี่ยนวิธีทำงานของห้องนี้

```
Q-WS-209  คำสั่งขอบเขตจากเจ้าของ งานคือ Taitam-D เท่านั้น เอา Platform กับ A1 ก่อน
          SHOP อยู่ในขอบเขต แต่เท่าที่งานรับใช้ Platform กับ A1
          ห้องนี้ไม่มี _PAUSED.md  ที่พักคือ 10 ห้องอื่น
Q-WS-200  ทับ Q-WS-198 ข้อ 2  อีโมจิแทนคำได้บางอย่าง ไม่ใช่ห้ามทั้งหมด
          เกณฑ์คือ อยู่ในช่อง แทนได้  อยู่ในตำแหน่งคำ แทนไม่ได้
Q-WS-201  Lead ต้องแจก Executor ทำงานขนาน ห้ามทำทีละอย่าง ห้ามหยุดรอ
```

## ผลของ Q-WS-209 ต่อคิวของห้องนี้ ตอบตรง ๆ

```
W-18 ไฟล์กำพร้า 19 ใบ        รับใช้ A1 หรือ Platform ตรงไหน  ตอบไม่ได้  รอ
ภาพ AI 3 ใบที่เหลือ          รับใช้ A1 ตรงไหน  ตอบไม่ได้ และรอรูปจริงจาก MKT อยู่แล้ว
ทางเข้ากรวยไป Academy       รับใช้ A1 โดยตรง  คือค่าเดียว SITE.academy ใน lib/site.ts
                            วัดแล้วว่าไม่ขวาง apex 200 และ academy 200
```

**แปลว่าห้องนี้ไม่มีแถวที่รับใช้ A1 ค้างอยู่เลย** สิ่งเดียวที่เชื่อมกับ A1 คือลิงก์ทางเข้า
ซึ่งวัดแล้วว่าทำงานอยู่ ถ้า ACAD หรือ MKT อยากได้ลิงก์ตรงไปคอร์ส A1 ให้ส่งใบมา
ห้องนี้แก้ค่าเดียวจบ ไม่ต้องให้ห้องอื่นแก้รีโปเอง

---

# ห้องนี้ได้สิทธิ์แตก Executor แล้ว 2026-08-17T00:0xZ

## คำอนุญาต

```
ผู้ให้      เจ้าของ komphet ในห้องนี้โดยตรง
ขอบเขต     ตามกติกาที่ WS วางไว้ Q-WS-147 ข้อ 2 และ Q-WS-204
เงื่อนไข    ถ้าเพดานถูกแก้ในใบใหม่ของ WS ให้ยึดใบล่าสุด โดยไม่ต้องกลับไปถามเจ้าของอีก
```

## เพดานที่ใช้จริง อ่านจากใบล่าสุดแล้ว ไม่ใช่จากใบที่คำสั่งอ้าง

```
Q-WS-201  จ่าหน้าถึง SHOP โดยตรง  ห้องนี้เป็น Lead ทั่วไป  เพดาน 2 ตัวขนาน
          งานที่รันเทสต์หรือ build นับเป็น 2 ตัวเสมอ เพราะมัน fork worker เอง
          หยุดเพิ่มทันทีเมื่อ swap ว่างต่ำกว่า 500 MB หรือหน่วยความจำว่างต่ำกว่า 20 เปอร์เซ็นต์
Q-WS-211  เพดาน 6 ตัว และข้อห้าม Lead ลงมือเอง จ่าหน้าถึง ACAD เท่านั้น
          ห้องนี้อยู่ในช่อง cc  จึงไม่ผูกพัน  อ่านไว้เป็นบริบท
Q-WS-147  ขอบเขต เฉพาะงานอ่าน วัด รายงานผลดิบ
          ห้ามให้ Executor ตัดสิน และห้ามให้แก้ไฟล์เนื้อหา  ผู้ตรวจไม่ใช่ผู้เขียน  D-AUTH2
Q-WS-204  ต้องประกาศ model ที่เลือกพร้อมเหตุผล ในใบหรือบรรทัดสถานะ ไม่ใช่ในหัว Lead
```

**ก่อนปล่อยต้องวัดเครื่องทุกครั้ง** `sysctl vm.swapusage` และ `memory_pressure | tail -2`

## ตาราง Executor ที่ปล่อยรอบนี้

```
วัดเครื่องก่อนปล่อย  2026-08-17T00:0xZ  swap ว่าง 945.50 MB จาก 6144 MB  หน่วยความจำว่าง 34 เปอร์เซ็นต์
                   ผ่านเกณฑ์ทั้งสองข้อ  945 มากกว่า 500  และ 34 มากกว่า 20

EX-1  ตรวจทางเข้ากรวยไป Academy ในซอร์ส   model haiku
      เหตุผล งานเชิงกลล้วน เกณฑ์เขียนครบในคำสั่ง อ่านและนับ ไม่มีการตัดสิน
EX-2  ตรวจทางเข้ากรวยเดียวกันบน production  model haiku
      เหตุผล เหมือน EX-1 เป็นการยิง curl แล้วรายงานตัวเลขดิบ ไม่มีการตัดสิน
ทั้งสองตัว  อ่านอย่างเดียว ห้ามแก้ไฟล์ ตามขอบเขต Q-WS-147 ข้อ 2
```

**ทำไมเลือกงานนี้เป็นงานแรก** `Q-WS-209` จำกัดขอบเขตห้องนี้ไว้ที่งานที่รับใช้ Platform กับ A1
พื้นผิวเดียวของเว็บร้านที่รับใช้ A1 คือทางเข้ากรวยไป Academy ซึ่งเป็นค่าเดียว `SITE.academy`
ไหลไป 5 คอมโพเนนต์ และ A1 กำลังเปิดขาย จึงเป็นงานที่อยู่ในขอบเขตจริง ไม่ใช่งานที่หามาทำ

---

# ผลรอบ Executor แรก 2026-08-16T17:2xZ

## ประโยคที่ห้องนี้เขียนผิดมาหลายวัน แก้แล้ว

```
เคยเขียนว่า   แก้ค่าเดียว SITE.academy ครอบคลุมทั้ง 9 จุด
ของจริง       มี 10 จุด  จุดที่ 10 คือ app/cookies/page.tsx ซึ่งฮาร์ดโค้ด URL เต็ม
              เป็นจุดเดียวที่การเปลี่ยนโดเมนจะทิ้งไว้ข้างหลัง และมันอยู่บนหน้านโยบาย
แก้แล้ว       commit 2de37f0 ประกอบจาก SITE.academy  ผลที่ render เหมือนเดิมทุกไบต์
```

**เหตุที่ประโยคนี้รอดมาได้นาน** เลข 9 ถูกต้องตรงกับที่ grep SITE.academy ได้ทุกครั้ง
คนตรวจซ้ำจึงเห็นว่าเอกสารตรงกับของจริง แล้วเลิกตรวจ โดยไม่มีเหตุให้ไป grep คำว่า academy
วิธีตรวจที่ถูกคือ `grep -rn 'academy\.taitam-d\.com' app components lib | grep -v 'SITE\.academy'`

## ที่วัดแล้วและยังดี

```
โดเมนเก่า 2 ตัวที่หลุดมือ   0 จุด ทั้งในซอร์สและในทั้ง 8 หน้าบน production
deep link ไปคอร์ส A1      0 จุด  เว็บร้านมีแต่ลิงก์หน้าแรก Academy
8 หน้าบน production       200 ทั้งหมด  ลิงก์ Academy ตามจนสุดทางได้ 200
ขาที่ต้องล้มเหลว           path ที่เติม -x ได้ 404  และ grep SITE.acadmy ได้ 0 ขณะของจริงได้ 9
```

## กติกาเวลาที่ต้องใช้ต่อ

`stat -f '%Sm'` พิมพ์เวลาเครื่อง +07 ทั้งที่รูปแบบลงท้ายด้วย Z ได้วันคนละวันกับ date -u
**เวลาทุกค่าที่จะเขียนลงไฟล์ ต้องมาจาก date -u เท่านั้น**
ห้ามใช้เวลาที่เครื่องมืออื่นพิมพ์ออกมา แม้รูปแบบจะลงท้ายด้วย Z แล้วก็ตาม

---

# WORKSPACE-RESUME 2026-08-18T16:1xZ

## วัดแล้ว ไม่ใช่เดา

```
working copy       สะอาด ตรง origin/main ที่ commit 9721641
deploy 3 ตัวล่าสุด   success ทุกตัว 1 run ต่อ 1 push ล่าสุด 31963289209
apex               200
พอร์ต 3300 3301     ว่างทั้งคู่
next-env.d.ts      ไม่หลุด  tsconfig.json ไม่หลุด
ตู้ SHOP            ไม่มีใบ open เลย ทุกใบปิดครบ 9 ใบ
positive control   ผ่าน Q-WS-101 ยังโผล่ในผลสแกน
MEMORY.md          ตรวจครบ ยังจริงทุกข้อ
                   cookie_domain none 1 จุด  dropWideCookies 2 จุด
                   2009 อยู่ 6 ไฟล์ ตรงกับกฎข้อ 4  images.unoptimized 1 จุด
                   เบอร์ 020 ในรีโป 0 จุด  มือถือ 07564292247 ยังใช้อยู่
                   ตาราง /cookies มีเลข 400 วัน 2 จุด
```

**ข้อเดียวที่ต่างจากเอกสาร** ใบ open ที่จ่าหน้าถึงห้องนี้เพิ่มจาก 0 เป็น **36 ใบ** ระหว่างที่ห้องปิดไป 2 วัน
ทั้ง 36 ใบอยู่ในตู้ `ALL` และเป็นใบกระจายกติกา **ไม่มีใบสั่งงานเฉพาะห้องนี้เลยสักใบ**

## ที่ทำในเทิร์นนี้ ปิดหนี้กติกา ไม่ใช่หยิบงานใหม่

```
Q-WS-219  เติมแถว SHOP ใน QUEUE-REGISTRY.md แล้ว  2 แถวค้าง จาก 3 แถว
Q-WS-228  ตรวจของตกค้างของระบอบที่ถูกยกเลิก  0 จุด  ห้องนี้ไม่เคยรับลงไฟล์
Q-WS-229  ack แล้ว  ไม่มี executor ของห้องนี้วิ่งอยู่ตอนรับใบ
Q-WS-231  ack แล้ว  ด่านปฏิเสธจริง ห้องนี้ไม่ปล่อย executor รอบนี้
Q-SHOP-024  รายงาน resume ถึง WS + ack ใบกระจายอีก 6 ใบ  205 222 223 224 225 226
commit ในรีโป workspace  def4aa6  push แล้ว
```

## ตัวนับที่ผมนับผิดก่อน 1 รอบ อ่านก่อนเขียนตัวนับตัวถัดไป

```
ตัวนับตัวแรก   grep '^| W-' ทั้งไฟล์        ได้ 22 แถวค้าง
ตัวนับที่ถูก    ผูกกับหัวข้อ ## คิวปัจจุบัน   ได้ 2 แถวค้าง จาก 3 แถว
```

สาเหตุ `QUEUE-WEB.md` มี **2 ตาราง** และตาราง `## เสร็จแล้ว` ใช้คอลัมน์ที่ 3 เป็น **วันที่** ไม่ใช่สถานะ
ตัวนับที่ไม่ผูกกับหัวข้อจึงกวาดตารางประวัติ 21 แถวมานับเป็นของค้าง

🔑 ***คำสั่งไม่ error เลยสักครั้ง และ 22 เป็นเลขที่ดูสมเหตุสมผลสำหรับห้องที่ทำงานมา 10 วัน***
ถ้าไม่บังเอิญพิมพ์รายชื่อแถวออกมาดู เลข 22 จะถูกลงทะเบียนไว้ให้ยามอ่าน และห้องนี้จะกลายเป็น
ห้องที่ดูมีงานค้างมากที่สุดในเครื่อง ทั้งที่ของจริงค้าง 2 แถว และทั้งสองแถวรออยู่โดยถูกกติกา

⇒ ตัวนับที่ลงทะเบียนไว้มี **ขาที่ต้องล้มเหลว** คือเปลี่ยนชื่อหัวข้อในคำสั่งแล้วต้องได้ `rows=0` ทดสอบแล้วผ่าน
📌 **บทเรียนที่ต่างจากที่ `MKT` และ `SUN` เขียนไว้** สองห้องนั้นเตือนว่าตารางที่คนเขียนมือเชื่อได้เท่าวินัยของห้อง
ข้อนี้เป็นอีกชั้นหนึ่ง คือ **ไฟล์เดียวมีได้หลายตารางที่คอลัมน์ตำแหน่งเดียวกันคนละความหมาย**

## ด่านโหลดปฏิเสธจริง และมันถูกด้วยเหตุผลคนละอันกับที่มันอ้าง

```
2026-08-18T16:16Z   load1 10.67   load5 8.46   load15 16.39   เครื่อง 10 คอร์
                    free+inactive 3.47 GB  page size 16384
                    swap ว่าง 541.81 MB จาก 15360 MB
ผลตัดสิน            ไม่ผ่านทั้งสองด่าน  Lead จึงลงมือเองทั้งเทิร์น
```

`Q-WS-229` ไม่ผ่านเพราะ `load1` ไม่น้อยกว่าจำนวนคอร์ · `Q-WS-231` ไม่ผ่านซ้ำเพราะ `load1` มากกว่า `load5` และมากกว่า 5

⚠️ **แต่ `load15` 16.39 สูงกว่า `load1`** ซึ่งแปลว่าเครื่องกำลัง **ลง** จากยอด ไม่ใช่กำลังไต่
ด่านแนวโน้มอ่านทิศจาก `load1` เทียบ `load5` อย่างเดียว จึงปฏิเสธทั้งขาไต่และขาลง
🔑 ***รอบนี้ผลออกมาถูก แต่ถูกด้วยเหตุผลที่ไม่ใช่เหตุผลที่กติกาอ้าง — ด่านแบบนั้นจะดูเหมือนด่านที่ผ่านการพิสูจน์แล้ว***
📌 แจ้ง `WS` ไว้ในใบ `Q-SHOP-024` ข้อ 4 แล้ว **และไม่เสนอให้แก้** เพราะใบเองประกาศว่าเกณฑ์ 5 เป็นค่าชั่วคราว
ที่ห้องนี้ทำได้คือ **จดผลวัดสามค่าทุกครั้งที่แตะด่าน** เพื่อสร้างฐานย้อนหลังให้ตัดสินจริงในอนาคต

## สองอย่างที่แก้ในรีโปนี้เพราะมันจะหลอกห้องถัดไป

```
QUEUE-WEB.md  หัวข้อยั้งคิวยังอ้าง Q-WS-101 ซึ่งหมดอายุ 16 ส.ค.
              แก้ให้ระบุว่าสิ่งที่ยั้งอยู่จริงคือขอบเขต Q-WS-209 และเก็บถ้อยคำเดิมไว้ใน details
HANDOFF.md    ขั้นที่ 5 ของหัวไฟล์ยังสั่งให้ใช้ mcp__ccd_session_mgmt__send_message
              ซึ่งถูกห้ามแล้ว  แก้เป็น ListAgents สด แล้ว SendMessage พร้อม ref
```

🔑 ***การพักกับการอยู่นอกขอบเขต ให้ผลหน้าตาเหมือนกันทุกประการ คือคิวที่ไม่มีใครหยิบ***
แต่การพักหมดอายุเองตามวันที่ ⇒ ห้องถัดไปที่เห็นวันที่ผ่านมาแล้วจะสรุปว่าเดินได้ทั้งคิว
ทั้งที่ของจริงยังไม่มีแถวไหนเข้าเกณฑ์ `Q-WS-209` เลยสักแถว

## แถวถัดไป ยังไม่มี และนี่คือคำประกาศ ไม่ใช่ช่องที่ยังไม่ได้กรอก

```
W-23  รับใช้ A1 ตรงไหน  ตอบไม่ได้  รอ
W-18  สิทธิ์ครบแล้วที่ D-W25 แต่รับใช้ A1 ตรงไหน  ตอบไม่ได้  รอ
ภาพ AI 3 ใบ  รอรูปจริงจาก MKT อยู่แล้ว  Q-SHOP-021
```

**ห้องนี้ไม่มีแถวที่รอห้องอื่นตอบ** จึงไม่มีนัดทวงตาม `Q-WS-205` — แจ้ง `WS` ไว้แล้วในใบ `Q-SHOP-024`
ถ้า `ACAD` หรือ `MKT` อยากได้ deep link ตรงไปคอร์ส `A1` จากเว็บร้าน **ส่งใบมา ห้องนี้แก้ค่าเดียวจบ**

---

# เมนูบริการเต็มขึ้นเว็บแล้ว และคำเคาะ Legal เรื่องกลุ่มการแพทย์ · 2026-08-18T19:3xZ

## ที่ทำและ live แล้ว

```
เจ้าของสั่ง       ยกเมนู Treatwell ทั้งหมดขึ้นเว็บเป็นข้อมูลกลาง ราคา+เวลา+ปุ่ม WhatsApp ต่อรายการ
ข้อมูลกลาง        lib/catalog.ts  แหล่งจริงจาก Treatwell Connect venue 287555 (2026-08-18)
คอมโพเนนต์        components/ServiceMenu.tsx  wire เข้า app/services/page.tsx แทน ServicesGrid
live              commit 50f4c1d  deploy 32173957709 success  วัด https://taitam-d.com/services/ 200
                  9 หมวดบริการ + 1 หมวดสินค้า  กดที่ราคา = WhatsApp เติมข้อความ บริการ+เวลา+ราคา
ตัดออกตั้งใจ      รายการภายใน NOT ON TREATWELL (Bupa, body waxing ภายใน, henna ฟรี, eyelash perming ภายใน)
รูป Story         285b779 เอารูปที่มีธนบัตร $100 บนพื้นออก แทนด้วย tour/lounge-beach-mural-wide.jpg (เจ้าของสั่งด่วน)
```

## กลุ่มการแพทย์ Legal เคาะแล้ว Q-LAW-046 (2026-08-18T19:12Z) ยังไม่ขึ้นเว็บ

```
กลุ่ม 1  Anti-Wrinkle Injections (botulinum toxin)   ห้ามขึ้นเว็บสาธารณะเด็ดขาด กฎหมายชัด HMR 2012 reg 280
                                                    ห้ามราคา ห้ามปุ่มจอง ห้ามคำว่า Botox ทุกที่บนเว็บ
กลุ่ม 2  ฟิลเลอร์ · วิตามินฉีด IV/IM · PRP            ขึ้นได้แบบปรึกษาก่อนเท่านั้น ไม่มีราคา ไม่มีปุ่มจองทันที
กลุ่ม 3  HIFU · เมโสไม่เข็ม · Microblading/SPMU · ลบรอยสัก   ขึ้นได้พร้อมราคาเวลา + คำบังคับ 18+ ผลต่างบุคคล ปรึกษาก่อน
                                                    Microblading และลบรอยสัก จองปรึกษา+ทดสอบผิว ไม่ใช่จองทำทันที
กลุ่ม 4  ไม่ใช่การแพทย์                              ขึ้นเต็ม  ทำแล้ว
```

**ด่านก่อนขึ้นกลุ่ม 2 และ 3 คือข้อ E ต้องเจ้าของยืนยัน 4 อย่าง** ทะเบียนกับหน่วยงานกำกับ · ผู้สั่งยาที่มีคุณสมบัติ ·
ใบอนุญาต special treatment ของท้องถิ่นสำหรับการสักผิวและลบรอยสัก · ประกันที่ครอบหัตถการ
LAW ยืนยันแทนไม่ได้ ห้องนี้ก็ยืนยันแทนไม่ได้ **รอเจ้าของ** ถามไปในห้องแล้ว 2026-08-18T19:2xZ

**คำแนะนำที่ห้องนี้ให้เจ้าของ** ขึ้นเป็น 2 จังหวะ กลุ่ม 3 ก่อนเมื่อยืนยันใบอนุญาต+ประกัน (เตรียมโค้ดรอแล้ว) ·
กลุ่ม 2 พักให้นักกฎหมายจริงดูเส้นแบ่ง · กลุ่ม 1 ไม่ขึ้นเลย

**สถานะโค้ด** กลุ่ม 3 staged ใน `lib/catalog.ts` เป็น `CATALOG_MEDICAL_G3` + `MEDICAL_G3_NOTICE`
**ยังไม่ถูก import ในเพจใด** จึงไม่ render · เมื่อเจ้าของยืนยัน ให้ wire เข้า ServiceMenu พร้อมกล่องคำบังคับ
และปุ่มต้องเป็น "book a consultation" ไม่ใช่ "book now" · ห้ามเติมกลุ่ม 1 ลง catalog ไม่ว่ากรณีใด

## รูป AI ต่อบริการ (Gemini) ยังไม่ได้ทำ

เจ้าของเคาะให้ใช้ Gemini ทำรูป (กลับทิศจาก D-W22 ที่เลิกรูป AI เจ้าของรับความเสี่ยงเอง)
ยังไม่เริ่ม เพราะเมนูปัจจุบันเป็นรายการราคาไม่มีรูปต่อรายการ และเครื่องพัก Q-WS-241 · ต้องถามเจ้าของว่าจะเอารูปต่อบริการหรือต่อหมวด

## ใบคิวที่เกี่ยว

```
Q-SHOP-027 → Q-LAW-046  โฆษณาเวชความงาม  ack แล้ว 178f0e4  รอเจ้าของข้อ E
Q-SHOP-026 → Q-LAW-045  ข้อมูลลูกค้า  ack แล้ว  relay ให้ห้อง OFFICE ทาง HANDOFF ของ repo office b6d13e4
Q-WS-242                repo office สร้างแล้ว  ack แล้ว  งานย้ายไปห้อง OFFICE
```

---

# สถานะคิว SHOP 2026-08-18T21:08Z — แยกสามกองตาม Q-WS-169

ทำไปในรอบนี้: เมนูเต็ม+ราคา+WhatsApp ต่อรายการ (50f4c1d) · Advanced Aesthetics ตาม LAW (de346a7) ·
ข้อความมาตรฐานร้าน Q-OFFICE-002 (e3fff95) · คำอธิบายบริการ 19 รายการ (97b9acd) — ทุกอัน deploy เขียว verify production แล้ว

3 ใบที่ stop-hook นับว่า open ถึง SHOP — แยกกอง:
```
ทำได้เลย     0 ใบ  (do-now ของรอบนี้คือคำอธิบายบริการ ทำเสร็จแล้ว 97b9acd)
รอห้องอื่น   2 ใบ
  Q-LAW-045  รอเจ้าของ + นักกฎหมายจริง เรื่องฐานทางกฎหมายของการดูดข้อมูลลูกค้า (Art 9)
             เป็น hard block ถาวร SHOP ไม่ปิดเอง · ถ้าไม่มีคำตอบ = ไม่ดูด Clients ต่อ ถูกแล้ว
  Q-WS-242   งานฝั่ง SHOP ทำครบ+ack แล้ว (repo/ย้ายแคตตาล็อก/เปลี่ยนชื่อห้อง) · broadcast ให้ ALL ปิด ไม่ใช่ SHOP
เสร็จรอปิด   1 ใบ
  Q-LAW-046  กลุ่ม 3 ขึ้นราคา · กลุ่ม 2 ปรึกษา · กลุ่ม 1 ไม่ขึ้น · verify แล้ว · เหลือข้อ E constraint ถาวร
```
รอเจ้าของเคาะ 2 เรื่อง (ไม่ใช่ใบคิว): รูป Gemini ต่อหมวด (ต้องขอ key จาก WS) · ช่องติ๊ก accept policy ตอนจอง

---

# สถานะคิว SHOP 2026-08-18T21:32Z — สามกอง (หลังปิดงานรูป+คำอธิบาย)

ทำรอบนี้: รูปหมวด 11 รูป Gemini (645470e,b6a5b0b) · คำอธิบายบริการ 19 รายการ (97b9acd) — deploy เขียว verify production ครบ
```
ทำได้เลย     0 ใบ  (do-now รอบนี้คือรูป+คำอธิบาย ทำเสร็จแล้ว)
รอห้องอื่น   2 ใบ
  Q-LAW-045  standing hard-block  รอเจ้าของ+นักกฎหมายจริง เรื่องฐานกฎหมายดูดข้อมูลลูกค้า
             SHOP ทำตามอยู่แล้วคือไม่ดูด ไม่คอมมิต PII · ไม่มีคำตอบ = ไม่ดูด ถูกแล้ว · do-now 0
  Q-WS-242   registry broadcast ถึง ALL  งานฝั่ง SHOP เสร็จ+ack แล้ว  WS/ALL เป็นคนปิด
เสร็จปิดแล้ว  Q-LAW-046  ปิดฝั่ง SHOP รอบนี้ (cc9db52) ขึ้นเว็บครบ verify แล้ว
```
รอเจ้าของเคาะ (ไม่ใช่ใบคิว ไม่บล็อกด่าน): ช่องติ๊ก accept policy ตอนจอง (OFFICE เสนอ)

---

# Service menu + images shipped · 2026-08-18T22:0xZ

## ทำอะไรไปแล้ว (live บน production ทั้งหมด verify แล้ว)

```
lib/catalog.ts           แหล่งกลางบริการ+สินค้า ดูดจาก Treatwell Connect (venue 287555)
components/ServiceMenu.tsx  render เมนูเต็ม ราคา เวลา ปุ่ม WhatsApp เติมข้อความต่อ variant
app/services/page.tsx     ใช้ ServiceMenu แทน ServicesGrid เดิม
public/images/services/   รูปหมวด 11 ใบ Gemini gemini-3-pro-image โทนแบรนด์ 72-112KB lazy
deploy ล่าสุดเขียว        b6a5b0b (asset fix) · commit เนื้อ 645470e 966c8f7
```

## กติกา Legal ที่ฝังในโค้ด (Q-LAW-046) — ห้ามย้อน

```
กลุ่ม 1 Anti-Wrinkle/botulinum   ไม่ขึ้นเว็บเลย ห้ามคำว่า Botox  (CATALOG ไม่มี)
กลุ่ม 2 filler PRP วิตามินฉีด      ขึ้นแค่ชื่อ ไม่มีราคา ปุ่มเดียว = ปรึกษา  (CONSULTATION_ONLY)
กลุ่ม 3 HIFU เมโส microblading ลบรอยสัก  ราคาได้ ใต้ MEDICAL_NOTICE ปุ่ม = consultation  (CATALOG_MEDICAL)
เจ้าของเคาะข้อ E 2026-08-18 ใบอนุญาตมีจริงแสดงที่ร้าน ไม่ต้องขึ้นเว็บ เว็บใช้คำ verify in store
```

## สถานะใบที่ค้าง (ฝั่ง SHOP ทำครบ)

```
Q-OFFICE-002  ปิด เจ้าของเลือกไม่ทำช่องติ๊ก standardsNotice คงไว้ 4 จุด live แล้ว
Q-LAW-046     done กติกากลุ่มการแพทย์ฝังในโค้ดแล้ว
Q-LAW-045     รอเจ้าของ+นักกฎหมายจริงเคาะฐานกฎหมายก่อนดูด Clients/Reports — ไม่ใช่งานเว็บ
              SHOP compliant แล้ว ไม่มี PII ใน git ทั้ง 2 repo
Q-WS-242      ack_SHOP ลงแล้ว WS ถือการปิด (ทะเบียน repo + ชื่อห้อง)
```

## งานบริหารร้านเต็มรูป (Clients/Reports/Pay)

อยู่ repo private solution-taitamd-shop-office ห้อง OFFICE ถือ · บล็อกด้วย Q-LAW-045
ห้ามดูด PII เข้า git · ต้องออกแบบฐานข้อมูลจริงก่อน (งานห้อง OFFICE ไม่ใช่ SHOP)

---

# WORKSPACE-RESUME 2026-08-21T02:5xZ — สามกอง 0 / 1 / 0

## วัดแล้ว ไม่ใช่เดา

```
working copy       สะอาด ตรง origin/main ที่ ddafbd3
deploy 3 ตัวล่าสุด  success ทุกตัว 1 run ต่อ 1 push  ล่าสุด 32191133229 (2026-08-18T22:06Z)
apex               200
พอร์ต 3300 3301     ว่างทั้งคู่ · next-env.d.ts และ tsconfig.json ไม่หลุด
positive control   ผ่าน Q-WS-101 ยังโผล่ในผลสแกน fetch-based
MEMORY.md          ตรวจครบ ยังจริงทุกข้อ  cookie_domain none 1 จุด · dropWideCookies 2 จุด
                   2009 อยู่ 6 ไฟล์ตรงกฎข้อ 4 · images.unoptimized 1 จุด
                   เบอร์ 020 ในรีโป 0 จุด · มือถือ 07564292247 ยังอยู่ · เลข 400 วันใน /cookies 2 จุด
```

## สามกองตาม Q-WS-169 ณ 2026-08-21T02:5xZ

```
ทำได้เลย      0 ใบ
รอห้องอื่น    1 ใบ  Q-LAW-045 รอเจ้าของ + นักกฎหมายจริง เคาะฐานกฎหมายก่อนดูดข้อมูลลูกค้า
                    ทวงแล้วด้วย Q-SHOP-028 (ddf43dc4) ขอให้ WS ใส่ digest ถึงเจ้าของ
                    ถ้าไม่ได้คำตอบ = ไม่ดูดต่อ ซึ่งถูกแล้ว และไม่หยุดงานกองอื่นของ SHOP
ยังไม่ได้เปิด  0 ใบ  เปิดอ่านครบทุกใบที่จ่าหน้าถึง SHOP ในเทิร์นนี้
ใบกระจายกติกา 37 ใบใน ALL  ไม่นับรวม เพราะไม่มีห้องเดียวที่ปิดมันได้
```

## 2 ใบที่เข้ามาระหว่างห้องปิดไป 2 วัน ตอบแล้วที่ daf0b186

```
Q-WS-239  มติเจ้าของ เดินคู่ขนาน 3 สาย  SHOP อยู่ใน to แต่ไม่มีบรรทัดงานของ SHOP ในสาย 1 ถึง 4
          หน้าแรกในสาย 3 คือหน้าแรกแพลตฟอร์มที่ ACAD ถือ ไม่ใช่ taitam-d.com
          วัดสด ทางเข้า Academy จากเว็บร้านมีค่าเดียวคือ SITE.academy ที่ lib/site.ts:38 ไม่มี deep link ไปคอร์ส a1
          WS ตอบกลับแล้ว ถ้าสาย 3 อยากได้ deep link จะออกใบมาทีหลัง ตอนนี้ยังไม่ต้องทำอะไร
Q-WS-253  หน้าต่างประหยัดโทเค็น  WS ปิดใบแล้วที่ 1322c932 ทุกห้องเดินเต็มรูป
```

## คำตอบ WS 2 ข้อที่ห้องนี้ถามไป อย่าถามซ้ำ

```
cron ปลุกตัวเอง   ไม่ต้องตั้ง  WS มี watchdog ชั้น 1 แล้ว (นาที 13 และ 48) และปลุกห้องหลับได้ด้วยท่อ A
                  ⇒ CronList ของห้องนี้ว่างเป็นสถานะที่ถูก ไม่ใช่ของตกหล่น
รูปต่อรายการบริการ  พัก ยังไม่อยู่ในโฟกัส Q-WS-239 และต้องขอ Gemini key ตาม Q-WS-158 ซึ่งเป็นของเจ้าของ
                  WS จดเข้ารายการเจ้าของแล้ว
```

## ข้อห้ามเด็ดขาดของ Q-LAW-045 วัดสดแล้วยังถูกปฏิบัติจริงทั้ง 2 รีโป

```
repo เว็บ      ไฟล์ชื่อ client/customer/booking/report/payment/export  0 ไฟล์
              เบอร์รูป 07xxxxxxxxx ที่ไม่ใช่เบอร์ร้าน  0 เบอร์
repo office   tracked 24 ไฟล์ ที่ ae07e17 · อีเมลผู้บริโภค 0 จุด · เบอร์ลูกค้า 0 จุด
              booking-history-2018-2026.md เก็บเฉพาะตัวเลขรวมรายปี ไฟล์ CSV ที่มี customer id อยู่นอก git บน Drive
```

📌 **OFFICE ทำถูกและควรลอกวิธี** คือเอา **ตัวเลขรวม** เข้า git และกัน **แถวดิบ** ไว้นอก git แล้วเขียนกำกับในไฟล์เองว่าทำไม
🔑 *ข้อห้ามที่เขียนว่าห้ามคอมมิตข้อมูลลูกค้า ถูกอ่านได้ว่าห้ามแตะข้อมูลลูกค้าเลย ซึ่งจะทำให้ไม่มีใครวิเคราะห์อะไรได้*
ของจริงที่ทำได้คือ **สรุปแล้วคอมมิตสรุป** — ผลวัดข้างบนคือหลักฐานว่าเส้นนี้เดินได้จริง ไม่ใช่ทฤษฎี

---

# WORKSPACE-RESUME 2026-08-22T18:10Z — สามกอง 0 / 1 / 0 · ด่านตรวจของห้องนี้เองมี 2 จุดตาบอด แก้แล้ว

## วัดแล้ว ไม่ใช่เดา

```
working copy       สะอาด ตรง origin/main ที่ 6a501c7
apex               200
พอร์ต 3300 3301     ว่างทั้งคู่ · next-env.d.ts และ tsconfig.json ไม่หลุด
deploy             วัดด้วยการเทียบ production กับ HEAD ไม่ใช่ gh (เหตุผลข้างล่าง)
                   /images/services/massage.jpg โผล่ในหน้า /services/ ของจริง 1 ครั้ง · ตัวไฟล์ 200
กฎ 4 ข้อของ CLAUDE.md  cookie_domain none 1 จุด · dropWideCookies 2 จุด · 2009 อยู่ 6 ไฟล์ครบตามรายการ
                   images.unoptimized 1 จุด · เบอร์ 020 ในรีโป 0 จุด · 07564292247 ยังอยู่ที่ lib/site.ts
                   เลข 400 ในหน้า /cookies 2 จุด
```

## จุดตาบอด 2 จุดในบล็อก Verify ของห้องนี้เอง เจอเพราะรันจริง ไม่ใช่เพราะอ่าน

```
1  gh ไม่มีบนเครื่อง       which gh = not found · รีโปเป็น private ⇒ GitHub API แบบไม่ล็อกอินคืน 404
                        บรรทัด gh run list ในบล็อกจึงพ่น command not found แล้วบล็อกเดินต่อเหมือนไม่มีอะไร
                        ⇒ แทนด้วยตัววัดที่วัดของจริง เทียบ path รูปใน lib/catalog.ts กับหน้า production
2  ใบ to: [ALL] มองไม่เห็น  สแกนเดิม grep แค่ \bSHOP\b ⇒ ใบที่จ่าหน้า to: [ALL] หายทั้งหมด
                        ของจริงที่หายคือ Q-WS-256 priority high ออก 2026-08-21 สั่งทุกห้องตรวจ 1 ครั้ง
                        ห้องนี้จึงไม่เห็นมันตั้งแต่ 21 ส.ค. ทั้งที่บล็อกรายงานว่า ไม่มีใบใหม่
```

🔑 *ด่านที่เขียนถูกตอนเขียน จะตาบอดเมื่อ **รูปแบบการจ่าหน้าเปลี่ยน** โดยที่ผลลัพธ์ยังหน้าตาเหมือนเดิมทุกประการ*
⇒ นี่คือเหตุผลที่ positive control ต้องมี **1 ตัวต่อ 1 ขาของคำสั่ง** ไม่ใช่ 1 ตัวต่อ 1 บล็อก

## positive control ตัวเดิมล้มจริง และมันล้มถูก

```
Q-WS-101  ปิดแล้ว WS กวาดใบหมดอายุ 2026-08-21T11:0xZ แทนด้วย Q-WS-238 ปิด a1 + Q-WS-239 เดินคู่ขนาน
          บล็อกเดิมเขียนไว้แล้วว่าเคสนี้ต้องเอาตาดู ไม่ใช่อ่านว่าตู้ว่าง ⇒ กลไกทำงานตามที่ออกแบบ
control ใหม่  Q-LAW-045 คุมขา SHOP · Q-WS-200 คุมขา ALL · วัดแล้วผ่านทั้งคู่หลังแก้บล็อก
```

## กับดักคำสั่งที่วัดเจอระหว่างแก้ อย่าเจอซ้ำ

```
git grep -E ไม่รองรับ \b   ยุบสองบรรทัดเป็น git grep -lE '^to:.*\b(SHOP|ALL)\b' แล้วคืน 0 ไฟล์ เงียบ ไม่มี error
                        ท่า -l ธรรมดาบน pattern เดียวกันคืน 86 ไฟล์ ⇒ ต้องใช้ 2 grep แล้ว sort -u
```

🔑 *คำสั่งที่คืน 0 โดยไม่มี error อ่านไม่ออกว่าแปลว่าไม่มีของ หรือแปลว่าคำสั่งตาบอด — สองอย่างนี้หน้าตาเหมือนกัน*

## สามกองตาม Q-WS-169 ณ 2026-08-22T18:10Z

```
ทำได้เลย      0 ใบ  Q-WS-256 ที่โผล่มาใหม่ ทำจบในเทิร์นนี้แล้ว
รอห้องอื่น    1 ใบ  Q-LAW-045 รอเจ้าของ + นักกฎหมายจริง เคาะฐานกฎหมายก่อนดูดข้อมูลลูกค้า
                    สถานะเท่าเดิมกับ 21 ส.ค. ทวงไปแล้วด้วย Q-SHOP-028 ยังไม่มีคำตอบ
ยังไม่ได้เปิด  0 ใบ
```

## Q-WS-256 ตอบแล้วที่ da930167 ในรีโป workspace

```
คำสั่งที่ใช้วัด  git log --all --since=2026-08-15 --until=2026-08-18 -S'ไม่' --oneline -- '*.py' '*.sh' '*.mjs' '*.js' '*.ts' '*.tsx'
ผล            0 commit ⇒ ห้องนี้ไม่เคยรัน sweep อักขระแบบกลไกในรีโปนี้ ไม่มีอะไรต้องซ่อม
กติกาที่รับมา   sweep กลไกห้ามแตะไฟล์โค้ดและสคริปต์ · โค้ดที่ต้องอ้างอักขระห้ามให้ใช้ chr() หรือ unicode escape
              มีผลกับสคริปต์ 3 ตัวของรีโปนี้ check-orphan-assets.mjs · prep-shop-photos.py · transcribe-voice-notes.py
```

## ใบใหม่ 2 ใบที่ขา ALL เห็นแต่ไม่ใช่ของเรา บันทึกไว้กันตรวจซ้ำ

```
Q-KWS-004  กติกายืนของห้องบนเครื่อง komphet-air
Q-TWS-002  ถ่ายทอดมติจาก C2Tech ให้ห้องบนเครื่อง tongo
เหตุที่ไม่ใช่ของเรา  ใบ broadcast ในตู้ ALL ของเครื่องหนึ่ง ผูกพันเฉพาะห้องของเครื่องนั้น WS เคาะใน Q-TWS-001 ข้อ 4
                  ⇒ บล็อก Verify จึงสแกนเฉพาะ machines/komphet-mac/ ตามเดิม
```

## 🔴 แก้คำที่ผมเขียนผิดในหัวข้อนี้เอง — ความจำไม่ได้หาย มันอยู่ในรีโปมาตลอด

```
ที่เขียนไว้ตอนแรก  ที่เก็บความจำว่างเปล่า ของเดิมหายไปพร้อมการเปลี่ยนบัญชี
ที่จริง            ไดเรกทอรี memory ว่างจริง แต่นั่นคือ สภาพปกติของเครื่องใหม่
                  ต้นทางชื่อ docs/MEMORY-shop.md 77 บรรทัด คัดเข้ารีโปตั้งแต่ 2026-08-11
                  ตาม gumon-workspace/docs/machine-handover.md ถัง A เพราะ memory ไม่ไปกับเครื่อง
ทำไมถึงพลาด        ค้นด้วย find -iname 'MEMORY.md' ซึ่งไม่แมตช์ชื่อ MEMORY-shop.md
```

🔑 *ค้นด้วยชื่อไฟล์ที่เอกสารบอก จะมองไม่เห็นไฟล์ที่ถูกตั้งชื่อใหม่ตอนคัดลอก* ⇒ ใช้ `-iname '*MEMORY*'`
เขียน memory ใหม่ครบ 6 ใบจากไฟล์นั้นแล้ว โดยตัดข้อ 5-10 ที่ซ้ำกับ `CLAUDE.md` ออก

## 🔴 และรหัสห้องบนเครื่องนี้ไม่ใช่ `SHOP` — `KWS` แจ้งเข้ามาและวัดยืนยันแล้ว

```
วัด        hw.model = Mac16,12 · hostname MacBook-Air-khxng-Komphet ⇒ เครื่องนี้คือ komphet-air
รหัสห้อง    บนเครื่องนี้คือ KWEB · ตู้ machines/komphet-air/queue/KWEB/
           SHOP เป็นรหัสของห้องเดียวกันบน komphet-mac ที่ยังเดินอยู่ รหัสห้ามซ้ำข้ามเครื่อง
ผลย้อนหลัง  ack_SHOP ที่ da930167 ลงตู้ komphet-mac ไม่ผิด เพราะใบ Q-WS-256 จ่าหน้าถึง SHOP จริง
           แต่ใบที่ ออกจากเครื่องนี้ ต่อจากนี้ต้องเซ็น from: KWEB และเดินเลขเขต KWEB
```

⚠️ **`CLAUDE.md` และ `docs/MEMORY-shop.md` เขียนว่าห้องนี้คือ `SHOP`** ซึ่งจริงเฉพาะบน `komphet-mac`
🔑 *เอกสารประจำรีโปพูดถึงห้องเป็นชื่อเดียว แต่รีโปเดินได้พร้อมกันหลายเครื่อง ⇒ ชื่อห้องเป็นของ **เครื่อง+รีโป** ไม่ใช่ของรีโป*

## `gh` ลงแล้ว v2.98.0 · ยังไม่ล็อกอิน

```
ที่มา     github.com/cli/cli release v2.98.0 zip macOS arm64 13,938,611 ไบต์
         sha256 8cfb027cc5310675f2b830eac8f9865c1155a45ffcf9757f699fdd5a22046ca4 ตรงกับ checksums.txt ที่ประกาศ
ที่อยู่    ~/.local/bin/gh (เครื่องนี้ไม่มี Homebrew ⇒ ลงจาก release ตรง ๆ ไม่ต้อง sudo)
         ~/.local/bin อยู่ใน PATH ของ zsh อยู่แล้ว (.zshrc และ .zprofile บรรทัด 2)
สถานะ     gh auth status = not logged into any GitHub hosts
         การล็อกอินเป็นของเจ้าของ agent ห้ามกรอก credential หรือ token เอง
```

---

# ตอบ KMKT เรื่อง tracking · 2026-08-22T18:39Z — วัดจาก production ทุกข้อ

เจ้าของสั่ง `KMKT` (Marketing @komphet-air) ให้ลดงบ Google Ads หลังจ่ายไป 100 GBP และมีอีกร้อยกว่า
เขาถามสถานะ tracking 5 ข้อ ผลวัดที่ตอบไป เก็บไว้ที่นี่เพราะเป็นของที่ห้องนี้ถือ

```
GA4              ติดจริง ฝังตรง ไม่ผ่าน GTM · G-R8SGQ58R5E ที่ components/Analytics.tsx:12
                 วัดจากบันเดิลจริง /_next/static/chunks/app/layout-229ddf171071fc70.js ไม่ใช่จาก HTML
                 เพราะเป็น client component ที่ inject script ตอน runtime
Google Ads tag   ไม่มี · grep -rn 'AW-\|send_to' app components lib = 0 บรรทัด
GA4 event        whatsapp_click และ phone_click ยิงจริงจาก Analytics.tsx:106-120
consent          Consent Mode v2 advanced · tag โหลดทุก visit · storage denied จนกด Accept
                 ระหว่าง denied ยิงเป็น cookieless ping ⇒ ไม่บล็อก แต่ ads_data_redaction=true ตัด click id
โดเมน            apex 200 redirects=0 · www 200 redirects=1 ไป apex ⇒ ไม่มี redirect chain
```

## 🔴 ของที่เจอระหว่างวัด และยังไม่ได้แก้ — ตัวเลข whatsapp_click พองจากปุ่มแชร์

ตัวดักคลิกที่ `Analytics.tsx:113` จับทุก `a[href]` ที่มีคำว่า `wa.me`
แต่บนเว็บมี `wa.me` **3 รูป** และรูปที่ 3 ไม่ใช่การจอง

```
wa.me/447882359499        ปุ่มจอง พร้อมข้อความสำเร็จรูป  = conversion จริง
wa.me/qr/YIN5KFQPKWI2F1   ลิงก์ QR ใน lib/site.ts:35       = conversion จริง
wa.me/?text=              ปุ่มแชร์ใน SocialShareBar.tsx:81  = การแชร์ ไม่ใช่การจอง
```

🔑 *ตัวดักที่แมตช์ด้วยโดเมนของบริการ จะจับทุกอย่างที่บริการนั้นทำได้ ไม่ใช่เฉพาะสิ่งที่เราตั้งใจนับ*
⇒ ถ้า `MKT` เอา `whatsapp_click` ไปตั้งเป็น conversion ใน Ads ตัวเลขจะพองด้วยจำนวนการกดแชร์
**ยังไม่แก้เพราะการอ่านผล analytics เป็นเขตของ Marketing** — แจ้งไปแล้ว รอเขาบอกว่าจะแยก event หรือกรองที่คอนโซล

## ผลวัด network ของ 3 ปุ่ม บน production ทั้ง 2 สถานะ consent

```
ก่อนกด Accept   page_view · phone_click · whatsapp_click · whatsapp_click   gcs=G100  npa=1  cookie 0 ตัว
หลังกด Accept   user_engagement · phone_click · whatsapp_click · whatsapp_click  gcs=G111  npa=0
               cookie ที่โผล่  _ga  _ga_R8SGQ58R5E  _gcl_au
ปลายทาง        google-analytics.com/g/collect ครบทุกครั้ง ทั้งก่อนและหลัง consent
```

🔴 **ตัวควบคุมเชิงลบได้ผลบวก** — ปุ่มแชร์ยิง `whatsapp_click` **ชื่อเดียวกับปุ่มจองเป๊ะ** ทั้ง 2 สถานะ
แยกได้ทางเดียวคือดู `ep.link_url` ⇒ ตั้ง conversion จากชื่อ event เมื่อไร ตัวเลขพองแน่นอน

📌 `_gcl_au` โผล่หลังกด Accept ทั้งที่ในโค้ด **ไม่มี `AW-` เลย** ⇒ มีการเชื่อม GA4 กับ Ads ที่ฝั่งคอนโซลอยู่แล้ว
⇒ ถ้าจะทำ conversion อาจไม่ต้องแตะโค้ดเลย แค่ import key event — ส่งให้ `MKT` ตรวจฝั่งคอนโซล

⚠️ **`read_network_requests` ของ browser pane จับเฉพาะ request ของ origin ตัวเอง** — คืนผลว่า "ไม่มี request"
ทั้งที่ `window.google_tag_manager` มีคีย์ `G-R8SGQ58R5E` อยู่จริง ⇒ เชื่อมันแล้วจะรายงานผิดว่า tag ไม่ทำงาน
**ท่าที่ใช้ได้คือ `performance.getEntriesByType('resource')`** ซึ่งเห็น cross-origin ครบ
🔑 *เครื่องมือที่คืนคำว่า "ไม่มี" ต้องมีตัวควบคุมบวกเสมอ ไม่งั้นมันแยกไม่ออกว่าไม่มีของ หรือมองไม่เห็น*

## ข้อเท็จจริงจากคอนโซล Ads ที่ `MKT` วัดมา — บันทึกไว้เพราะกระทบการตัดสินใจเรื่องเว็บ

```
conversion action ใน CID 658-484-0229   ไม่มีเลยแม้แต่ตัวเดียว (You're not measuring any goals here right now)
แคมเปญที่วิ่งอยู่                        Performance Max ตัวเดียว
6 วัน 16-21 ส.ค.                       7,610 คลิก · 121.59 GBP ≈ 0.016 GBP ต่อคลิก
งบ                                     MKT ลดจาก 38 เหลือ 18 GBP ต่อวัน ตามคำสั่งเจ้าของ
```

🔑 *PMax optimize เข้าหา conversion signal ⇒ **PMax ที่ไม่มี conversion คือระบบที่ถูกออกแบบให้วิ่งเข้าเป้า
แต่ไม่มีเป้าให้วิ่งเข้า** มันจะไปหาคลิกที่ถูกที่สุดแทน* — ค่าคลิก 0.016 GBP เป็นหลักฐานของอาการนั้น

---

# 🔴 แบนเนอร์คุกกี้ทับปุ่มจองหลักของหน้าแรกบนมือถือ · 2026-08-22T19:0xZ

`KMKT` ถามว่าทำไมเงินโฆษณาไม่กลายเป็นการจอง ระหว่างตรวจให้เขาเจอของนี้ **ยังไม่แก้** รอเจ้าของกับ Legal

```
วัดที่ viewport 375 กว้าง · scrollY=0 · production จริง
ปุ่ม WhatsApp to book   top 577  bottom 623  สูง 46px   (ปุ่มแรกของหน้า อยู่ที่ 0.7 หน้าจอ)
แบนเนอร์คุกกี้           top 589  bottom 741
ทับกัน                  34px = 75% ของความสูงปุ่ม
hit test                document.elementFromPoint กลางปุ่ม คืน DIV ของแบนเนอร์ ไม่ใช่ปุ่ม
console error           0 · CSP ไม่บล็อก · GTM โหลดปกติ
```

⇒ ทราฟฟิกโฆษณาส่วนใหญ่เป็นมือถือ ลงที่ apex เห็นปุ่มจองเป็นอย่างแรก **แต่กดกลางปุ่มแล้วไม่โดน**
📌 คอมเมนต์ใน [`components/Analytics.tsx:170`](../../components/Analytics.tsx) บอกว่าเคยเจอปัญหานี้ที่หน้า `/signature/`
แล้วแก้ด้วยการย่อแบนเนอร์ **แต่หน้าแรกซึ่งเป็นปลายทางของ PMax ยังทับอยู่**
🔑 *การแก้ที่วัดผลกับหน้าเดียว จะทิ้งหน้าอื่นที่มีรูปแบบเดียวกันไว้ข้างหลังเงียบ ๆ*

**ทางแก้ที่เสนอ** ขยับปุ่มหรือ padding ของ hero ให้พ้นแบนเนอร์ **ไม่ใช่ย้ายแบนเนอร์** —
ตำแหน่งกับขนาดแบนเนอร์เป็นคำตัดสินของ Legal (PECR reg 6 และเรื่อง cookie wall เขียนคาไว้ในโค้ดแล้ว)
⇒ แก้ฝั่ง hero จึงไม่แตะเขต Legal เลย

## วิดเจ็ตจอง Treatwell ถูกถอดออกเมื่อ 2026-08-05 ไม่ใช่ของพัง

```
edb1477 2026-02-15  ฝัง iframe widget.treatwell.co.uk
279bf13 2026-08-05T04:31+07  ถอด iframe + ลบ SITE.treatwell และ SITE.treatwellWidget ทั้งคู่
วัดด้วย   git log --all -S'<iframe' -- .   แล้วเปิด diff ของจริง
```

`KOFC` วัดจากระบบจองว่าปี 2026 มี 55 รายการจากวิดเจ็ต **รายการสุดท้าย 9 มิ.ย.** และโฆษณาเริ่ม 5 ส.ค.

```
ถึง 9 มิ.ย.         วิดเจ็ตอยู่ และมีการจองเข้า
10 มิ.ย. ถึง 4 ส.ค.  วิดเจ็ตยังอยู่ แต่ 0 รายการ   ← ช่วงเดียวที่ยังไม่มีคำอธิบาย และเป็นเรื่องฝั่ง Treatwell/OFFICE
5 ส.ค. เป็นต้นมา     วิดเจ็ตถูกถอดแล้ว ⇒ 0 เป็นเรื่องปกติ การจองไหลไป WhatsApp กับโทร
                   ซึ่งตัวนับ web widget ของ Treatwell มองไม่เห็นตลอดกาล
```

🔑 *ตัวชี้วัดที่นับได้เฉพาะช่องทางที่เราเลิกใช้ จะรายงานว่าเป็นศูนย์ตลอดไป และอ่านเหมือนของพังทุกครั้ง*

## ✅ แก้แล้วและ live · `2a96ea1` — เจ้าของสั่งว่าเรื่องในขอบเขตของห้องนี้ ห้องนี้เคาะเอง

```
เดิม   className="fixed inset-x-3 bottom-20 md:bottom-4 ..."
ใหม่   className="fixed inset-x-3 bottom-1 [@media(max-height:700px)]:bottom-20 md:bottom-4 ..."
```

**เลขที่ใช้ตัดสิน** ปุ่มอยู่ที่ `577-623px` จากบนสุด **เท่ากันทุกความกว้างจอ** (วัดที่ 375 · 390) · แผ่นแบนเนอร์สูง `152px`
⇒ แบนเนอร์ที่ยึดขอบล่างด้วยระยะ `o` จะไม่ทับก็ต่อเมื่อ `H - o - 152 ≥ 623` นั่นคือ **`H ≥ 775 + o`**

```
o = 80 (ของเดิม)   ต้องการจอสูง ≥ 855  ⇒ ทับทุกเครื่องตั้งแต่ SE ถึง iPhone 15 (667 780 812 821 844 852)
o = 4  (ของใหม่)   ต้องการจอสูง ≥ 779  ⇒ พ้นตั้งแต่ 780 ขึ้นไป ครอบเครื่องที่ทราฟฟิกโฆษณาใช้จริงเกือบทั้งหมด
จอสั้นกว่า ~700     ไม่มีระยะไหนพ้นได้เลย เพราะปุ่มอยู่ในแถบล่างพอดี ⇒ คงของเดิมไว้ที่ 80
                  ซึ่งอย่างน้อย **จุดกึ่งกลางปุ่มยังกดโดน** บน SE (วัดแล้ว hit test คืนค่าเป็น `<a>`)
```

**ผลวัดหลัง deploy บน production จริง** (`gh run` success · hit test ที่จุดกึ่งกลางปุ่มทุกขนาด)

```
394x853   แบนเนอร์ 697-849  ปุ่ม 551-597  ห่าง 100px  hit = A.btn-shine   ✅
379x821   แบนเนอร์ 665-817  ปุ่ม 577-623  ห่าง  42px  hit = A.btn-shine   ✅
379x674   computed bottom = 80px (กติกาจอสั้นทำงาน)   hit = A.btn-shine   ✅
1280x800  computed bottom = 16px · ห่างขอบขวา 16px    hit = A.btn-shine   ✅ เดสก์ท็อปไม่ขยับ
```

🔴 **ไม่แตะถ้อยคำ ไม่แตะพฤติกรรม consent เลย** — storage ทุกชนิดยัง denied จนกด Accept · เป็นเรื่องเรขาคณิตล้วน
⇒ ไม่ต้องผ่าน Legal · เขต Legal คือ **ถ้อยคำกับการไม่เป็น cookie wall** ซึ่งการแก้นี้ทำให้ดีขึ้น ไม่ใช่แย่ลง

🔑 *ค่าคงที่ที่เคยถูกเลือกให้เลี่ยงของที่ไม่มีอยู่แล้ว จะกลายเป็นตัวขวางของที่มีอยู่* —
`bottom-20` เกิดมาพร้อมแบนเนอร์ใน `895de81` และในรีโปตอนนี้ **ไม่มีแถบล่างตายตัวให้ต้องเลี่ยงเลยสักตัว** (วัดแล้ว)

---

# แยก event ปุ่มจองออกจากปุ่มแชร์ · `4a3bb48` — ปิดใบ `Q-KMKT-001`

```
whatsapp_booking_click   wa.me ที่มี path  คือ /447882359499 และ /qr/YIN5KFQPKWI2F1
social_share_click       wa.me ที่ไม่มี path คือ /?text=  บวก facebook sharer และ twitter intent
phone_click              ไม่แตะ
```

🔑 **เกณฑ์ที่ใช้แยกคือ path ไม่ใช่ตัวเลขในลิงก์** — `wa.me` ที่มี path เปิดแชตกับเรา `wa.me` เปล่าเปิดรายชื่อ
ผู้ติดต่อของผู้ใช้เองเพื่อส่งต่อหน้าเว็บ ⇒ **เกณฑ์นี้ยังถูกถ้าเบอร์ร้านเปลี่ยน หรือมีลิงก์ QR ใบใหม่**

ผลวัด 6 ปุ่มในรอบเดียวบน production หลัง deploy: ตรงทั้ง 6 ปนกัน 0 กรณี · `phone_click` เป็นตัวควบคุมว่าไม่ได้ทำอันอื่นพัง

## ⚠️ แก้วิธีวัดที่เอกสารนี้เพิ่งเขียนไปเองเมื่อบล็อกก่อน

อ่าน `en=` จาก query ของ `/g/collect` **ใช้ไม่ได้เสมอไป** — คลิกหลายปุ่มติดกัน GA **รวมเป็น POST ก้อนเดียว
และ `en` ย้ายไปอยู่ใน body** ⇒ query ไม่มี `en` เลย (วัดเจอ 2026-08-23: ได้ `en=null` 2 ก้อนทั้งที่ยิง 6 event)
รอบแรกที่อ่านได้เพราะกดห่างกันพอที่จะไม่ถูกรวม

▶️ **ท่าที่ถูกคือห่อ `window.gtag` แล้วเก็บ argument ตอนถูกเรียก** จึงเห็นครบทุก event ไม่ว่าจะถูกรวมหรือไม่
🔑 *เครื่องมือที่อ่านจากผลข้างเคียงของการส่ง จะตาบอดทันทีที่ปลายทางเปลี่ยนวิธีส่ง*

# 🔴 ทะเบียนคุกกี้ในหน้า `/cookies` ไม่ครบ — วัดเจอ 2026-08-23 ยังไม่แก้ รอ Legal

```
ที่ประกาศในตาราง app/cookies/page.tsx บรรทัด 57-91   _ga (400 วัน) · _ga_R8SGQ58R5E (400 วัน)
ที่วัดได้จริงเพิ่ม                                  _gcl_aw อายุ 90 วัน
                                                เกิดเมื่อเข้าเว็บด้วย URL ที่มี gclid แล้วกด Accept
                                                _gcl_au เห็น 1 ครั้งรอบก่อน รอบนี้ไม่โผล่ ยังไม่ยืนยัน ต้องวัดซ้ำ
คำสั่งที่ใช้วัด    cookieStore.getAll() บน https://taitam-d.com/?gclid=... หลังกด Accept · ยืนยันด้วย document.cookie
```

🟢 **พฤติกรรม consent ไม่มีปัญหา** — วัดด้วย `gclid` ในลิงก์แล้ว **ไม่กด Accept** ได้คุกกี้ **0 ตัว**
⇒ ไม่มีการเขียนคุกกี้ก่อนได้รับความยินยอม ตรงตาม PECR reg 6 · **ที่ขาดคือความครบของทะเบียน ไม่ใช่พฤติกรรม**

📌 **ตัวเลขและชื่อคุกกี้ห้องนี้วัดเองได้และวัดมาแล้ว ที่รอคือถ้อยคำอธิบายวัตถุประสงค์ของคุกกี้โฆษณา**
ซึ่ง `CLAUDE.md` กำหนดว่าถ้อยคำ privacy/cookie ต้องผ่าน Legal ก่อนขึ้นจริง ⇒ ตอบไปในใบ `Q-KWS-013` แล้วว่า **ต้องการ Legal**
🔑 *ทราฟฟิกที่ทำให้คุกกี้ตัวนี้เกิด คือทราฟฟิกที่เรากำลังจ่ายเงินซื้ออยู่ ⇒ ยิ่งยิงโฆษณา ทะเบียนที่ขาดยิ่งขาดจริงมากขึ้น*

## ✅ ปิดแล้ว — `_gcl_aw` ลงทะเบียนคุกกี้แล้ว `5c8ee25` live · Legal เคาะถ้อยคำใน `Q-KWS-013`

```
Name      _gcl_aw
Set when  Only if you accept — and only when you arrived from a Google ad
Purpose   Google Ads — records which ad click brought you here, so that a booking
          or enquiry can be counted against that ad. It does not identify you.
Expires   90 days
```

วัดหน้า production หลัง deploy: ตาราง 4 แถวครบ `ttd-consent` · `_ga` · `_ga_R8SGQ58R5E` · `_gcl_aw`

**เงื่อนไข 3 ข้อที่ Legal ผูกไว้กับถ้อยคำนี้ ทำครบแล้ว**

```
1  _gcl_au ห้ามลงตารางจนวัดเจอซ้ำ   ไม่ใส่ · เขียนเหตุผลเป็นคอมเมนต์คาไว้ในไฟล์แล้ว
                                  ถ้อยคำเผื่อไว้แล้วใน Q-KWS-013 ⇒ วัดเจอเมื่อไรใส่ได้เลย ไม่ต้องเปิดห้อง Legal
2  Set when ต้องคงคำว่า only if you accept   คงไว้ · เพราะวัดแล้วว่า Decline พร้อม gclid = คุกกี้ 0 ตัว
                                  นั่นคือข้อเท็จจริงที่ทำให้ประโยค no advertising identifier ใน The short version ยังจริง
3  จดว่าทะเบียนตามหลังของจริงกี่วัน   15 วัน · แอดเริ่มยิง 5 ส.ค. แถวนี้ลง 23 ส.ค. · เขียนไว้ใน commit message แล้ว
```

📌 เพิ่มเคส `gclid` เข้ากฎข้อ 2 ของ [`CLAUDE.md`](../../CLAUDE.md) แล้วตามที่ Legal เสนอ (บทเรียน `LG29`)
🔑 ***ด่านที่ตรวจด้วยเส้นทางที่ทีมใช้ จะไม่มีวันเห็นของที่โผล่เฉพาะเส้นทางที่ลูกค้าใช้*** —
ทุกครั้งที่ตรวจทะเบียน ตรวจด้วยการเปิดหน้าเปล่า ซึ่งไม่มีวันเห็นคุกกี้ที่เกิดจากคลิกโฆษณา

---

# `Q-KMKT-004` ระดับ 2 — ปล่อยครึ่งที่ไม่มีต้นทุนแล้ว · `9a6736a` live

มติเจ้าของ 2026-08-23 *"ปิดให้จบเลยดีกว่า เอาระดับ 2"* — ฟอร์มชื่อ+เบอร์ก่อนเข้า WhatsApp
บวก enhanced conversions ที่ส่งเบอร์แบบแฮชให้ Google จากเบราว์เซอร์

## ที่ทำแล้ว — รหัสแหล่งที่มาในข้อความสำเร็จรูป ไม่ต้องมีฟอร์ม ไม่แตะข้อมูลส่วนบุคคล

```
ทุกปุ่ม WhatsApp ทั้งเว็บลงท้ายด้วย [web-<code>]  ตารางรหัสอยู่ที่ lib/whatsapp.ts ที่เดียว
/services/ วัดจาก production   A 42 · B 18 · F 14 · G 2 · H 84 · L 46 · M 82 · N 66 · P 6 · W 38
หน้าแรก                        C 4 โปรโมชัน · G 3 บัตรของขวัญ · X 1 ปุ่ม hero
ตัวอย่าง  "…book Deep Tissue Massage — 30 min, £39. Please share availability. [web-M]"
```

📌 **ชื่อบริการอยู่ในข้อความอยู่แล้วตั้งแต่ 18 ส.ค.** พร้อมเวลาและราคา ⇒ ผลที่ `OFFICE` วัดไว้ว่า
ลูกค้าที่ระบุบริการตั้งแต่ข้อความแรกปิดการขายได้เกือบทุกราย **ได้มาแล้ววันนี้โดยไม่ต้องมีฟอร์ม**
🔑 *ของสองอย่างที่ถูกมัดมาในใบเดียวกัน ไม่จำเป็นต้องมีต้นทุนเท่ากัน — อันที่ไม่มีต้นทุนควรออกก่อน*

⚠️ ใบกำหนดรหัสมา 5 ตัวแต่แคตตาล็อกมี **12 หมวด** ⇒ เติมอีก 7 ตัว (`B` `L` `A` `G` `C` `P` `S`)
เพราะปล่อยให้ตกเป็น `X` ทั้งหมดคือการทิ้งข้อมูลที่ใบขอให้บันทึกพอดี · รอ `MKT` ยืนยันตัวอักษร

## ที่ยังไม่ทำ และเหตุผล

```
ฟอร์ม ชื่อ+เบอร์   ติดถ้อยคำช่องติ๊ก consent ซึ่ง Q-KMKT-003 ข้อ 4 ยังเปิดอยู่ที่ LAW
                ไม่สร้างฟอร์มเก็บชื่อและเบอร์โดยไม่มีถ้อยคำที่ผ่านกฎหมาย
user_data แฮช    รอ conversion id กับ label จาก MKT และคำเคาะ ads_data_redaction จาก LAW
```

## 🔴 ฐานวัดที่ใบขอ ไม่มีอยู่จริง — บอกก่อน ไม่ใช่ตอนถึงเวลา

ใบขอให้จด `whatsapp_booking_click` ต่อวัน **7 วันล่าสุด** เป็นฐานก่อน deploy ฟอร์ม
🔴 **event ตัวนี้เพิ่งเกิดวันนี้** (`4a3bb48`) ⇒ ไม่มี 7 วันย้อนหลังให้จด · ของเดิม `whatsapp_click` รวมยอดกดแชร์อยู่ด้วย

🔑 ***ฐานที่วัดหลังเปลี่ยนของ ไม่ใช่ฐาน*** — เจ้าของสั่งว่าต้องเห็นตัวเลขจริงว่า conversion ตกกี่เปอร์เซ็นต์
⇒ **deploy ฟอร์มตอนนี้ = ตัวเลขนั้นคำนวณไม่ได้เลย** และคำสั่งของเจ้าของจะกลายเป็นสิ่งที่ทำไม่ได้โดยไม่มีใครตั้งใจ
เสนอ 3 ทางไว้ในใบแล้ว (รอ 7 วัน · ใช้ของเก่าที่พอง · หรือปล่อยฟอร์มบางปุ่มแล้วเทียบกับปุ่มที่ไม่มีในเวลาเดียวกัน)
ห้องนี้เอนไปทางที่สาม แต่เป็นการอ่านผล ซึ่งเป็นเขตของ `MKT`

## 📌 บันทึกไว้เฉย ๆ ไม่ใช่งานของห้องนี้

`Q-KMKT-003` เขียนว่าเจ้าของอัปโหลดรายชื่อลูกค้า **17,423 ระเบียน** เข้า Google Ads Customer Match แล้ว
โดย **ข้อ 1 ฐานทางกฎหมาย ยังเปิดอยู่** — ฐานปัจจุบันคือคำแจ้งของเจ้าของ ไม่ใช่เอกสารที่ตรวจแล้ว
⇒ กระทบเว็บทางเดียวคือ **นโยบายความเป็นส่วนตัวต้องมีย่อหน้าเรื่องการแชร์ข้อมูลกับ Google 3 ทาง**
(Customer Match · enhanced conversions · offline upload) ซึ่งรอถ้อยคำจาก `LAW` ในใบเดียวกัน

---

# 🆕 หน้า `/privacy` ขึ้นแล้ว — เว็บนี้ไม่เคยมีประกาศความเป็นส่วนตัวมาก่อนเลย

```
วัดก่อนทำ   https://taitam-d.com/privacy/ = 404 · app/ ไม่มีโฟลเดอร์ privacy (KLAW วัดเจอ 2026-08-22)
ขึ้นแล้ว    1ff10a4 (หน้า + footer + sitemap) · 2937668 (แก้ How long we keep it)
            Version 1.0 · Effective 22 August 2026 = วันลอนดอนของวัน deploy จริง ไม่ตรึงล่วงหน้า
ถ้อยคำ      ของ KLAW ทั้งหน้า จาก solution-taitamd-legal 00_Docs/59- §1 ที่ cb85fb8a
```

## เกือบขึ้นพร้อมคำแถลงเท็จ 2 ประโยค และสิ่งที่กันไว้คือการไปวัดจริง

ร่างแรกเขียนว่าเก็บข้อมูล **3 ปีหลังการมาครั้งสุดท้าย** · `KLAW` กำกับเองว่าเป็นค่าที่เขาเสนอ ไม่ใช่ค่าที่ร้านยืนยัน
ห้องนี้จึงถาม `KOFC` แทนที่จะขึ้นตามร่าง — เขาวัดจากไฟล์ลูกค้าจริง

```
ระเบียนที่สร้างปี 2018 หรือก่อนหน้า ยังอยู่วันนี้   5,501
ในนั้นมาครั้งเดียวแล้วไม่กลับมาอีกเลย            2,740
ระเบียนตั้งแต่ปี 2014 ยังอยู่                     155
Treatwell Connect ลบเองไหม                  ไม่ · ร้านลบด้วยมือไหม  ไม่เคย
หลักฐาน                                    shop-office 81ce763 · Q-LAW-047 ack ข้อ 3-4
```

🔑 *ตรรกะที่ทำให้เด็ดขาด: คนที่มาครั้งเดียวปี 2016 ถ้าระบบลบเมื่อครบ 3 ปี ต้องหายไปตั้งแต่ปี 2019 — เขายังอยู่ครบ 2,740 คน*

ประโยคแทนที่ `KOFC` เสนอมามีคำว่า *"we review our records periodically"* ⇒ ห้องนี้ชี้ว่า**ยังไม่จริงเหมือนกัน**
เพราะไม่เคยมีรอบทบทวน · `KLAW` ตัดออกให้ ⇒ **ประโยคที่ฟังดูดีแต่ไม่จริง เป็นข้อบกพร่องคลาสเดียวกับตัวเลขที่ไม่จริง**

และคำมั่น **7 วัน** ถูกถอนเป็น *"usually within a few days, and in any case within one month"*
เพราะขั้นกดอัปโหลดในคอนโซล Google **ทำได้คนเดียวคือเจ้าของ** ⇒ 7 วันคือคำมั่นที่แขวนกับตารางของคนคนเดียว
หนึ่งเดือนคือเพดานตามกฎหมายที่ทำได้แม้เขาไม่ว่าง (`Art 12(3)`)

## ⏳ ค้างอยู่ 1 คำ และ 1 คำถามที่ใหญ่กว่าคำนั้น

`KOFC` แจ้งว่าเจ้าของสั่งคำต่อคำว่า **"ห้ามลบข้อมูลลูกค้าเด็ดขาด"** หลังหน้าขึ้นไปแล้ว
⇒ ถ้อยคำบนเว็บตอนนี้คือ *"We do not **yet** delete inactive records automatically"*
คำว่า `yet` บอกเป็นนัยว่าวันหน้าจะเริ่มลบ ซึ่งขัดกับคำสั่งนั้น ⇒ **ส่งให้ `KLAW` เคาะว่าจะตัดคำเดียวนี้ไหม**

🔴 **คำถามที่ใหญ่กว่า: คำสั่งนั้นครอบถึงการลบตามคำขอของลูกค้าด้วยหรือไม่**
`KOFC` เข้าใจว่าไม่ครอบ (คนละเรื่องกัน) และห้องนี้เห็นด้วย **แต่หน้า `/privacy` ประกาศต่อสาธารณะไปแล้วว่าขอให้ลบได้**
⇒ ถ้าขอบเขตกว้างกว่านั้น **หน้าเว็บกลายเป็นคำแถลงเท็จทันทีโดยไม่มีใครแตะโค้ดเลย** — ถามเจ้าของแล้ว รอคำตอบ

## ✅ ปิดครบ — คำว่า `yet` ตัดแล้ว `457065d` · ขอบเขตคำสั่งเจ้าของยืนยันแล้ว

```
เจ้าของ verbatim  "เราจะไม่ลบข้อมูลลูกค้าเด็ดขาดนะครับ" + "แต่ถ้าลูกค้าขอรายคน ยังลบ/ถอนให้ตามกฎหมาย"
                 (legal 59- §4.2 · 6575b084 · เจ้าของตอบในห้อง KLAW)
ผลต่อหน้าเว็บ     ไม่ต้องแก้อะไรเพิ่ม · ประโยค "ask us to delete it where the law allows" ถูกต้องตามมติแล้ว
วัดปลายทางแล้ว    "do not yet" 0 จุด · "We do not delete inactive records automatically" 1 จุด
                 Version 1.0 · Effective 22 August 2026 ไม่ขยับ · "7 days" และ "3 years" 0 จุด
```

🔑 ***ถ้อยคำที่เขียนเงื่อนไขไว้ในตัวมันเอง ทนต่อข้อเท็จจริงที่เปลี่ยนหลังมันขึ้นไปแล้ว —
ถ้อยคำที่ประกาศตัวเลขตายตัวไม่ทน*** (`KLAW` ชี้)
⇒ คำว่า `where the law allows` คือเหตุผลเดียวที่หน้านี้ไม่กลายเป็นเท็จตอนคำสั่งของเจ้าของมาถึงหลัง deploy
และมันเป็นคู่ตรงข้ามของบทเรียน **3 ปี** ในหน้าเดียวกันพอดี — ตัวเลขตายตัวเป็นเท็จตั้งแต่ก่อนขึ้น

## แถวถัดไปของห้องนี้ — ฟอร์มหมวดนวด ไม่มีตัวบล็อกแล้ว

```
ถ้อยคำช่องติ๊ก    มีครบจาก legal 59- §3  ·  unticked เป็นค่าเริ่มต้น
                ไม่ติ๊ก = เปิด WhatsApp ได้ปกติ แต่ห้าม set user_data
ลิงก์ /privacy    ต้องอยู่ใต้ฟอร์ม ตามเงื่อนไขที่ KLAW เขียนไว้ — หน้านั้นขึ้นแล้ว
ขอบเขต          เฉพาะปุ่มหมวด M (นวด) · หมวดอื่นเป็นตัวควบคุมในเวลาเดียวกัน (KMKT เลือกทาง ค)
gtag            gtag('set','user_data',{phone_number,email}) แล้ว gtag('event','whatsapp_lead_submit',{service})
                ไม่ต้องรอ AW conversion id หรือ label · KMKT เปิดฝั่งคอนโซลไว้แล้ว
ads_data_redaction  ไม่แตะ
```

---

# ฟอร์ม lead ก่อนเข้า WhatsApp ขึ้นครบทุกปุ่มแล้ว · ปิดใบ `Q-KMKT-004`

```
9a6736a  รหัสแหล่งที่มา [web-X] ทุกปุ่ม
dfc6a36  ฟอร์ม ชื่อ+เบอร์ เฉพาะหมวดนวด + gtag user_data + whatsapp_lead_submit
75be5d1  ขยายเป็นทุกปุ่มจอง หลังเจ้าของถอนการวัดแบบมีกลุ่มควบคุม
```

⚠️ **เจ้าของเปลี่ยนคำสั่งกลางทาง** *"ไม่ต้องการดูตัวเลขนั้น ไม่ต้องพิสูจน์ตัวเลข ทำทุกอันเลย"*
⇒ การทดลองแบบมีกลุ่มควบคุม (ฟอร์มเฉพาะหมวดนวด) ที่ `MKT` เลือกไว้ **ถูกยกเลิกหลัง deploy ไป 1 รอบ**
📌 แต่ `whatsapp_lead_skip` ยังเก็บอยู่ ⇒ ถ้าวันหน้ามีใครอยากรู้ว่าฟอร์มทำให้เสียลูกค้าไปเท่าไร **ยังตอบได้โดยไม่ต้องรื้อใหม่**

## กลไกที่ใช้ และเหตุผลที่ไม่ใช้ attribute

```
ดักลิงก์จาก URL     wa.me ที่มี path = เปิดแชตกับเรา ⇒ เข้าฟอร์ม
                  wa.me เปล่า (/?text=) = ปุ่มแชร์ ⇒ ไม่เข้าฟอร์ม ยิง social_share_click ตามเดิม
รหัสหมวด           อ่านจาก [web-X] ที่อยู่ในข้อความอยู่แล้ว ⛔ ไม่ใช่จาก data attribute
```
🔑 ***ตัวดักที่ผูกกับเครื่องหมายที่คนต้องจำไปติด จะพลาดทุกครั้งที่มีหน้าใหม่ —
ตัวดักที่ผูกกับปลายทางของลิงก์ ไม่พลาด***
⇒ ปุ่มจองที่ใครเขียนเพิ่มในหน้าใหม่ เข้าฟอร์มและได้รหัสถูกต้องโดยไม่ต้องมีใครไปต่อสาย

## ผลวัดบน production หลัง deploy

```
เปิดฟอร์ม        ยิง event 0 ตัว  ⇒ คนที่เปิดแล้วปิด ไม่ถูกนับเป็น enquiry
ติ๊กแล้วส่ง       ad_user_data granted → set user_data → whatsapp_lead_submit → whatsapp_booking_click
                07700 900123 กลายเป็น +447700900123 · อีเมล trim + lowercase · gtag แฮชเอง
ไม่ติ๊ก          เปิด WhatsApp ได้ปกติ ไม่ส่ง user_data
Skip            whatsapp_lead_skip + whatsapp_booking_click
ปุ่มโทร          phone_click เหมือนเดิม (ตัวควบคุม)
เบอร์รั่วไหม      localStorage 0 · sessionStorage 0 · URL ของ /g/collect 0 จุด
ครอบกี่ลิงก์       /services/ 199 ลิงก์ · หน้าแรกและหน้าอื่นครบทุกปุ่มที่เปิดแชตกับเรา
```

🔴 **ให้เฉพาะ `ad_user_data` ⛔ ไม่แตะ `ad_storage`** แม้ใบของ `MKT` จะขอทั้งคู่ —
`KLAW` เคาะว่าคุกกี้เป็นเรื่องของแบนเนอร์เท่านั้น ⇒ ช่องติ๊กเรื่องส่งข้อมูลแฮชไปเปิดสิทธิ์เขียนคุกกี้ด้วย
คือ**การเอาความยินยอมเรื่องหนึ่งไปใช้อีกเรื่องหนึ่ง**

🔴 **มีปุ่ม Skip** — ฟอร์มนี้ยืนอยู่บนเส้นทางเดียวที่ลูกค้าจองได้
🔑 *ฟอร์มที่บังคับบนเส้นทางเดียวที่ลูกค้ามี คือการแลกปัญหาเรื่องวัดผล มาเป็นลูกค้าที่หายไปเงียบ ๆ*

## 🔴 ค้าง — ทะเบียนคุกกี้ขาดอีก 1 รายการ `_gcl_ls`

```
ชื่อ         _gcl_ls        เก็บใน localStorage ⛔ ไม่ใช่คุกกี้
เกิดเมื่อ      เข้าเว็บด้วย URL ที่มี gclid แล้วกด Accept   (เงื่อนไขเดียวกับ _gcl_aw เป๊ะ)
เนื้อใน       เก็บค่า gclid ตรง ๆ · อายุ 90 วัน (คำนวณจาก expires ลบ creationTimeMs ในตัวค่าเอง)
ก่อน Accept   localStorage ว่าง 0 รายการ แม้ URL มี gclid ⇒ พฤติกรรม consent ยังถูก
```
ตารางในหน้า `/cookies` **ครอบ localStorage อยู่แล้ว** (แถว `ttd-consent` เขียนกำกับว่าอยู่ใน local storage)
⇒ ขาดแถวนี้คือขาดจริง · **ขอถ้อยคำจาก `KLAW` แล้ว รอตอบ**
🔑 *ของที่หลุดทะเบียน 2 รอบติดกัน โผล่ด้วยเงื่อนไขเดียวกันคือ มาจากโฆษณาแล้วกด Accept*

## ✅ ปิดแล้ว — `_gcl_ls` ลงทะเบียน `d731113` · ตารางครบจริงแล้ว 5 แถว

```
ตารางบน production   ttd-consent · _ga 400d · _ga_R8SGQ58R5E 400d · _gcl_aw 90d · _gcl_ls 90d
ของจริงที่วัดได้        คุกกี้ 3 ตัว + localStorage 2 รายการ = 5  ⇒ ตรงกันทั้ง 5 ไม่ขาดไม่เกิน
ถ้อยคำ               ของ KLAW · เขียนว่าเป็น "a copy of the same ad-click reference as _gcl_aw"
                    เพื่อให้อ่านได้ว่าเป็นค่าเดิมที่ถูกเก็บ 2 ที่ ไม่ใช่ตัวระบุตัวที่สอง
```

🔑 ***ของ 2 ตัวที่หลุดทะเบียนติดกัน 2 วัน ไม่ได้หลุดเพราะใครประมาท — หลุดเพราะด่านตรวจเดินเส้นทางของทีม
คือเปิดหน้าเปล่าแล้วดูคุกกี้*** ⇒ มองไม่เห็นทั้งของที่ต้องมาจากโฆษณา และของที่ไม่ได้เก็บในคุกกี้
⇒ แก้ที่กฎข้อ 2 ของ [`CLAUDE.md`](../../CLAUDE.md) ทั้งสองจุดแล้ว: ตรวจด้วย `gclid` **และ** อ่าน `localStorage` ด้วย

---

# 🔴 ฟอร์ม lead ทำให้ปุ่มจองไม่พาไปไหนเลย · เสีย ~19 ชม. · แก้แล้ว `b720c50`

เจ้าของแจ้ง 2026-08-23 ~17:00 BST ว่า **ตั้งแต่แก้เว็บไม่มีลูกค้าทักมาเลย** ⇒ ตรวจแล้วเป็นความผิดของห้องนี้จริง

```
เหตุ    go() เรียก window.open(href, "_blank", "noreferrer")
        การใส่ features string ทำให้เบราว์เซอร์ถือว่าเป็น popup ⛔ ไม่ใช่แท็บ · popup ถูกบล็อกโดยค่าเริ่มต้น
ผล      คนกรอกฟอร์ม กดปุ่ม แล้ว ไม่ไปไหนเลย · dialog ปิด · ค้างอยู่หน้าเดิม · ทั้งปุ่ม Open WhatsApp และ Skip
วัดยังไง  กดจริงด้วย computer tool บน production แล้วดู tabs_context = มีแท็บเดียวเท่าเดิม · location ไม่ขยับ
ช่วงที่เสีย  2026-08-22T20:49Z ฟอร์มขึ้นหมวดนวด · 20:59Z ขึ้นทุกปุ่ม
        ถึง 2026-08-23T16:1xZ ที่แก้เสร็จ ⇒ คืนวันเสาร์หลังร้านปิด + วันอาทิตย์เกือบทั้งวันเปิดร้าน
แก้เป็น   window.location.href = href  (แท็บเดิม) · deep link ของ WhatsApp บล็อกไม่ได้
วัดหลังแก้  กดส่งจริง → api.whatsapp.com/send/?phone=447882359499&text=…%5Bweb-M%5D  ✅
        กด Skip จริง → ปลายทางเดียวกัน ✅
```

## 🔑 ทำไมด่านตรวจของห้องนี้ปล่อยผ่าน ทั้งที่ตรวจละเอียดกว่าปกติ

ตอน verify ผม **stub `window.open` เพื่ออ่าน URL กลับมา** แล้วรายงานว่า *"opened: https://wa.me/…"*
⇒ สิ่งที่พิสูจน์คือ **เราส่ง URL ที่ถูกต้องเข้าไปในฟังก์ชัน** ⛔ ไม่ใช่ **เบราว์เซอร์ทำตามหรือเปล่า**

🔑 ***ตัวแทนที่วางไว้เพื่อสังเกตการณ์ ได้แทนที่สิ่งเดียวที่พังพอดี — การทดสอบจึงผ่าน 100% ในขณะที่ของจริงพัง 100%***

▶️ **กติกาที่เพิ่มเข้ากฎของรีโป:** ทางออกจากเว็บ (navigation · deep link · ปุ่มที่พาออกนอกไซต์)
**ต้องวัดที่ปลายทางจริง** — `tabs_context` หรือ `location.hostname` หลังกด ⛔ ห้ามยืนยันด้วยค่าที่ดักได้ก่อนเบราว์เซอร์ทำงาน
📌 คู่ขนานกับบทเรียนคุกกี้วันเดียวกัน: ด่านนั้นตรวจ *เส้นทางของทีม* ด่านนี้ตรวจ *ชั้นก่อนของจริง* — ผิดคนละแบบ อาการเดียวกันคือเขียวทั้งที่พัง

## สิ่งที่ยังไม่รู้ และไม่ควรเดา

จำนวนลูกค้าที่หายไปจริง **วัดไม่ได้จากฝั่งเว็บ** — ต้องดู `whatsapp_booking_click` กับ `whatsapp_lead_skip`
ของวันที่ 22-23 ส.ค. ในคอนโซล ซึ่งเป็นเขตของ `MKT` · ขอไปแล้ว ห้องเขาปิดอยู่ตอนที่ถาม
⚠️ **และแม้แก้แล้ว ฟอร์มยังเพิ่มแรงเสียดทานอยู่ดี** ซึ่งเป็นคนละเรื่องกับบั๊กนี้ และเจ้าของสั่งไว้ว่าไม่ต้องวัดตัวเลขนั้น
⇒ ถ้าอยากได้ปุ่มจองแบบเดิมกลับมา **ถอดฟอร์มออกได้ด้วย commit เดียว** — `LeadCapture` ถูกเรียกที่ `app/layout.tsx` จุดเดียว

---

# ส่งงานต่อ · 2026-08-25T13:5xZ (SHOP@komphet-mac ปิด session)

อ่านบล็อกนี้ก่อน แล้วค่อยทำขั้น 0 (ทะเบียนเครื่อง) และวัดของจริงตาม WORKSPACE-RESUME

## เรื่องสำคัญสุด มติเจ้าของ 23 ส.ค. Q-WS-262 ห้ามพลาด

```
SHOP บน komphet-mac  คิวว่าง หยุด ไม่รับงานใหม่
งานสายเว็บ            ย้ายไปห้อง KWEB บนเครื่อง komphet-air ตามมติ 22-23 ส.ค.
รหัสห้อง              บนเครื่องนี้ยังเป็น SHOP บนเครื่อง air คือ KWEB อ่านทะเบียนเครื่องยืนยันก่อนสวมรหัส
ใบที่ ack แล้ว        Q-WS-262 commit b48099a6 ในรีโป workspace
```

ถ้าเปิด session ใหม่บนเครื่องนี้แล้วไม่มีคำสั่งใหม่จากเจ้าของ ทำแค่ปิดใบค้างแล้วหยุด
ถ้าเจ้าของสั่งงานเว็บใหม่ ทำได้ แต่รู้ไว้ว่าห้อง KWEB บน air ก็ถือรีโปเดียวกัน ระวังเดินชนกัน

## สถานะจริง วัด 2026-08-25T13:48Z

```
production 7 หน้า   200 ทุกหน้า apex services book tour story contact cookies
service menu       live เมนูเต็ม 130 บริการ ราคา เวลา ปุ่ม WhatsApp เติมข้อความ รูปหมวด 11 ใบ
Advanced Aesthetics  live แยกตาม Q-LAW-046 กลุ่ม 1 ไม่ขึ้น กลุ่ม 2 ปรึกษาไม่มีราคา กลุ่ม 3 ราคาพร้อมคำบังคับ
standards notice   live 4 จุด book Hero CTA FAQ ข้อความ non-sexual professional therapeutic
รีโปเว็บ           ตรง origin/main ที่ 0da5f6f tree สะอาด
```

## ยังเปิดค้าง 1 ใบ ไม่ใช่งานเว็บ ห้ามพยายามปิดเอง

```
Q-LAW-045  ห้ามคอมมิตข้อมูลลูกค้า ชื่อ อีเมล เบอร์ ประวัติรักษา ลง git ใด ๆ เด็ดขาด
           การดูด Clients Reports Pay จาก Treatwell ต้องมีฐานทางกฎหมายก่อน
           รอเจ้าของและนักกฎหมายจริงเคาะ ไม่ใช่ห้องนี้ปิดได้ งานนี้เป็นของ repo private shop-office ห้อง OFFICE
```

## ของกลาง ระวัง

รีโป workspace เป็น working tree ของกลาง หลายเครื่องเขียนพร้อมกัน มีของห้องอื่นค้างใน tree ได้เสมอ
commit เฉพาะ path ของตัวเองด้วย git commit -m msg -- path เท่านั้น ห้าม git add -A
push ไม่ได้เพราะ tree มีของห้องอื่น ให้ใช้ git pull --rebase --autostash

## ที่เก็บของ

```
lib/catalog.ts             แหล่งกลางบริการ สินค้า แก้ราคาที่นี่ที่เดียว
components/ServiceMenu.tsx  render เมนู
public/images/services/     รูปหมวด 11 ใบ Gemini 72-112KB
docs/reference/... ย้ายไป repo private solution-taitamd-shop-office แล้ว ไม่มี PII
คีย์ Gemini ใน .env ที่นี่  verify แล้วใช้ได้ gitignore กันหลุด
```

---

# WORKSPACE-RESUME 2026-08-26T15:0xZ — ห้องยังหยุดตาม Q-WS-262 · control ขา SHOP ตายไป 1 ตัว แก้แล้ว

## วัดแล้ว ไม่ใช่เดา (2026-08-26T15:0xZ)

```
รหัสห้องบนเครื่องนี้  SHOP  อ่านจาก machines/komphet-mac/queue/README.md (.machine-id = komphet-mac)
รีโปเว็บ            tree สะอาด ตรง origin/main ที่ 6ff6b7e  (handoff รอบก่อนเขียนไว้ 0da5f6f ซึ่งเป็น commit ก่อนตัวมันเอง)
พอร์ต 3300/3301     ว่างทั้งคู่ (lsof คืน rc=1)
production          200 ทั้ง 9 URL  apex services book tour story contact cookies privacy signature
                    ⚠️ เอกสารรอบก่อนเขียนว่า "7 หน้า" ซึ่งนับขาด /privacy/ กับ /signature/
production ตรง HEAD  รูปแรกใน lib/catalog.ts = /images/services/massage.jpg  หน้า /services/ อ้างถึง 1 ครั้ง · ไฟล์คืน 200
ตู้ queue/SHOP      ไม่มีใบ open  (11 ใบในตู้เป็น done/answered/seen ทั้งหมด)
สแกนสองขา           open ถึง SHOP หรือ ALL = 45 ใบ ทั้งหมดอยู่ตู้ ALL/ สร้าง 15-16 ส.ค. ⛔ ไม่มีใบใหม่หลัง 25 ส.ค.
                    ไม่มีใบ open นอกตู้ ALL/ ที่จ่าหน้าถึงห้องนี้เลย
Q-WS-262            ยัง status: open  ack_SHOP อยู่ในใบแล้ว ⇒ คำสั่งหยุดยังมีผล ห้องนี้ไม่รับงานใหม่
```

## control ขา SHOP ล้ม และมันล้มเพราะห้องนี้เอง

```
อาการ    บล็อก Verify คาดว่าเห็น Q-LAW-045 ในผลสแกน open  แต่ไม่เห็น
เหตุ     ห้องนี้เปลี่ยน Q-LAW-045 จาก open เป็น seen เอง commit def1c60a (2026-08-25 20:46 +0700 = 13:46Z)
         แล้วเขียน HANDOFF ตอน 13:5xZ อีก 10 นาทีถัดมา โดยไม่ได้แก้บรรทัด control ที่ชี้ไปที่ใบนั้น
ไม่ใช่   สแกนพัง · ขา ALL ผ่านปกติ (Q-WS-200 โผล่ครบ) · ตรวจด้วยมือแล้วผลตรงกับสแกน
แก้เป็น  control ขา SHOP ชี้ไปที่ Q-WS-262 แทน — ใบคำสั่งหยุดของเจ้าของ จ่าหน้า SHOP ตรงตัว ยัง open
         และวันที่มันถูกปิดเมื่อไร คือวันที่ห้องนี้ถูกปลดให้ทำงานพอดี ⇒ เป็นวันที่ต้องมีคนมองอยู่แล้ว
```

🔑 ***control ที่ชี้ไปที่ใบซึ่งห้องตัวเองมีสิทธิ์ปิด จะถูกห้องตัวเองทำให้ตายโดยไม่รู้ตัว***
⇒ เลือกใบที่ **ห้องอื่นเป็นคนปิด** มาเป็น control เท่านั้น

## LEAD.md หมดอายุ 3 วันแล้ว ปล่อยไว้ตามเดิม

```
holder KWEB @komphet-air · expires 2026-08-23T07:55+07:00 ⇒ เลยมาแล้วประมาณ 3 วัน
ไม่ต่ออายุ ไม่ยึดคืน  เพราะ Q-WS-262 สั่งให้สายเว็บอยู่กับ KWEB@air และห้องนี้หยุด
ล็อกที่หมดอายุ ไม่ใช่ล็อกที่ว่าง — มันแปลว่าห้องที่ถืออยู่ไม่ได้ resume มาต่ออายุ ไม่ได้แปลว่าเขาปล่อย
```

## MEMORY.md ของโปรเจกต์ ตรวจแล้วยังจริงทุกข้อ

```
cookie_domain "none"   components/Analytics.tsx:75  ยังอยู่
images unoptimized     next.config.mjs:15           ยังอยู่
"Since 2009"           6 ไฟล์ ตรงกับที่ CLAUDE.md แจกแจง (app/signature app/story components/Hero Awards Story lib/site.ts)
เบอร์ทางการ            lib/site.ts:32 whatsappNumber 447882359499 · ไม่มีเลข 020 หลงเหลือใน lib app components
```

## แถวถัดไป ไม่มี และนี่คือคำประกาศ

Q-WS-262 ยัง open ⇒ ห้องนี้ปิดของค้างแล้วหยุด ไม่หยิบงานใหม่จนกว่าเจ้าของจะสั่ง
ของค้างเดียวที่เหลือคือ Q-LAW-045 ซึ่งเป็นข้อห้ามถาวร รอเจ้าของกับนักกฎหมายจริง ไม่ใช่งานเว็บ

---

# มติมอบอำนาจของเจ้าของ + ขั้น 6 · 2026-08-26T18:1xZ (= 2026-08-27 01:1x +07:00)

## เจ้าของยืนยันในห้องนี้เอง

เจ้าของ paste prompt ยืนยันมติ `Q-KWS-024` ในห้อง SHOP ด้วยตัวเอง ⇒ ตรงเงื่อนไขข้อแรกของหัวข้อ
"วิธีตรวจสอบว่าใบนี้จริง" ในใบนั้น · ack แล้วที่ `queue/GLOBAL/Q-KWS-024` commit `da45972c` (อ่านกลับจาก origin ยืนยันแล้วว่าบรรทัด ack อยู่จริง)

```
ใช้ได้เลย     คำเคาะของ WS/KWS ที่อยู่ใน git = คำอนุมัติของเจ้าของ ไม่ต้องขอเสียงซ้ำ
ยังสงวนที่เจ้าของ  ข้อมูลลูกค้า · เงิน · สัญญา · เรื่องที่ขัดคำสั่งยืนของเจ้าของ
วาล์ว          เห็นว่าคำเคาะขัดคำสั่งยืน ยกถามเจ้าของได้เสมอ แต่ต้องยกพร้อมเหตุผล
ผลกับห้องนี้     Q-WS-262 (หยุด สายเว็บอยู่กับ KWEB@air) เป็นคำเคาะที่ยังยืน ⇒ ห้องนี้ยังหยุด
               อำนาจที่เพิ่ม ไม่ได้แปลว่าห้องนี้เปิดคิวเว็บใหม่เอง
               Q-LAW-045 อยู่ในพื้นผิวที่สงวนไว้ที่เจ้าของพอดี ⇒ การมอบอำนาจไม่ปลดใบนั้น
```

## 🔴 ด่านสแกนตู้ของห้องนี้ตาบอดทั้งตู้ และตัวที่จับได้คือ hook ไม่ใช่ด่าน

```
อาการ    resume เมื่อ 15:0xZ รายงานว่า "ไม่มีใบใหม่หลัง 25 ส.ค." ขณะที่ตู้ GLOBAL มีใบ open 8 ใบ
         ใน 8 ใบนั้นมี priority high 2 ใบ และมีมติมอบอำนาจของเจ้าของรวมอยู่ด้วย
เหตุ     ทั้ง 2 ขาของด่านผูก pathspec machines/komphet-mac/queue/*/*.md
         ตู้ queue/GLOBAL/ เปิดใช้ 26 ส.ค. (Q-WS-269) และมันอยู่ที่ **root ของรีโป** ไม่ใช่ใต้ machines/
         ⇒ ไม่ใช่ pattern ผิด ไม่ใช่ control ล้ม แต่เป็นที่ที่ด่านไม่เคยมองเลย ⇒ ไม่มีสัญญาณผิดปกติให้เห็น
แก้      เพิ่มขาที่ 3 + control Q-KWS-024 · หลังแก้ได้ open 42 ใบ เป็นของตู้ GLOBAL ครบ 8 ใบ · commit 258a644
```

🔑 ***control ทุกตัวของด่านนี้เฝ้า "คำตอบผิด" ⛔ ไม่มีตัวไหนเฝ้า "คำถามที่ไม่ได้ถาม" — ตู้ใหม่จึงเงียบสนิท***
⇒ กติกาที่เพิ่ม: **ทุกครั้งที่ WS ประกาศตู้ใหม่หรือย้ายที่เก็บใบ ให้ถือว่า pathspec ของด่านนี้ผิดจนกว่าจะวัดใหม่**

## ขั้น 6 ของ WORKSPACE-RESUME (Q-KWS-022) เดินจบแล้ว

```
ทำได้เอง → ทำแล้ว   ตั้งชื่อ session (Q-WS-270) · ขาที่ 3 ของด่าน (Q-WS-269) ·
                    control ขา SHOP ที่ตายแล้วย้ายไปผูก Q-WS-262 · ack ใบ GLOBAL 5 ใบ
ทวงได้ → ไม่มี       ใบที่ SHOP ออก 28 ใบ ไม่มีใบไหน open · answered 5 ใบ อ่านคำตอบครบแล้ว
ค้างที่คน → รอ       Q-LAW-045 รอเจ้าของกับนักกฎหมายจริง (WS วางเข้าโต๊ะการ์ดที่ 463cdb87)
```

## ของที่เจอระหว่างทาง ยังไม่ได้ทำอะไร

- `machines/komphet-mac/queue/WS/Q-SHOP-006` มีคำว่า `ไม่SHOP` ในเนื้อ ซึ่งเดิมคือป้าย `⛔` ติดกับ `SHOP`
  ⇒ ร่องรอยการกวาดอักขระเชิงกลที่ `Q-WS-256` ห้ามไว้ · ใบเก่าปิดแล้ว ไม่แก้ย้อน บันทึกไว้เป็นตัวอย่าง
- `docs/plans/LEAD.md` ยังหมดอายุ (holder `KWEB@komphet-air` · expires 2026-08-23T07:55+07:00) ปล่อยตามเดิม

---

# รหัสแหล่งที่มาของปุ่ม WhatsApp · `e081668` live 2026-08-27T16:10:44Z · ปิดใบ `Q-MKT-060`

เจ้าของสั่งในห้องนี้ว่า `KMKT` ปิดอยู่ ทุกห้องที่เกี่ยวข้องบนเครื่องนี้รับงานต่อได้ ⇒ ห้องนี้รับใบทั้งใบ รวมส่วนที่เป็นของทะเบียน `KMKT`

## ตัวอักษรใหม่ 4 ตัว และกติกาที่ใช้เลือก

```
O  ปุ่มจองหัวหน้าแรก        hOme
K  ปุ่มข้อความสำเร็จรูป /book  booK
Q  ปุ่มถามสั้นในเมนูมือถือ    Quick question
T  แถบปิดท้าย CTA          cTa · ขึ้น 5 หน้า home services story tour contact
```

กติกา: **ตัวแรกของชื่อพื้นผิวที่ยังว่าง** ⇒ ปุ่มใหม่ในอนาคตเลือกเองได้โดยไม่ต้องเปิดประชุม
ทุกตัวอยู่ใน [`lib/whatsapp.ts`](../../lib/whatsapp.ts) แก้บรรทัดเดียวต่อตัว

## 🔴 ใบสั่งประเมินรูไว้ 1 ปุ่ม ของจริง 6 ปุ่ม และด่านของใบสั่งเองมองไม่เห็น

ด่านที่ใบให้มาคือ `grep` หา call site ของ `buildWhatsAppLink` ⇒ **ปุ่มที่ไม่เรียกฟังก์ชันนั้นเลย ไม่มีวันโผล่**
และปุ่มที่ไม่มีรหัส คือปุ่มที่ไม่เรียกฟังก์ชันนั้นพอดี — 6 จุดลิงก์ตรงไปที่ `SITE.whatsappLink`

🔑 ***ด่านที่นับจาก "จุดที่เรียกฟังก์ชัน" ตาบอดต่อของที่หายไปทั้งการเรียก — มันวัดคุณภาพของสิ่งที่มี ไม่ใช่ความครบของสิ่งที่ควรมี***

แก้ไป 1 จุด (`components/CTA.tsx` ปุ่มเขียนว่า book ตรงตัวและขึ้น 5 หน้า) · ปล่อยไว้ 5 จุดพร้อมคอมเมนต์ในโค้ดว่าจงใจ
เพราะทั้ง 5 มีไว้ให้ลูกค้าพิมพ์เอง การยัดข้อความเพื่อให้แบกรหัส เปลี่ยนสิ่งที่ลูกค้าเห็น ⇒ เป็นเรื่องถ้อยคำของ `MKT` กับเจ้าของ

📌 และลิงก์ `wa.me/qr/…` ที่ 5 ปุ่มนั้นใช้ **เป็นลิงก์ย่อตัวเดียวกับ QR ที่พิมพ์หน้าร้าน** ⇒ คนกดจากเว็บกับคนสแกนหน้าร้าน แยกไม่ออกที่ปลายทาง
แยกได้ต้องขอลิงก์ที่สองจากฝั่ง WhatsApp Business ⛔ ไม่ใช่เพิ่มตัวอักษร · แจ้ง `OFFICE` แล้วใน `Q-SHOP-029`

## วิธี verify ที่ใช้ และเหตุผลที่ไม่ใช้ท่าที่ง่ายกว่า

กดปุ่มจริงผ่านฟอร์ม lead แล้วอ่าน URL ที่เบราว์เซอร์ **ไปถึงจริง** ทั้ง 4 ปุ่ม ได้ `[web-O]` `[web-K]` `[web-Q]` `[web-T]`
ไม่อ่านจาก `href` อย่างเดียว เพราะบทเรียน `b720c50` เดือนนี้: ตัวแทนที่วางไว้สังเกตการณ์ แทนที่สิ่งเดียวที่พังพอดี
📌 `[web-Q]` **ไม่โผล่ใน HTML แบบ static** เพราะลิงก์ประกอบตอนรัน ⇒ `curl | grep` ไม่เห็นมัน และนั่นไม่ได้แปลว่ามันหาย

## กับดัก deploy ที่เพิ่งเจอ อย่าเจอซ้ำ

`gh run list` เผยว่า push ของ handoff เมื่อ 2026-08-26T15:08Z ได้ **`startup_failure` ใน 4 วินาที**
ห้องนี้รายงานว่า "push แล้ว" ในเทิร์นนั้นโดยไม่ได้ดู run เลย · push ถัดมา (18:15Z) เขียวและกลบร่องรอยไป
⇒ **push สำเร็จ ไม่ใช่ deploy สำเร็จ** · เนื้อหาเป็นแค่เอกสารจึงไม่มีใครเดือดร้อน แต่ถ้าเป็น commit ที่แก้ของจริง เว็บจะค้างของเก่าเงียบ ๆ
▶️ ต่อไปนี้: push แล้วต้องดู run จนจบ (`gh run watch <id> --exit-status`) ก่อนบอกว่าขึ้นเว็บแล้ว

---

# ปุ่ม WhatsApp มีรหัสครบทุกปุ่มแล้ว · `8e4b056` + `5bf020e` live 2026-08-27T16:4xZ

```
เส้นแบ่งเวลาที่ OFFICE ใช้  commit 8e4b056 · UTC 16:43:31Z · ลอนดอน 17:43:31 BST
ทะเบียนตัวอักษรครบชุด        lib/whatsapp.ts เป็นแหล่งเดียว แก้บรรทัดเดียวต่อตัว
O hero · K /book · Q ถามสั้น · T แถบปิดท้าย · U ถามโปร · E ถาม signature
D /book แชทเปิด · I หัว /contact · R ใต้ภาพ QR บน /contact
```

`R` คือตัวที่แยก **กดปุ่มบนการ์ด QR บนเว็บ** ออกจาก **สแกนป้ายหน้าร้าน** ได้เป็นครั้งแรก — ก่อนหน้านี้กองรวมกันสนิท
📌 รูที่เหลือและไม่มีตัวอักษรไหนปิดได้: ลิงก์ `wa.me/qr/…` ที่ยังใช้อยู่ที่อื่น เป็นลิงก์เดียวกับ QR ที่พิมพ์ ⇒ ต้องขอลิงก์ที่สองจาก WhatsApp Business

## `buildOpenChatLink` และเหตุผลที่มันมีบรรทัดว่างท้ายข้อความ

3 ปุ่มที่เคยเปิดแชทเปล่า ตอนนี้มีข้อความตั้งต้น ⇒ **มันเลิกเป็นแชทเปล่าโดยนิยาม**
`MKT` เห็นต้นทุนนี้ก่อนของขึ้น: ถ้าไม่มีบรรทัดว่าง ลูกค้าจะพิมพ์ต่อท้ายวงเล็บ ⇒ รหัสไปนั่งกลางประโยคของเขาเอง

🔑 ***ตัวเดียวกันที่อ่านเรียบร้อยเมื่อปิดท้ายประโยคที่เขียนเสร็จ จะอ่านเหมือนสิ่งแปลกปลอมเมื่อคั่นระหว่างคำทักทายกับสิ่งที่ลูกค้าอยากพูด***

⇒ `buildOpenChatLink` ปิดท้ายด้วย `\n\n` · ปุ่มข้อความสำเร็จรูปเดิม 17 จุดไม่ถูกแตะ เพราะไม่มีใครพิมพ์ต่อจากพวกมัน
วัดที่ปลายทางจริงแล้ว บรรทัดว่างรอดผ่าน `api.whatsapp.com` ทั้ง `[web-D]` และ `[web-R]`

## 🔴 deploy รอบพิเศษเพราะอ่านใบครั้งเดียวแล้วลงมือยาว

`MKT` แก้ถ้อยคำ 3 ปุ่มหลังจากที่ห้องนี้อ่านใบไปแล้ว ⇒ push แรกขึ้นด้วยถ้อยคำที่ตกรุ่นไปแล้ว แก้ตามใน 3 นาที
ไม่มีความเสียหายถึงลูกค้า และตัวอักษรไม่เปลี่ยน ⇒ สมุดของ `OFFICE` ยังใช้เส้นแบ่งเวลาเดียว

🔑 ***ใบคิวไม่ใช่เอกสารนิ่ง มันเป็นสถานะที่ห้องอื่นแก้ได้ตลอดเวลาที่เรากำลังทำงานตามมัน***
▶️ **กติกาใหม่ของห้องนี้: งานที่อ้างอิงใบ ต้อง `git fetch` แล้วอ่านใบซ้ำก่อน push ทุกครั้ง**

## เรื่อง GA ที่ยังไม่จบ และไม่ใช่ของห้องนี้ปิด

```
custom dimension  MKT วัดคอนโซล 0 of 0 ⇒ link_url กับ page_path ที่เว็บส่งออกไป ดึงรายงานไม่ได้
                  GA4 ไม่ย้อนหลัง ⇒ ลงทะเบียนวันไหน เห็นตั้งแต่วันนั้น · เป็นบัญชี Google ของเจ้าของ
whatsapp_click    ไม่มีโค้ดตัวไหนยิงชื่อนี้แล้วตั้งแต่ 4a3bb48 (2026-08-22T19:21Z)
                  ในรีโปไม่มี GTM container เลย มีแต่ gtag.js ของ G-R8SGQ58R5E (Analytics.tsx:81)
                  ⚠️ ช่วงวัดที่ห้องนี้แนะนำไปตอนแรก (7 วันล่าสุด) **ปนวันก่อนเปลี่ยนชื่อ** ⇒ ตัดสินไม่ได้
                  ช่วงที่ตัดสินได้คือ Aug 23 00:00Z เป็นต้นมา · Q-MKT-066 ถือเรื่องนี้อยู่
```

## บรรทัดสถานะ · SHOP สามกอง 0 / 2 / 0 · วัด 2026-08-27T16:55:14Z

```
ทำได้เลย       0  เทิร์นนี้ปิด 7 ใบ ตอบ 1 ใบ · ใบที่ SHOP ออกไม่เหลือใบเปิดเลย
รอห้องอื่น/รอคน  2  Q-LAW-045 (เจ้าของ+นักกฎหมายจริง) · Q-KWEB-001 (KWEB ปิด)
ยังไม่ได้เปิด    0  เปิดอ่านครบ รวมตู้ GLOBAL 8 ใบ
```

3 เรื่องที่รอ ทั้งหมดเป็นการตัดสินของเจ้าของ สรุปไว้ใน `Q-SHOP-030` ถึง `WS`
📌 2 ใน 3 มีนาฬิกาเดิน — GA4 ไม่ย้อนหลัง ⇒ ทุกวันที่ยังไม่ลงทะเบียน custom dimension คือวันที่นับไม่ได้ตลอดกาล
และ key event ที่ชี้ผิดชื่อ ทำให้ conversion ไหลลงตั้งแต่ 22 ส.ค. **โดยอ่านเหมือนโฆษณาแย่ลง ทั้งที่เป็นการนับ**

---

# WORKSPACE-RESUME 2026-09-08T14:0xZ — ห้องเปิดใหม่บน mac · ยืน standby รอ MKT

## คำสั่งเจ้าของในเทิร์นนี้ verbatim

> "ห้องนี้เป็นห้อง Shop Wesite นะครับ / WORKSPACE-RESUME / เสร็จแล้วให้ Standby รอห้อง MKT นะครับเผื่อเขาอยากแก้อะไร / ถ้าเขาไม่สั่งให้แก้ไม่ต้องทำอะไรเพิ่มนะครับ"

⇒ **ขั้น 6 ของ WORKSPACE-RESUME (เคลียร์งานค้างที่ทำได้จนจบ) ถูกจำกัดด้วยคำสั่งนี้** — ห้องนี้ทำเฉพาะของที่เป็น
สุขอนามัยของตัว resume เอง (ด่านที่ตาบอด · ล็อกที่ค้าง · ทะเบียนที่ขัดกัน) ⛔ ไม่หยิบแถวงานเว็บใด ๆ จนกว่า MKT จะสั่ง

## วัดแล้ว ไม่ใช่เดา · 2026-09-08T14:06:56Z

```
รีโปเว็บ          clean · ตรง origin/main · HEAD 6e2f130 · ไม่มี next-env.d.ts หรือ tsconfig.json หลุด
production       apex 200 164202 ไบต์ · /book/ 200 48751 ไบต์ · /images/services/massage.jpg 200 105842 ไบต์
                 หน้า /services/ อ้างรูปเดียวกับที่อยู่ใน lib/catalog.ts ของ HEAD (นับได้ 1)
Actions          3 run ล่าสุด success ทั้งหมด 1 run ต่อ 1 push · ล่าสุด 33096079797 เมื่อ 2026-08-27T16:59:30Z
พอร์ต 3300 3301   ว่างทั้งคู่
MEMORY.md        ตรวจครบทุกข้อ ยังจริง — cookie_domain "none" ที่ components/Analytics.tsx:75 ·
                 images unoptimized ที่ next.config.mjs:15 · คำว่า 2009 อยู่ใน 6 ไฟล์ตามรายการเป๊ะ ·
                 ตาราง /cookies ครบ 5 แถว (ttd-consent _ga _ga_ _gcl_aw _gcl_ls) · เบอร์ 07564292247 ที่ lib/site.ts:24
ใบถึง SHOP        open 0 ใบ · ตู้ machines/komphet-mac/queue/SHOP/ ทุกใบอยู่ชั้น archive แล้ว
ตู้ GLOBAL        open 2 ใบ คือ Q-PWS-001 กับ Q-PWS-003 ทั้งคู่เป็นเรื่องเครื่อง pel-mac ⛔ ไม่ใช่งานเว็บ
```

## 3 อย่างที่เอกสารเดิมบอกไว้ผิดกับของจริง และแก้แล้วในเทิร์นนี้

**① ด่านสแกนตู้ตาบอดทั้งชั้น — control ล้มครบ 3 ขาพร้อมกัน และล้มโดยชอบทั้ง 3**
`Q-WS-343` ย้ายใบที่จบลงชั้น `archive/` และตั้งชั้น `standing` ให้มติถาวร · `Q-WS-362` ไล่จัดชั้นใบเก่าเมื่อ 2026-09-04
⇒ pathspec `queue/*/*.md` ไม่ลงชั้นลูก และเกณฑ์ `^status: open` มองไม่เห็นชั้น `standing` **ที่มีอยู่ 68 ใบ**
แก้บล็อก `## Verify` แล้ว: สแกนด้วยโฟลเดอร์เปล่า · พิมพ์ทุกใบที่ยังไม่ `done`/`closed`/`superseded` · control ย้ายไปยืนบนรายชื่อไฟล์
🔑 ***ด่านที่ผูกกับรูปร่างของตู้ จะตาบอดเงียบทุกครั้งที่คนจัดตู้เปลี่ยนวิธีจัด และไม่มีใครต้องทำอะไรผิดเลยสักคน***

**② ล็อก `docs/plans/LEAD.md` ค้างชื่อ `KWEB@komphet-air` ที่หมดอายุมา 16 วัน และไม่เคยมี session**
`_CLOSED-ROUTE.md` ของตู้ SHOP เขียนเองว่าห้อง `KWEB` "ไม่เคยมี session" ⇒ ล็อกใบนั้นไม่เคยมีห้องที่มีชีวิตถือ
หยิบมาถือในนาม `SHOP@komphet-mac` แล้วตามกติกาในไฟล์เอง (หมดอายุ = ปล่อย)

**③ ทะเบียนของ workspace 2 ไฟล์ตอบคนละอย่างเรื่องห้องนี้ — ออกใบ `Q-SHOP-031` ถึง `WS` แล้ว**
`machines/komphet-mac/queue/SHOP/_CLOSED-ROUTE.md` บอกว่าห้องปิด สายเว็บร้านให้จ่าหน้า `MKT` ·
`machines/komphet-mac/queue/rooms-status.tsv` ไม่มีแถว `SHOP` ซึ่งหัวไฟล์เองประกาศว่าแปลว่า open โดยปริยาย
เหตุ: `Q-WS-359` คืนสายผลิต Taitam-D กลับ mac โดยเอ่ยชื่อ 4 ห้อง `ACAD VID EDU MKT` ⇒ **`SHOP` ตกสำรวจ**
📌 ใบนี้ไม่บล็อกงาน — เจ้าของสั่งเปิดห้องด้วยปากในห้องนี้เอง ใบมีไว้ให้ห้องอื่นมองเห็นเรา

## สถานะ standby

```
ทำได้เลย       0   ไม่มีงานเว็บค้าง และคำสั่งเจ้าของห้ามหยิบเพิ่มจนกว่า MKT จะสั่ง
รอห้องอื่น/รอคน  2   MKT (คำสั่งงานเว็บ ถ้ามี) · WS (Q-SHOP-031 แถวทะเบียน)
ยังไม่ได้เปิด    0   เปิดอ่านครบ รวมตู้ GLOBAL
```

📌 **MKT รันอยู่จริง** วัดสด `list_sessions` 2026-09-08T14:05:28Z — session `[MKT] Taitam-D Marketing Lead`
⇒ ช่องทางรับงานเปิดอยู่ทั้งสองทาง คือใบคิวในตู้ `SHOP/` และข้อความตรงจากห้อง `MKT`

## ✅ ปิดแล้วในเทิร์นเดียวกัน — `Q-SHOP-031` · `WS` แก้ทะเบียนครบ 3 ข้อ คอมมิต `bfcece81`

```
_CLOSED-ROUTE.md ของตู้ SHOP    หัวไฟล์เป็น "เปิดกลับแล้ว" · สายเว็บร้านจ่าหน้า SHOP ตรงได้ตามเดิม
rooms-status.tsv บรรทัดที่ 19    แถว SHOP open พร้อมเวลาวัด 2026-09-08T14:1xZ
ใบปิดโดยผู้ออก                    คอมมิต 6b5dbfbd · origin/main e7e74214 · SHOP วัดซ้ำเองหลัง pull
```

🔴 **ข้อเท็จจริงที่ `WS` ชี้กลับมา และห้องนี้ไม่รู้มาก่อน — มันเปลี่ยนขอบเขตความเสียหายของบั๊กด่าน**
ห้องอื่นบน mac สแกนตู้ด้วย `~/dev/gumon-workspace/bin/queue-open-to` ซึ่ง **ไม่ได้ใช้ pathspec รูปที่ตาบอด**
⇒ ด่านที่ตาบอดเพราะ `Q-WS-343`/`Q-WS-362` เป็นของ **ห้องนี้ห้องเดียว** ⛔ ไม่ใช่ของทั้งเครื่อง

🔑 ***ด่านที่ห้องเขียนเองด้วยมือ จะไม่ได้รับการแก้ตอนกติกากลางเปลี่ยน เพราะไม่มีใครรู้ว่ามันมีอยู่***
▶️ **แถวถัดไปของห้องนี้เมื่อพ้น standby**: ย้ายบล็อกสแกนใน `## Verify` ไปเรียก `bin/queue-open-to`
แทนคำสั่ง `git grep` 3 ขาที่เขียนเอง แล้วเหลือ control ไว้เป็นด่านของ **สคริปต์กลาง** ไม่ใช่ของคำสั่งเรา
📌 **ยังไม่ลงมือในเทิร์นนี้** เพราะเจ้าของสั่ง standby ไว้ว่าถ้า MKT ไม่สั่ง ไม่ต้องทำอะไรเพิ่ม

## 📌 `Q-WS-422` (ลายเซ็น C2PA) ไม่ใช่งานของห้องนี้ — วัดแล้ว 2026-09-08T14:3xZ

hook ตู้ GLOBAL ฟ้องใบ `priority: high` ⇒ เปิดอ่านเต็มแล้ว **จ่าหน้า `EDU ACAD VID IMP` ⛔ ไม่มี `SHOP`**
เนื้อคือถอดกลไกลงลายเซ็นดิจิทัลของภาพและวิดีโอ (C2PA) ออกจากรีโปแพลตฟอร์มกับรีโปเนื้อหา ตามคำสั่งเจ้าของ

```bash
grep -rliE "c2pa|content.credential|provenance" app components lib scripts public .github   # ได้ 0 ไฟล์
```

🔴 **กับดักชื่อพ้องที่ต้องเขียนไว้ก่อนมีคนพลาด** — รีโปนี้มี `app/signature/page.tsx` ซึ่งเป็น
**หน้า Landing ของบริการ Taitam-D Signature Thai Massage** ⛔ ไม่เกี่ยวกับลายเซ็นดิจิทัลแม้แต่น้อย
ใบนั้นเตือนเรื่องนี้ไว้เองด้วยประโยค *"ถอดสิ่งที่เจ้าของสั่งถอด ไม่ใช่ทุกอย่างที่มีคำว่าลายเซ็น...อยู่ในชื่อ"*
🔑 ***คำสั่งที่เดินทางด้วยคำ จะกวาดของที่ชื่อพ้องติดไปด้วยเสมอ ถ้าไม่มีใครวัดพื้นผิวจริงก่อน***

## 📮 `MKT` ตอบแล้ว 2026-09-08 — วันนี้ไม่มีงานแก้เว็บ

```
Search campaign ใหม่   ชี้ /services/#cat-massage #cat-facials #cat-nails #cat-waxing #cat-lashes #cat-sauna
                     MKT วัดเองแล้วว่า anchor ทั้ง 6 มีอยู่บนหน้าจริง ⇒ ไม่ต้องแก้อะไร
GA 2 เรื่องที่ค้าง      MKT รับเป็นของตัวเอง แต่ทำวันนี้ไม่ได้ — บัญชีในเบราว์เซอร์ของเขา
                     ไม่มีสิทธิ์บน GA4 property 549034894 (ขึ้น Missing permissions)
                     ⇒ ต้องเจ้าของหรือบัญชี info@ เป็นคนทำ · เป็นเรื่องสิทธิ์ ⛔ ไม่ใช่เรื่องโค้ด
Google Ads           ไม่ติด · conversion action whatsapp_booking_click เป็น primary Active ตัวเดียวอยู่แล้ว
```

⇒ **ห้องนี้ standby ต่อ · สามกอง 0 / 1 / 0** (รอเรื่องเดียวคือคำสั่งจาก `MKT` ถ้ามี)

---

# รอบปลุก standby 2026-09-08T17:5xZ — ด่านของห้องนี้มองไม่เห็นใบที่จ่าหน้าถึงตัวเอง 3 ใบ

## เกิดอะไร

รอบปลุก 15:53Z ห้องนี้รายงานว่า "ไม่มีใบใหม่ถึง SHOP" ด้วยคำสั่ง `git grep` ที่ห้องเขียนเอง
รอบ 17:5xZ **hook ของตู้ GLOBAL ฟ้องรายชื่อใบ `open` ที่ห้องนี้ยังไม่ ack มา 21 ใบ** ⇒ ไปวัดด้วยสคริปต์กลาง

```
bin/queue-open-to SHOP    จ่าหน้าตรง 3 ใบ · ประกาศทุกห้อง 14 ใบ   (วัด 2026-09-08T17:5xZ)
คำสั่งที่ห้องเขียนเอง         เห็นใบที่จ่าหน้าถึง SHOP ในตู้ GLOBAL  0 ใบ
3 ใบที่หายไป                Q-WS-413 (บัญชี mac ย้ายเสร็จ) · Q-WS-416 (มอบอำนาจ WS) · Q-WS-419 (แคช python)
                          ทั้งสามใบ priority: high
```

**เหตุ**: ขา GLOBAL ของคำสั่งเดิมจับ `to:.*\bGLOBAL\b` เท่านั้น ⇒ ใบในตู้ GLOBAL ที่จ่าหน้าเป็นรายชื่อห้อง
`to: [ACAD, EDU, ..., SHOP, ...]` **ไม่มีคำว่า GLOBAL อยู่ในบรรทัดนั้นเลย** จึงหลุดทั้งหมด
📌 นี่คนละบั๊กกับรอบเช้า (archive/ กับ standing) — **บั๊กที่สามในด่านเดียวกันภายในวันเดียว**

## ทำอะไรไปแล้ว

```
เขียน ack 3 ใบ    queue/GLOBAL/ack/Q-WS-413|416|419/SHOP@komphet-mac.md   คอมมิต a219282a
ย้ายด่าน          บล็อก ## Verify เรียก bin/queue-open-to SHOP แทนคำสั่งที่เขียนเอง
control          เลิกใช้ของที่ห้องประดิษฐ์เอง ใช้ bin/queue-open-to --selftest ของสคริปต์กลาง (ผ่านครบทุกขา)
Q-WS-419         วัดพื้นผิวรีโปนี้แล้ว ⛔ ไม่โดน — python 2 ไฟล์รันเป็น __main__ ⛔ ไม่ถูก import · แคชของรีโปนี้ 0 ไฟล์
Q-WS-416         ไล่กระดานแล้ว แถวที่เขียนว่ารอเจ้าของ 0 แถว
```

🔑 ***ด่านที่ห้องเขียนเอง จะแม่นเท่ากับความเข้าใจของห้อง ณ วันที่เขียน แล้วมันหยุดเรียนรู้ทันทีในวันนั้น***
ส่วนสคริปต์กลางถูกแก้โดยห้องที่เจ็บ — `Q-EDU-216` แก้บั๊กที่ทำให้ 21 ห้องเสียใบไปเงียบ ๆ และห้องนี้ **ไม่เคยรู้เรื่องนั้นเลย**
⇒ **ราคาของการเขียนด่านเอง ไม่ใช่แค่บั๊กของตัวเอง แต่คือทุกบั๊กที่คนอื่นแก้ไปแล้วและเราไม่ได้รับ**

📌 ห้องนี้ยัง standby ตามคำสั่งเจ้าของ · ทั้งหมดข้างบนเป็นงานตู้คิวกับด่านของตัวเอง ⛔ ไม่ได้แตะโค้ดเว็บแม้แต่บรรทัดเดียว

---

# Q-MKT-077 ข้อ 1 2 4 ขึ้น production · `0f6bdcc` · run 34343180950 · 2026-09-09T11:0xZ

standby จบเมื่อ MKT สั่งงาน ตามเงื่อนไขที่เจ้าของวางไว้เอง

```
home title  Thai Massage & Beauty Spa in King's Cross, London | Taitam-D   (metadata ใหม่ใน app/page.tsx)
home H1     Thai massage and beauty in King's Cross                        (components/Hero.tsx)
svc  H1     Massage, Nails, Waxing, Facials & Lashes in King's Cross       (app/services/page.tsx)
schema      เพิ่ม hasMap อ่านจาก SITE.social.googleMaps                     (components/StructuredData.tsx)
Rich Results Test  2 valid items · ไม่มี error ไม่มี warning · Local businesses 1 · Organization 1
```

## 🔑 `title.absolute` ไม่ใช่รายละเอียด มันคือเหตุผลที่หน้าอื่นไม่พัง

layout วาง `title.template` เป็น `%s — Taitam-D Beauty & Spa` ⇒ ถ้าใส่ title ธรรมดาในหน้าแรก
จะได้แบรนด์ต่อท้ายซ้ำสองครั้ง เพราะถ้อยคำที่ MKT ขอมีแบรนด์อยู่ท้ายอยู่แล้ว
และถ้าไปแก้ `title.default` ของ layout แทน จะกระทบ**ทุกหน้าที่ไม่มี title ของตัวเอง** ไม่ใช่หน้าแรกหน้าเดียว

## 🔴 ใบวัดผิด 1 ข้อ และรูปของความผิดนี้จะกลับมาอีก

ใบเขียนว่าหน้าแรก **ไม่มี** `HealthAndBeautyBusiness` · **วัดแล้วมีมาตลอด** ในรูปอาร์เรย์
`"@type": ["BeautySalon", "HealthAndBeautyBusiness"]` ⇒ การค้นด้วยสตริง `"@type":"HealthAndBeautyBusiness"` ไม่มีวันเจอ

🔑 ***ค่าที่ schema.org ยอมให้เป็นได้ทั้งสตริงและอาร์เรย์ จะหายไปจากการค้นแบบสตริงเสมอ
และผลลัพธ์ที่ได้คือคำว่า ไม่พบ ซึ่งอ่านเหมือนคำว่า ไม่มี***

▶️ ท่าที่ใช้วัด ld+json ในรีโปนี้ต่อจากนี้ **แยกก้อนออกมา `json.loads` ก่อน แล้วค่อยอ่านช่อง** ห้าม grep สตริงดิบ

## 🛑 ข้อที่ห้องนี้ปฏิเสธ และเหตุผลที่ปฏิเสธคือการทำถูก

ใบสั่งให้ตัด `Established in 2009` ออกจาก meta description · **ไม่ทำ** — ชนกฎเหล็กข้อ 4 ของ [`CLAUDE.md`](../../CLAUDE.md)
ซึ่งบันทึกมติเจ้าของไว้ว่าไม่ต้องแก้ถ้อยคำปีก่อตั้ง · `MKT` ไม่ใช่เจ้าของ และห้องนี้ก็ไม่ใช่

📌 ถ้าวันหนึ่งเรื่องนี้ถูกเคาะ **ต้องแก้ทั้ง 6 ไฟล์พร้อมกันรอบเดียว** รวม `foundingDate` ที่ไหลเข้า JSON-LD
ถอดครึ่งเดียวจะได้หน้าเว็บที่ประกาศสองอย่างพร้อมกัน ซึ่งแย่กว่าประกาศปีเดียวที่ยังไม่ได้ข้อยุติ

## `geo` ที่ใบขอ แต่ไม่ใส่

ไม่มีพิกัดที่วัดแล้วในรีโปนี้เลย และพิกัดเป็นคำกล่าวอ้างเกี่ยวกับจุดบนพื้นโลก
🔑 ***ค่าที่พิมพ์จากความจำ หน้าตาเหมือนค่าที่รังวัดมาทุกประการสำหรับทุกคนที่อ่านหลังจากเรา***
Rich Results Test ผ่านโดยไม่มี `geo` ⇒ ต้นทุนของการไม่ใส่คือศูนย์ ส่วนต้นทุนของการเดาคือคำแถลงเท็จที่ตรวจไม่ได้

## กับดักของ visual gate ที่เพิ่งเจอ อย่ารายงานผิดซ้ำ

ท่าบังคับ `opacity:1; transform:none` เพื่อให้ `Reveal` โผล่ในภาพ **ทำให้ React ฟ้อง hydration mismatch ใน console เอง**
วัดยืนยันแล้ว เปิดแท็บใหม่ที่ไม่ได้ฉีดสไตล์ ได้ `No console logs`
⇒ **เจอ hydration error ระหว่าง visual gate ให้สงสัยเครื่องมือของตัวเองก่อน** แล้วยืนยันด้วยแท็บสะอาด

## ที่ยังไม่ทำ

ข้อ 3 หน้า `/massage-kings-cross/` ตามลำดับที่ใบกำหนดเอง (PR ถัดไป) รอ `MKT` บอกเริ่ม
ตัวอักษรรหัสแหล่งที่มาของปุ่ม WhatsApp บนหน้านั้น ห้องนี้เสนอ `M` ไปแล้วในใบตอบ

---

# หน้า `/massage-kings-cross/` ขึ้นแล้ว · `9737fd7` · run 34344347806 · 2026-09-09T11:1xZ

ปิด `Q-MKT-077` ครบทั้ง 4 ข้อ · **เว็บมี 8 หน้าแล้ว ไม่ใช่ 7**

```
URL       https://taitam-d.com/massage-kings-cross/  200 · 166418 ไบต์
ราคา      82 ตัว อ่านจาก lib/catalog.ts หมวด massage · ⛔ ไม่มีตัวไหนพิมพ์ซ้ำมือ
ลิงก์เข้า   footer ทุกหน้า 1 จุด · /services/ หมวด massage 1 จุด
sitemap   10 URL แล้ว (ลิสต์มือ ลืมแล้ว build ⛔ ไม่เตือน — บทเรียน W-5 ยังใช้ได้ทุกครั้งที่เพิ่มหน้า)
```

## 🔴 รหัสแหล่งที่มา `J` และเหตุที่ `M` ใช้ไม่ได้ ทั้งที่ทั้งสองห้องตกลงกันแล้ว

ห้องนี้เสนอ `M` เอง `MKT` ตอบตกลง · ตอนลงมือเปิด [`lib/whatsapp.ts`](../../lib/whatsapp.ts) แล้วพบว่า **`M` ถูกจองเป็นรหัส _หมวด_ massage มาตั้งแต่ `Q-KMKT-004`**
⇒ ปุ่มราคาทุกปุ่มใน `/services/#cat-massage` ส่ง `M` อยู่แล้ว · ใช้ซ้ำ = หน้าใหม่ถูกนับรวมกับกองที่มันถูกสร้างมาเพื่อเทียบพอดี

🔑 ***ตัวอักษรที่ชนกัน ⛔ ไม่ทำให้อะไรพัง มันทำให้สองสิ่งกลายเป็นสิ่งเดียว โดยที่ทุกด่านยังเขียว
มีข้อความเข้า อ่านตัวอักษรออก ไม่มี error ใด ๆ*** — ความเสียหายอยู่ในรายงาน ⛔ ไม่ได้อยู่ในระบบ

📌 ตัวที่ว่างเหลือ `J V Y Z` เท่านั้น (ใช้ไปแล้ว 22 ตัว) ⇒ **หน้าใหม่ครั้งหน้าต้องเปิดไฟล์ก่อนเสนอตัวอักษร ห้ามเดา**
▶️ ถ้าวันหนึ่งตัวอักษรหมดจริง ทางออกไม่ใช่ใช้ซ้ำ แต่คือเปลี่ยนรูปรหัสทั้งระบบ ซึ่งเป็นการตัดสินใจของ `KMKT` ⛔ ไม่ใช่ของห้องนี้

## การ์ดราคามีตัวเรนเดอร์เดียวแล้ว

ย้ายออกจาก `ServiceMenu` ไปเป็น [`components/ServicePriceGrid.tsx`](../../components/ServicePriceGrid.tsx) · `source` เป็นพารามิเตอร์ ⛔ ไม่ใช่ค่าที่มาจาก slug
เพราะหน้า landing มีเหตุผลในการมีอยู่คือถูกนับแยกจาก `/services/` ⇒ แถวเดียวกัน ราคาเดียวกัน คนละตัวอักษรตอนออก
📌 แถวการแพทย์ใน `ServiceMenu` **จงใจไม่ใช้ตัวนี้** — มันขอ consultation ⛔ ไม่ใช่ booking (`Q-LAW-046`)
ถ้ายัดความต่างนั้นไว้หลัง flag จะกลายเป็นการซ่อนสิ่งเดียวที่ทำให้มันต่าง

## 2 ข้อในใบที่ห้องนี้ไม่ทำ และเหตุผลอยู่ในโค้ดแล้ว

**เวลาเดินจากสถานี** — เว็บมี 2 ค่าที่ไม่ตรงกันอยู่ก่อนแล้ว `Hero.tsx:127` เขียน `5 min` · `LocationBlock.tsx:38` เขียน `~6 min walk`
🔑 ***หน้าที่เขียนมาแข่งคำค้นของย่านหนึ่ง เป็นหน้าที่แย่ที่สุดสำหรับการใส่ตัวเลขที่ยังไม่มีข้อยุติ
เพราะตัวเลขที่ชนะจะเป็นตัวที่คนเขียนหน้านั้นพิมพ์ ⛔ ไม่ใช่ตัวที่มีคนวัด***
⇒ เขียน `a short walk` ไว้ก่อน พร้อมคอมเมนต์ในไฟล์ · รอ `MKT` เคาะแล้วแก้ทีเดียว 3 จุด

**ราคาเริ่มต้น** — หัวไฟล์ `lib/catalog.ts` ห้าม render ราคาเป็น `from £X` ⇒ วางรายการราคาเต็มแทนการเขียนราคาเริ่ม

## ปีก่อตั้งบนหน้าใหม่ วัดให้ชัดเพราะใบห้ามไว้

```
เนื้อหาที่มองเห็นบนหน้า + meta description ของหน้านี้   2009 ปรากฏ 0 ครั้ง
HTML ทั้งไฟล์                                      12 ครั้ง ทุกครั้งมาจากชั้น layout ที่ทุกหน้าใช้ร่วมกัน
                                                 คือ SITE.description และ foundingDate ใน JSON-LD
```
⇒ **ข้อห้ามระดับหน้า ทำได้ครบ · ข้อห้ามระดับเว็บ ทำไม่ได้จนกว่า D-71 จะปิด** และนั่นคือกฎเหล็กข้อ 4 ⛔ ไม่ใช่การละเลย

## ✅ เวลาเดินจากสถานี เคาะแล้ว 5 นาที · `e40466b` live 2026-09-09T11:2xZ

`MKT` วัดเส้นทางจริงใน Google Maps ได้ **4 นาที 0.2 ไมล์ ทั้ง 2 เส้นทางที่แนะนำ** แล้วเคาะให้ปัดขึ้นเป็น 5

```
ก่อน   Hero.tsx "5 min"  ·  LocationBlock.tsx "~6 min walk"     สองค่า live พร้อมกันมา 1 เดือน
หลัง   lib/site.ts ช่อง walkMinutes: 5 ที่เดียว · 3 จุดประกอบถ้อยคำของตัวเองรอบค่านั้น
วัดสด  หน้าแรก "5 min walk from King's Cross / St Pancras" + การ์ด "5 min"
      /massage-kings-cross/ "a 5-minute walk" 2 จุด (เนื้อหา + meta description) · คำว่า 6 min เหลือ 0 ทั้งเว็บ
```

🔑 ***การแก้ค่าที่ไม่ตรงกัน โดยไม่แก้เหตุที่ทำให้มันแยกกันได้ คือการรอให้มันแยกอีกครั้ง***
⇒ ที่ทำเกินคำสั่งคือย้ายตัวเลขมาไว้ที่เดียว ไม่ใช่แค่พิมพ์ 5 ลงไปทั้ง 3 ไฟล์
📌 รูปเดียวกับ `SITE.academy` ที่ค่าเดียวคุม 9 จุด และเป็นเหตุผลที่ `CLAUDE.md` วางให้เนื้อหารวมศูนย์ที่ `lib/site.ts` ตั้งแต่แรก

## ปีก่อตั้ง D-71 ปิดแล้วจริง และกฎข้อ 4 ของรีโปนี้เคยเขียนผิดมา 1 เดือน

`Q-WS-454` (`WS` เคาะ 2026-09-09) — ครูนกยืนยัน `2009` ตั้งแต่ 2026-08-09 คอมมิต `8dda4fd`
หลักที่ปิดเรื่องคือ **ปีก่อตั้ง ⛔ ไม่ใช่ปีจดทะเบียนบริษัท** · โปสเตอร์ `SINCE 2013` คือปีเปิดร้าน ⛔ ไม่ขัดกัน

⚠️ คอมมิตนั้นแก้ไฟล์นี้กับกระดานมติ **แต่ไม่ได้แก้ [`CLAUDE.md`](../../CLAUDE.md)** ซึ่งยังเขียนว่า
"ห้ามแตะจนกว่าเจ้าของจะเคาะ" ⇒ วันที่ 2026-09-09 `MKT` อ่านแล้วสั่งถอดถ้อยคำ · ห้องนี้อ่านแล้วปฏิเสธถูกแต่**อ้างเหตุผลที่ตกรุ่น**
และรายงานเจ้าของว่าเรื่องยังรอเขาอยู่ ทั้งที่เขาตอบไปแล้วเดือนก่อน

🔑 ***กฎที่เขียนว่ารอคำตอบ หลังคำตอบมาถึงแล้ว ⛔ ไม่ได้อ่านเหมือนกฎที่ล้าสมัย มันอ่านเหมือนงานที่ยังไม่เสร็จ
และทุกคนที่อ่านจะพยายามทำให้มันเสร็จ*** — วันเดียวมีคนพยายาม 2 ห้อง
⇒ แก้แล้ว `23649a2` · **บทเรียนที่ใช้ต่อ: คอมมิตที่ปิดคำถาม ต้องกวาดทุกไฟล์ที่ประกาศว่าคำถามยังเปิด ⛔ ไม่ใช่แค่ไฟล์ที่บันทึกคำตอบ**

## บรรทัดสถานะ · `Q-MKT-077` ปิดครบฝั่ง SHOP · วัด 2026-09-09T11:2xZ

```
ทำได้เลย       0  ทั้ง 4 ข้อของใบ + เวลาเดิน ขึ้น production ครบ
รอห้องอื่น/รอคน  2  geo (MKT รอสิทธิ์ GBP) · ส่ง URL เข้า Search Console (MKT รอสิทธิ์)
ยังไม่ได้เปิด    0
```

---

# ฟอร์ม lead ถูกถอดออกทั้งชิ้น · `80c46c3` live 2026-09-09T12:08Z · `Q-MKT-078` ข้อ 1 กับ 2

คำสั่งเจ้าของผ่าน `MKT` — ลูกค้าทักน้อยลงหลังใส่ฟอร์ม ⇒ เอาออก · **ทาง WhatsApp กลับมาเป็น 1 แตะ**

```
ก่อน   แตะปุ่ม → กล่อง "Before we open WhatsApp" → พิมพ์ชื่อ + เบอร์ (บังคับ) → แตะครั้งที่ 2
หลัง   แตะปุ่ม → WhatsApp เปิดทันที พร้อมข้อความที่เติมบริการ เวลา ราคาไว้แล้ว
ผลกับปุ่ม  265 ปุ่มทั้งเว็บ · รหัสแหล่งที่มาและข้อความสำเร็จรูป ⛔ ไม่ถูกแตะเลยสักตัว
```

## 🔴 จุดที่เกือบพังเงียบตอนถอด และเป็นบทเรียนที่ใหญ่กว่าตัวฟอร์ม

`whatsapp_booking_click` ซึ่งเป็น conversion ที่ Google Ads นำเข้า **ถูกยิงจากในกล่องนั้น ⛔ ไม่ได้ยิงจากตัวลิงก์**
`components/Analytics.tsx` มีบรรทัดที่ **จงใจข้ามลิงก์จอง** พร้อมคอมเมนต์อธิบายว่าทำไม เพราะกล่องรับหน้าที่รายงานแทน

⇒ ถอดกล่องอย่างเดียวจะได้ **เว็บที่ทำงานถูกต้องทุกจุด โดยที่ Ads เห็นการทักเป็นศูนย์**
🔑 ***ความพังที่ทำให้ทุกอย่างบนหน้าจอดูปกติ คือความพังที่ไม่มีใครสังเกตเห็นเป็นสัปดาห์***
📌 สิ่งที่กันไว้ได้คือ**คอมเมนต์ที่คนเขียนตอนนั้นเขียนไว้ว่าทำไมถึงข้าม** ⛔ ไม่ใช่ความจำของใคร
▶️ **กติกาที่ใช้ต่อ: ถอดชิ้นไหนออก ให้ `grep` ก่อนว่ามีอะไร**เท่านั้น**ที่ยิง event หรือถือหน้าที่นั้นอยู่**

## ต้นทุนที่หายไปพร้อมฟอร์ม บันทึกไว้ให้ครบ ⛔ ไม่ใช่ผลข้างเคียงที่ลืมบอก

```
whatsapp_lead_submit · whatsapp_lead_skip   เลิกยิงตั้งแต่ 2026-09-09T12:08Z
ชื่อ + เบอร์แบบแฮชที่ส่งให้ Google              ⛔ ไม่ส่งอีก ⇒ Ads จับคู่การทักกับโฆษณาที่พามาไม่ได้
```
กล่องนั้นถูกสร้างมาเพื่อสัญญาณตัวนี้โดยเฉพาะ (`Q-KMKT-004`) ⇒ ถอดกล่อง = ถอดสัญญาณ · เรื่องนี้เป็นของ `MKT`

## 🔬 ช่วงเวลาที่ OFFICE ต้องกันออกจากการเทียบก่อน-หลัง

```
dfc6a36  2026-08-23T03:49:06+07  ฟอร์มเข้ามา เฉพาะปุ่มหมวด massage
75be5d1  2026-08-23T03:59:42+07  ครอบทุกปุ่มจอง
b720c50  2026-08-23T23:12:27+07  แก้บั๊กที่ทำให้ปุ่มในกล่อง ⛔ ไม่พาไปไหนเลย
```
🔴 **19 ชั่วโมงระหว่าง 03:49 ถึง 23:12 คนกรอกครบแล้วไปไม่ถึง WhatsApp** ⇒ ตัวเลขช่วงนั้นมีสองสาเหตุที่แยกไม่ออก
🔑 ***ข้อมูลที่มีสองสาเหตุแยกไม่ออก ⛔ ไม่ใช่ข้อมูลที่อ่อน มันคือข้อมูลที่ตอบคำถามนั้นไม่ได้เลย***
⇒ ช่วงที่เทียบได้จริงคือตั้งแต่ **2026-08-24 00:00 +07** เทียบกับก่อน **2026-08-23 03:49**

## ข้อ 3 Treatwell ค้างที่ค่าเดียว

ออกใบ `Q-SHOP-032` ถึง `OFFICE` ขอ URL หน้าจองสาธารณะ (venue 287555) · รีโปนี้ ⛔ ไม่มี URL นั้นอยู่เลย
🔑 ***ปุ่มสำรองที่ลิงก์ผิด แย่กว่าไม่มีปุ่มสำรอง — คนที่กดมันคือคนที่เพิ่งบอกเราว่าเขาไม่มีทางติดต่ออีกทางแล้ว***
⇒ ⛔ ไม่เดา URL จากเลข venue · ได้ค่ามาเมื่อไร ขึ้น 3 หน้าในเทิร์นเดียว (`/book/` · `/services/` · `/massage-kings-cross/`)

---

# วันเดียวที่เว็บโตจาก 7 หน้าเป็น 15 URL · 2026-09-09 · `Q-MKT-077` `Q-MKT-078` `Q-MKT-079`

`MKT` ออกใบ 3 ใบในวันเดียว ทั้งหมดมาจากคำสั่งเจ้าของว่า **"อยาก Boot SEO"** และ **"ให้ทัก WhatsApp ได้ใน 1 แตะ"**

```
Q-MKT-077  title + H1 หน้าแรก · schema hasMap + geo · H1 /services/ · หน้า /massage-kings-cross/   ปิดฝั่ง SHOP
Q-MKT-078  ถอดฟอร์มก่อนเข้า WhatsApp · ปุ่มสำรอง Treatwell 3 หน้า · วิดเจ็ตฝัง /book/            ปิดฝั่ง SHOP
Q-MKT-079  H2 หน้าแรก 5 จาก 9 · landing 5 หน้า · FAQPage หน้าแรก + massage landing              ปิดฝั่ง SHOP
```

## หน้าใหม่ 6 หน้า ทุกหน้ารูปเดียวกัน

```
/massage-kings-cross/              J    686 คำ
/deep-tissue-massage-kings-cross/  MD  1,004 คำ
/facial-kings-cross/               MF  1,006 คำ
/waxing-kings-cross/               MW  1,076 คำ
/nails-kings-cross/                MN  1,201 คำ
/lash-extensions-kings-cross/      ML    989 คำ
```
โครงร่วม ย่อหน้าเปิดบอกทำเลกับเวลาเปิด · เนื้อหาเลือกบริการ · ราคาจาก `lib/catalog.ts` **เท่านั้น** ·
สิ่งที่เจอในนัดแรก · FAQ 7 ข้อ + `FAQPage` · ปุ่ม WhatsApp 1 แตะ · ปุ่มรอง Treatwell · ลิงก์เข้าจาก footer และหมวดของตัวเองใน `/services/`

🔑 **ราคาไม่เคยถูกพิมพ์ลงหน้าไหนเลย** ทุกหน้าเป็นตัวกรองบนแคตตาล็อกเดียวกัน ผ่าน [`ServicePriceGrid`](../../components/ServicePriceGrid.tsx)
⇒ แก้ราคาที่แคตตาล็อกที่เดียว แล้ว 6 หน้ากับ `/services/` ขยับพร้อมกัน

## 🔴 รหัสแหล่งที่มาชนเพดาน 26 ตัว และแก้เป็น 2 ตัวอักษรแล้ว

```
ก่อนหน้านี้  ตัวอักษรเดี่ยว ใช้ไป 22 จาก 26 · เหลือว่าง J V Y Z
ใบขอ       landing 5 หน้า ตัวละหน้า ⇒ ขาด 2 ตัว
เคาะโดย MKT  ใช้ 2 ตัวอักษร MD MF MW MN ML · รหัสเดิม 22 ตัว ⛔ ไม่ขยับ
ทะเบียน     ตารางในคอมเมนต์ที่ lib/whatsapp.ts (ตัวอักษร → หน้า → วันที่เริ่ม) · MKT ถือทะเบียนแทน KMKT ที่มืดแล้ว
sourceFromLink  regex เป็น [A-Z]{1,2} แล้ว
```
🔑 ***ตัวอักษรที่ชนกัน ⛔ ไม่ทำให้อะไรพัง มันทำให้สองสิ่งกลายเป็นสิ่งเดียวโดยที่ทุกด่านยังเขียว***
▶️ **หน้าใหม่ครั้งหน้า เปิด `lib/whatsapp.ts` ก่อนเสนอตัวอักษร ห้ามเดา**

## ฟอร์ม lead ถูกถอด และสิ่งที่เกือบหายไปพร้อมกัน

`components/LeadCapture.tsx` ถูกลบทั้งไฟล์ · จาก 2 แตะ + 2 ช่องบังคับ เหลือ 1 แตะ ครบ 265 ปุ่ม
🔴 `whatsapp_booking_click` ถูกยิงจาก**ในกล่องนั้น** ⛔ ไม่ได้ยิงจากตัวลิงก์ ⇒ ถอดเฉย ๆ = Ads เห็นการทักเป็นศูนย์โดยเว็บยังดูปกติ
สิ่งที่กันไว้ได้คือ**คอมเมนต์ที่คนเขียนตอนนั้นเขียนว่าทำไมตัวดักคลิกถึงข้ามลิงก์จอง** ⛔ ไม่ใช่ความจำของใคร

## 🔴 วิดเจ็ต Treatwell กลับมาแล้ว โดยเจ้าของทับคำวินิจฉัยของ LAW

`components/TreatwellWidget.tsx` บน `/book/` · **โหลดเมื่อกดเท่านั้น** เพราะวิดเจ็ตหย่อนคุกกี้ของ Treatwell 2 ตัวทันทีที่โหลด
และแบนเนอร์ของเราสัญญาว่าถ้าปฏิเสธจะไม่มีอะไรถูกเก็บ

```
สิ่งที่วัดได้     วิดเจ็ตแสดงเมนู Treatwell ทั้งหมด ⛔ ไม่มีพารามิเตอร์กรอง
                หน้าที่ดึงมามีคำว่า Botox 10 ครั้ง · botulinum 6 · Anti-Wrinkle 5
คำวินิจฉัยเดิม   Q-LAW-046 ห้ามกลุ่มนี้ขึ้นเว็บเด็ดขาด เหตุเป็นยาที่ต้องสั่งโดยแพทย์
คำสั่งเจ้าของ     เห็นผลวัดแล้ว 2 ครั้ง และสั่งให้ขึ้นอยู่ดี 2026-09-09
```
📌 **`Q-LAW-046` ⛔ ไม่ได้ถูกยกเลิก** มันถูกทับเฉพาะพื้นผิวนี้ โดยเจ้าของ ในวันนั้น · บันทึกอยู่หัวไฟล์คอมโพเนนต์
⇒ ทางที่ปิดช่องว่างนี้โดยไม่ต้องแก้โค้ดเลย คือ `OFFICE` ซ่อนรายการโบทูลินัมท็อกซินใน Treatwell Connect

## FAQ ของร้านอยู่ในไฟล์เดียว

[`components/ShopFaq.tsx`](../../components/ShopFaq.tsx) ถือ 8 คำตอบที่ `OFFICE` ยืนยัน (นับจาก 51 การสนทนา 20 ส.ค. ถึง 8 ก.ย.)
ใช้บนหน้าแรกกับ `/massage-kings-cross/` · หน้า landing เขียนรายการของตัวเองเพราะคนละหัวข้อ
⏳ **2 ข้อที่ยังหายไป** จ่ายบัตร · บัตรของขวัญ — `OFFICE` ยืนยันไม่ได้ ⇒ **ห้ามเดา** ได้คำตอบเมื่อไร เติมไฟล์เดียวแล้วขึ้นทุกหน้าพร้อมกัน

## กับดักที่เจอซ้ำในวันเดียวกัน ทั้งที่เอกสารนี้เตือนไว้เอง

```
ยิง curl โดยลืมสแลชท้าย ⇒ ได้ 301 แล้วนับเนื้อหาเป็น 0
```
บล็อก `## Verify` เตือนข้อนี้ไว้ตั้งแต่ 2026-08-27 และห้องนี้ยังเหยียบซ้ำ ⇒ ⛔ ไม่ใช่เพราะไม่รู้ แต่เพราะพิมพ์เร็วในคำสั่งที่ประกอบ URL จากตัวแปร
▶️ **กติกาที่ใช้ได้จริงกว่าการเตือน: อ่าน `http_code` คู่กับผลเสมอ เห็น 301 เมื่อไร อย่าอ่านตัวเลขที่ตามมา**

## บรรทัดสถานะ · วัด 2026-09-09T23:5xZ

```
ทำได้เลย       0   ทั้ง 3 ใบของ MKT ปิดครบฝั่ง SHOP · รอ MKT พลิกสถานะ
รอห้องอื่น/รอคน  3   2 คำถาม FAQ (คนที่ร้าน) · geo/Search Console (MKT รอสิทธิ์) · เมนู Treatwell (OFFICE + เจ้าของ)
ยังไม่ได้เปิด    0
```
