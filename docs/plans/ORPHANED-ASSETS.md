# ไฟล์ใน `public/` ที่ไม่มีหน้าไหนอ้างถึง — แต่เข้าถึงได้สาธารณะ

> สำรวจ **2026-08-10** ตามคำขอของห้อง `WS` (สืบเนื่องจาก `Q-SHOP-003` เรื่องรูปครูนก)
>
> ## ✅ อัปเดต 2026-08-10 — **ลบ 3 ใบที่มีข้อมูลผิดแล้ว · อีก 28 ไฟล์ยัง hold**
>
> **ผู้อนุมัติ: komphet โดยตรงในห้อง `SHOP`** — *"หากไม่ได้ใช้ ลบได้เลยครับ"* ⇒ ยืนยันแล้วว่าไม่มีหน้าไหนอ้างถึง
> (`WS` อนุมัติมาก่อนหน้าทางข้อความด้วย **แต่ยังไม่ได้ commit ลงใบคิว** ⇒ ตามกติกากลาง
> *"ห้ามใช้คำบอกเล่าเป็นหลักฐานการอนุมัติ"* ผมจึงไม่ลงมือด้วยข้อความนั้น **แต่ komphet อยู่ในห้องและสั่งเอง
> ซึ่ง charter ระบุว่า first-hand แรงกว่า commit** ⇒ เดินด้วยคำสั่งนั้น)
>
> 🔴 **สิ่งที่การลบนี้ *ไม่* ได้แก้ — อ่านก่อนคิดว่าเรื่องจบ:** ปัญหาอยู่ที่ **ตัวอาร์ตเวิร์ก** ไม่ใช่ที่ไฟล์บนเซิร์ฟเวอร์
> ชื่อไฟล์คือ `WhatsApp Image …` ⇒ **มันมาจากแชท** ⇒ **ถ้า `MKT` ยังส่งโปสเตอร์ใบเดิมให้ลูกค้าทางแชทอยู่
> ลูกค้าก็ยังได้เบอร์ที่ยกเลิกและโดเมนที่กำลังจะกลายเป็นสแปมอยู่ดี** — การลบสำเนาของเราไม่ได้หยุดสิ่งนั้น
> ⇒ ข้อกำหนดสำหรับทำใบใหม่อยู่ท้ายเอกสารนี้
>
> ## ✅ อัปเดตรอบสอง — ลบเพิ่มอีก **2 ไฟล์** ตามคำสั่ง Legal ([D-W22](DECISIONS-BOARD.md))
>
> `promo-spa-ritual-gemini-v2.jpg` (มี `HERITAGE SPA` + โลโก้ทองอ่านออกได้บนขวด) และ `promo-spa-ritual-gemini.jpg`
> **รวมลบทั้งหมด 5 ไฟล์** · เก็บ `-v3` (ใช้อยู่ · ไม่มีตัวอักษร) และ `promo-hair-spa-gemini-v2.jpg` (ใช้อยู่)
>
> ## 🔴 แก้ตัวสแกน — **ผมมีข้อบกพร่องเดียวกับที่ `WS` เตือน แค่มาทางไฟล์อื่น**
>
> `WS` เตือนว่าตัวสแกนอาจนับ `docs/ORPHANED-ASSETS.md` เป็นการอ้างถึง ⇒ **ตรวจแล้ว `docs/` ไม่เคยอยู่ในขอบเขตสแกนของผม**
> **แต่ `README.md` อยู่** — และหัวข้อ *"Image credits"* ของมัน **ลิสต์ `stock/*.jpg` ทั้ง 5 ไฟล์ไว้เพื่อให้เครดิต Unsplash**
> ⇒ ตัวสแกนอ่านว่า "มีคนใช้" ⇒ **ซ่อนไฟล์กำพร้าไป 5 ไฟล์ (~15 MB)**
>
> 🔑 **กลไกที่ `WS` อธิบายถูกต้องทุกประการ — แค่ไฟล์ที่ก่อเหตุคนละใบ**: *เอกสารที่ **พูดถึง** ไฟล์
> ทำให้ไฟล์นั้นดูเหมือน **ถูกใช้** ในสายตาเครื่องมือ* · และรอบนี้มันคือ **หน้าให้เครดิตลิขสิทธิ์** —
> เอกสารที่มีอยู่เพื่อบอกว่า *"ภาพพวกนี้มาจากไหน"* กลับกลายเป็นหลักฐานว่า *"ภาพพวกนี้ถูกใช้อยู่"*
>
> ✅ **ตัวสแกนฉบับใช้จริงตอนนี้: ขอบเขต = `app/` `components/` `lib/` + `next.config.mjs` `package.json` เท่านั้น**
> **ไม่รวมเอกสารทุกชนิด** (`docs/` · `README.md` · `CLAUDE.md`) เพราะเอกสารบรรยายไฟล์ ไม่ได้เรียกใช้ไฟล์
>
> ✅ **และมี self-test บังคับ**: ก่อนเชื่อผลทุกครั้ง ให้ตัวสแกนตอบไฟล์ที่ **รู้อยู่แล้วว่าถูกใช้**
> (`promo-hair-spa-gemini-v2.jpg` · `promo-spa-ritual-gemini-v3.jpg` · `founder-860x1075.jpg` · `logo.png`)
> **ถ้ามันตอบว่ากำพร้าแม้แต่ใบเดียว = ผลทั้งชุดเชื่อไม่ได้** ⇒ รอบล่าสุด **ตอบถูกทั้ง 4 ใบพร้อมชี้ไฟล์ที่อ้างถึง**
>
> ส่วนที่เหลือด้านล่างเป็นผลสำรวจเดิม **เก็บไว้ตามเดิมเพราะเป็นบันทึกว่าเจออะไร** ตัวเลข 31/60 คือ ณ วันสำรวจ

## ทำไมเรื่องนี้ถึงสำคัญกว่าที่ชื่อหัวข้อทำให้รู้สึก

`WS` สรุปกลไกไว้คมมากตอนสั่งงาน และมันตรงกับสิ่งที่พบ:

> 🔑 **"ไฟล์ที่ไม่มีหน้าอ้างถึง จะไม่มีใครนึกถึงตอนตัดสินใจ — มันจะรอดทุกการทบทวน เพราะไม่มีใครเห็นมัน"**

`next.config.mjs` ตั้ง `output: "export"` ⇒ **ทุกไฟล์ใน `public/` ถูก deploy ขึ้น GitHub Pages เสมอ
ไม่ว่าจะมีหน้าไหนอ้างถึงหรือไม่** · การไม่มีลิงก์ **ไม่ได้แปลว่าเข้าไม่ถึง** แปลว่า **ไม่มีใครเห็นตอนตรวจ**

## ภาพรวม

| | จำนวน |
|---|---|
| ไฟล์ทั้งหมดใน `public/` | **60** |
| **ไม่ถูกอ้างถึงในซอร์สเลย** (`app/` · `components/` · `lib/` · config) | **31** (~7 MB) |

⚠️ **"ไม่ถูกอ้างถึง" ≠ "ต้องลบ"** — บางไฟล์ถูกเรียกด้วย URL ตรงตามธรรมเนียม
(เช่น `llms.txt`) หรืออาจถูกใช้เป็นลิงก์ที่ส่งให้ลูกค้าทางแชท **ซึ่งซอร์สมองไม่เห็น**
⇒ นี่คือเหตุผลที่ต้องให้เจ้าของเคาะ ไม่ใช่ลบตามผล grep

## 🔴 4 ไฟล์ที่ต้องอ่านก่อน — เปิดดูด้วยตาแล้ว มีเนื้อหาที่ขัดกับมติที่ปิดไปแล้ว

ทั้งสี่ไฟล์ **ยืนยันแล้วว่าตอบ 200 บน production**

### 1 · `images/campaigns/WhatsApp Image 2569-08-03 at 21.32.16.jpeg` (285 KB) — โปสเตอร์ร้านเต็มใบ

ในภาพมี **สามอย่างที่ขัดกับสิ่งที่เราเพิ่งตัดสินไป**:

| ในภาพ | ชนกับอะไร |
|---|---|
| **`020 7278 0906` — Call / WhatsApp** | 🔴 **เบอร์ที่ยกเลิกไปแล้ว** ([D-W21](DECISIONS-BOARD.md)) · เรากำลังบอก `MKT` ให้ไล่ลบเบอร์นี้ทุกช่องทาง **แต่มันอยู่บนโดเมนของเราเอง** |
| **`SINCE 2013`** + *"Serving King's Cross community since 2013"* | เว็บและ structured data ระบุ **2009** ([D-W1](DECISIONS-BOARD.md) ครูนกยืนยันเอง) |
| **`OVER 10 YEARS OF TRUST`** · **`THOUSANDS OF HAPPY CLIENTS`** | คำกล่าวอ้างที่ไม่มีทะเบียนรองรับ — คลาสเดียวกับ `over 1,000 students` ที่ `WS` เพิ่งสั่งให้ตัดทิ้ง |

🔑 **ข้อ `SINCE 2013` — นี่ไม่ใช่การรื้อเรื่องปีก่อตั้ง** `D-W1` ปิดแล้วว่า **2009 ถูก ห้ามเปิดใหม่** และผมไม่ได้เปิด
สิ่งที่รายงานคือ **ข้อเท็จจริงใหม่คนละเรื่อง**: `HANDOFF` เคยบันทึกว่า *"โปสเตอร์หน้าร้าน SINCE 2013 ยังไม่มีคำอธิบาย
**แต่ไม่กระทบเว็บ**"* — ⚠️ **ประโยคหลังไม่จริงอีกต่อไป** เพราะโปสเตอร์ใบนั้น **อยู่บนโดเมนเราและเปิดได้สาธารณะ**

### 2 · `images/campaigns/WhatsApp Image 2569-08-04 at 00.35.43 (1).jpeg` (283 KB) — เมนู Hair Spa & Detox

- **ราคาเต็มรายการ**: Hair Spa £40/£59/£69/£79 · Face Spa £30 · Hand Spa £20 · Foot Spa £30 · **ส่วนลด £10**
- 🔴 **ท้ายภาพเขียน `www.taitamd-Beautyacademy.com`** — **โดเมนเก่าที่เจ้าของสั่งยกเลิกทั้งหมด**
  ⇒ เว็บเราลบลิงก์ไปโดเมนนั้นหมดแล้วใน `W-14` **แต่ยังโฆษณามันอยู่ในรูปภาพ**
  ⚠️ `HANDOFF §3` บันทึกไว้ว่าโดเมนพี่น้องของมัน (`taitamdbeautyacademylondon.com`) **หลุดมือไปแล้วจริง
  และตอนนี้เสิร์ฟเนื้อหาสแปม** ⇒ ความเสี่ยงนี้ไม่ใช่ทฤษฎี

### 3 · `images/campaigns/WhatsApp Image 2569-08-04 at 00.35.43.jpeg` (266 KB) — โปสเตอร์ Hair Spa

**ราคาชุดเดียวกัน + ส่วนลด £10** · ไม่มีเบอร์ ไม่มีโดเมนเก่า

### 4 · `images/story/founder-860x1260.jpg` (104 KB) — **รูปครูนก** (ไฟล์ที่ `WS` ชี้มา)

รูปบุคคลที่**ไม่มีหน้าไหนเป็นเจ้าของ** · รายละเอียดครบใน `Q-SHOP-003` — metadata ถูกลบหมด กู้ที่มาไม่ได้

## 🔑 ประเด็นที่ใหญ่กว่ารายไฟล์: **ราคาและโปรโมชันที่ไม่มีใครเป็นเจ้าของ**

ไฟล์ 2 และ 3 ประกาศ **ราคาและส่วนลด £10** อยู่บนโดเมนของร้าน **โดยไม่มีหน้าไหนแสดงมัน**
⇒ ไม่มีใครในรอบทบทวนราคาจะนึกถึงมัน เพราะ **มันไม่โผล่ในเมนูหรือหน้าไหนเลย**
⇒ ถ้าราคาปรับ ไฟล์พวกนี้จะกลายเป็น **ใบเสนอราคาเก่าที่ยังเปิดได้** และเราจะไม่รู้ตัว

## รายการเต็ม 31 ไฟล์ (จัดกลุ่ม)

| กลุ่ม | จำนวน | หมายเหตุ |
|---|---|---|
| `images/treatwell/treatwell-*.jpg` | 14 | ภาพจากลิสต์ Treatwell · **น่าจะเคยใช้แล้วเลิก** — ตรวจก่อนว่าไม่ได้ถูกส่งเป็นลิงก์ให้ใคร |
| `images/campaigns/*` (รวม `-gemini`) | 6 | **มี 3 ใบที่มีปัญหาข้างบน** · ที่เหลือเป็นภาพโปรโมตที่สร้างด้วย AI |
| `images/hero*` · `hero/hero-16x9-1600x900.jpg` | 3 | ของเวอร์ชันก่อน |
| `images/contact/storefront-3x2-*` (jpg + webp) | 2 | เวอร์ชัน 3x2 ไม่ได้ใช้ (ใช้ 21x9) |
| `images/academy/academy-2` · `academy-3` | 2 | ใช้แค่ `academy-1` |
| `images/gallery/lashes-1200x800.jpg` | 1 | — |
| `images/whatsapp/WhatsApp Image … 19.43.01.jpeg` | 1 | สกรีนช็อต QR ตัวซ้ำ (ตัวที่ใช้จริงคือ `whatsapp-qr-only.jpg`) |
| `images/story/founder-860x1260.jpg` | 1 | **รูปบุคคล** |
| `llms.txt` | 1 | ✅ **ปกติ** — เป็นไฟล์ที่ถูกเรียกด้วย URL ตรงตามธรรมเนียม ไม่ต้องมีหน้าอ้าง |

## ข้อเสนอถึงเจ้าของ (ยังไม่ทำ รอเคาะ)

**เร่งกว่าเพื่อน — 2 ใบที่มีข้อมูลผิด/ตายแล้ว:**
1. `…21.32.16.jpeg` (เบอร์ที่ยกเลิก + SINCE 2013 + คำกล่าวอ้างไม่มีหลักฐาน)
2. `…00.35.43 (1).jpeg` (โดเมนเก่าที่กำลังถูกยกเลิก + ราคา)

⇒ เสนอให้ **เอาออกจาก `public/`** · ไฟล์ยังอยู่ในประวัติ git กู้ได้เสมอ ⇒ **ไม่ใช่การทำลายข้อมูล**
⚠️ **ต้องถามก่อนว่ามีใครส่งลิงก์ตรงของไฟล์เหล่านี้ให้ลูกค้าทางแชทหรือโฆษณาไหม** — ถ้ามี ลบแล้วลิงก์พัง

**ไม่เร่ง:** ที่เหลือเป็นเรื่องพื้นที่และความสะอาด ไม่มีข้อมูลผิด ⇒ รวบไว้ทำทีเดียวตอนว่าง

## 🗂 จัดหมวด 30 ไฟล์ที่เหลือ พร้อมข้อเสนอรายกลุ่ม (2026-08-12)

> ทำท่อนนี้โดยไม่ต้องรอเจ้าของ — **ท่อนที่ต้องเจ้าของคือ "กดลบ" ไม่ใช่ "จัดว่าอะไรควรลบ"**
> ตัวเลขทั้งหมดมาจาก `node scripts/check-orphan-assets.mjs --list` + `stat` ณ 2026-08-12

**รวม 30 ไฟล์ · 20.51 MB** — deploy ขึ้นเว็บทุกครั้งโดยไม่มีหน้าไหนอ้างถึง

| กลุ่ม | ไฟล์ | ขนาด | ข้อเสนอ |
|---|---|---|---|
| `stock/` | 5 | **15.51 MB** | 🔴 **ลบ — กลุ่มนี้คุ้มที่สุดกลุ่มเดียว** |
| `treatwell/` | 14 | 2.67 MB | 🟡 ถามก่อน |
| `campaigns/promo-hair-spa-gemini.jpg` | 1 | 0.67 MB | 🟢 ลบ |
| `images/hero-16x9-1920x1080` · `hero-21x9-1920x823` | 2 | 0.58 MB | 🟢 ลบ |
| `academy/academy-2` · `academy-3` | 2 | 0.37 MB | 🟡 ถามก่อน |
| `contact/storefront-3x2` (jpg+webp) | 2 | 0.23 MB | 🟢 ลบ |
| `hero/hero-16x9-1600x900.jpg` | 1 | 0.18 MB | 🟢 ลบ |
| `gallery/lashes-1200x800.jpg` | 1 | 0.15 MB | 🟡 ถามก่อน |
| `story/founder-860x1260.jpg` | 1 | 0.10 MB | 🔴 **รูปบุคคล — ต้องเจ้าของเคาะ** |
| `whatsapp/WhatsApp Image …19.43.01.jpeg` | 1 | 0.05 MB | 🟢 ลบ |

### 🔴 `stock/` — **5 ไฟล์นี้คือ 76% ของปัญหาทั้งหมด และไม่ใช่แค่ "ไม่ได้ใช้"**

```
7.4 MB  5145×7717  white-candle.jpg     ← ไฟล์เดียวใหญ่กว่าทั้งเว็บที่เหลือรวมกัน
4.4 MB  6016×4016  waiting-area.jpg
2.0 MB  6000×4000  spa-room.jpg
1.5 MB  3712×5568  candle-bath.jpg
0.5 MB  3300×2200  candles-table.jpg
```
🔑 **นี่คือไฟล์ต้นฉบับจาก Unsplash ที่ไม่เคยถูกย่อเลย** — ความละเอียดระดับ 5000–6000 px บนเว็บที่
**เสิร์ฟรูปดิบ** และหน้าจอที่ใหญ่ที่สุดที่เราออกแบบไว้คือ 1600 px ⇒ ต่อให้เอากลับมาใช้ **ก็ต้องย่อก่อนอยู่ดี**
⇒ **ลบได้โดยไม่เสียโอกาสอะไรเลย** · เครดิต Unsplash ใน `README.md` ต้องเอาออกพร้อมกัน (ไม่งั้นเครดิตค้างชี้ไฟล์ที่ไม่มี)

### 🟡 `treatwell/` 14 ไฟล์ — **ถามก่อน เพราะมันอาจไม่ใช่ของเรา**

ชื่อไฟล์เป็นเลข ID ของ Treatwell (`treatwell-14714922` …) ⇒ **น่าจะดึงมาจากลิสต์ Treatwell**
❓ **คำถามที่ต้องได้คำตอบก่อนตัดสิน: ภาพชุดนี้เราถ่ายเองหรือมาจาก Treatwell** — ถ้าเป็นของ Treatwell
**การเก็บไว้บนโดเมนเราโดยไม่มีหน้าไหนใช้ เป็นเรื่องสิทธิ์ ไม่ใช่เรื่องพื้นที่**
⚠️ และ `treatwell-14715041.jpg` มีป้ายผนังสะกด `TAI TAM-D` (การสะกดที่ `D-71` เลิกใช้) · `treatwell-14715891.jpg` มีพรมลายธนบัตร $100 ที่ `MKT` ตีตกจาก Google Ads ไปแล้ว

### 🟡 `academy-2` · `academy-3` · `lashes` — **ถามก่อน เพราะอาจตั้งใจเอากลับมาใช้**

ทั้งสามเป็นภาพที่ดูตั้งใจถ่าย/เลือกมา และมีพี่น้องที่ยังใช้อยู่ (`academy-1` ใช้อยู่) ⇒ **น่าจะเป็นตัวเลือกที่ไม่ได้เลือก ไม่ใช่ของเสีย**

### 🔴 `founder-860x1260.jpg` — **รูปบุคคล ต้องเจ้าของเคาะ ห้ามลบเอง**

รายละเอียดอยู่ใน `Q-SHOP-003` — metadata ถูกลบหมด กู้ที่มาไม่ได้ · เป็นครอปที่สองของภาพเดียวกับที่ใช้อยู่

### 🟢 ที่เหลือ (hero เก่า · storefront 3x2 · promo v1 · QR ซ้ำ) — **เวอร์ชันที่ถูกแทนที่แล้ว**

ทุกใบมีตัวที่ใช้งานจริงอยู่แล้วในเว็บ (`hero-16x9-1600x900` ถูกแทนด้วย `taitamd-gemini-hero` · `storefront-21x9` ใช้แทน 3x2 · `promo-hair-spa-gemini-v2` แทน v1 · `whatsapp-qr-only` แทนสกรีนช็อต) ⇒ **ลบได้ ไม่มีอะไรเสีย**

### ▶️ สรุปสำหรับเจ้าของ — ตัดสินสามข้อ ไม่ใช่สามสิบ

```
① ลบกลุ่ม "เวอร์ชันที่ถูกแทนแล้ว" + stock  = 10 ไฟล์ · 17.1 MB   ← ได้ 83% ของพื้นที่คืน
② treatwell 14 ไฟล์ — ตอบคำถามสิทธิ์ก่อน (เราถ่ายเอง หรือของ Treatwell)
③ founder-860x1260 — รูปบุคคล เคาะแยก
```
🔑 **ข้อ ① ไม่ต้องรอข้อ ② และ ③** — สามข้อนี้ไม่ผูกกัน

## 📋 ข้อกำหนดสำหรับโปสเตอร์ใบใหม่ (ถ้า `MKT` จะทำแทนใบที่ลบ)

komphet เสนอให้ generate ภาพใหม่มาแทนถ้ายังใช้อยู่ · **ห้องนี้ไม่มีเครื่องมือสร้างภาพ**
⇒ ใครทำก็ตาม **ต้องผ่านทั้ง 6 ข้อนี้** ไม่งั้นจะได้ปัญหาเดิมในไฟล์ใหม่:

| ต้องเป็น | ห้ามเป็น | อ้างอิง |
|---|---|---|
| `07564292247` (มือถือ) | ⛔ `020 7278 0906` — **ยกเลิกแล้ว** | [D-W21](DECISIONS-BOARD.md) |
| `Since 2009` / ไม่ใส่ปีเลย | ⛔ `SINCE 2013` · `OVER 10 YEARS` | [D-W1](DECISIONS-BOARD.md) |
| ไม่ใส่ตัวเลขที่พิสูจน์ไม่ได้ | ⛔ `THOUSANDS OF HAPPY CLIENTS` | คลาสเดียวกับ `over 1,000 students` ที่ `WS` สั่งตัด |
| `taitam-d.com` | ⛔ `taitamd-Beautyacademy.com` — **กำลังถูกยกเลิก** | `W-14` |
| `Taitam-D` (ยัติภังค์ ASCII · `D` ใหญ่ตัวเดียว) | ⛔ `TaiTam-D` · non-breaking hyphen | `D-71` / `W-10` |
| ราคา **ตรวจกับราคาปัจจุบันก่อนพิมพ์ลงภาพ** | ⛔ ราคาที่ไม่มีใครรับผิดชอบว่าอัปเดตเมื่อไร | บทเรียนของ `W-18` เอง |

🔑 **และข้อสำคัญที่สุดที่ไม่ใช่เรื่องเนื้อหา: ถ้าไฟล์ใหม่จะอยู่ใน `public/` ต้องมีหน้าใดหน้าหนึ่งอ้างถึงมัน**
ไม่งั้นมันจะกลายเป็นไฟล์กำพร้าใบต่อไปที่รอดทุกการทบทวน · ถ้าใช้แค่ส่งในแชท **อย่าเอาเข้ารีโปนี้**

## Verify (รันซ้ำได้)

```bash
# ไฟล์ที่ไม่มีใครอ้างถึง ยังเปิดได้จากภายนอกจริงไหม
curl -s -o /dev/null -w "%{http_code}\n" \
  "https://taitam-d.com/images/campaigns/WhatsApp%20Image%202569-08-03%20at%2021.32.16.jpeg"
# วัดได้ 200 เมื่อ 2026-08-10 — ถ้าได้ 404 แปลว่ามีคนเอาออกแล้ว ให้อัปเดตเอกสารนี้

# นับไฟล์กำพร้าใหม่หลังแก้อะไรก็ตาม — สคริปต์เต็มอยู่ในประวัติของ commit ที่เพิ่มไฟล์นี้
ls public/images/campaigns/
```
