# QUEUE-WEB — คิวงานเว็บไซต์ taitam-d.com

> เจ้าของคิว: **ห้อง Taitam-D Shop Lead** (Lead + Concept · ได้อำนาจเต็มจากเจ้าของ 2026-08-09)
> กติกา: [`../../CLAUDE.md` §วิธีทำงาน](../../CLAUDE.md) → กติกากลางที่ `solution-taitamd-platform/docs/dev/ai-agent-operating-rules.md`
> คิวนี้คือ **source of truth** ของงานเว็บ — ห้องอื่นส่ง request มาที่ห้องนี้ แล้ว Lead เป็นคนลงแถว

**สถานะที่ใช้:** 🟡 queued · 🔄 in-progress · ⏸ hold (ติดคนอื่น/รอเคาะ) · ✅ done
**Claim:** Executor เขียน `owner=EX-n` ในช่อง owner ของแถวตัวเองเป็นงานแรก แล้ว commit+push ทันที · push ชน = มีคน claim ก่อน → หยุดและรายงาน

---

## คิวปัจจุบัน

| # | งาน | สถานะ | owner | โซนไฟล์ | verify gate | หมายเหตุ |
|---|---|---|---|---|---|---|
| W-24 | 🖼 **Gallery พร้อมคำบรรยาย — โจทย์จากลูกค้า 13 ส.ค.** *"อยากให้ดูบรรยากาศของร้าน"* | 🟡 queued — **วางแผนไว้แล้ว ยังไม่เริ่ม** (Kom สั่ง: *"อย่าเพิ่งทำ ฝากโจทย์ไว้ก่อน"*) | — | หน้าใหม่ `app/gallery/` + `app/sitemap.ts` + `public/images/shop/` | build + lint + `check:assets` + **visual gate ทั้ง desktop และ mobile** + `curl` ยืนยันหน้าใหม่ 200 และอยู่ใน sitemap | 📄 **รายละเอียดครบใน [`GALLERY-BRIEF.md`](GALLERY-BRIEF.md)** — อ่านก่อนเริ่ม อย่าออกแบบใหม่จากศูนย์ |
| W-23 | **เกณฑ์ `LAW`: "สองรายการบนหน้าเดียวกันต้องคำนวณเข้ากันได้"** — เช่น คำโปรยสัญญา workbook 4 ชื่อ แต่ฐานมีไฟล์แนบ 0 (`Q-SHOP-006`) | 🟡 queued — **ตั้งใจ *ไม่* ยัดเข้า `W-19`** | — | ยังไม่กำหนด | ต้องพิสูจน์ว่าจับเคสจริงได้ก่อนเชื่อ | 🔑 **เหตุผลที่แยกออกมา ไม่ใช่เพราะขี้เกียจ**: `W-19` ตรวจ **ข้อเท็จจริงเชิงกล** (ไฟล์นี้มีหน้าอ้างถึงไหม — ตอบได้แน่นอน ไม่มีสีเทา) · ข้อนี้ตรวจ **ความสอดคล้องของความหมาย** ซึ่งต้องรู้ว่า "สี่ชื่อในย่อหน้าโฆษณา" ควรถูกนับคู่กับ "จำนวนไฟล์แนบในฐาน" ⇒ **เป็นกฎเฉพาะหน้า ไม่ใช่กฎทั่วไป** · ⚠️ **ถ้ายัดสองอย่างนี้ไว้ในด่านเดียวกัน ด่านที่แม่นจะถูกลากลงมาเชื่อถือไม่ได้เท่าด่านที่เดา** — และคนจะปิดทั้งด่าน · 📌 เคสนี้อยู่ฝั่ง Academy ไม่ใช่เว็บร้าน ⇒ ถ้าจะทำจริง **น่าจะเป็นของ `ACAD`** ห้องนี้ให้แบบตัวสแกน + self-test ไปใช้ได้ |
| W-18 | **ไฟล์กำพร้าใน `public/` เหลือ 19 ไฟล์ · 3.3 MB** (เดิม 30 · 20.5 MB) | ⏸ hold — **เหลือเฉพาะที่ต้องคำตอบจากคน ไม่ใช่การอนุมัติ** | — | `public/` เท่านั้น | `npm run check:assets` + `curl` ยืนยัน 404 และ 7 หน้ายัง 200 | ✅ **ข้อ ① ทำแล้ว 2026-08-12** — ลบ 11 ไฟล์ 17.17 MB (`846c614`) ยืนยัน 404 บน production · `out/images/` เหลือ 9.6 MB · เดินด้วยไฟเขียวยืนพื้น `Q-WS-100` + คำสั่งเดิมของเจ้าของ ⇒ ไม่ต้องรออนุมัติซ้ำ · **เหลือ**: `treatwell/` 14 (❓ **คำถามข้อเท็จจริง ไม่ใช่การอนุมัติ**: เราถ่ายเองหรือของ Treatwell — ถ้าของเขา เป็นเรื่องสิทธิ์ไม่ใช่พื้นที่) · `founder-860x1260` (รูปบุคคล) · `academy-2/3` · `lashes` (อาจตั้งใจเอากลับมาใช้) · `llms.txt` อยู่ใน allowlist แล้ว |

---

## 🔁 กลับมาเดินต่อ (13 ส.ค. 2026 — เปลี่ยนบัญชี Claude กลางงาน)

> เขียนตอนถูกสั่งปิดห้องกะทันหัน · **ห้องใหม่อ่านบล็อกนี้ก่อนแตะอะไร**
> 🔑 *ห้องใหม่จะไม่รู้ว่ามีอะไรหาย — จะเห็นคิวที่ดูปกติ แล้วเริ่มใหม่โดยคิดว่ากำลังเดินต่อ*

**กำลังทำอยู่ตรงไหน** — งานจากบรีฟครูนก 13 ส.ค. · **หน้า `/signature/` เสร็จและขึ้น production แล้ว** (`216ec3a`)
ถ้อยคำครูนกอยู่บนสุดครบ · ภาพ AI ถูกลบทิ้งทั้งหมด · ใช้รูปร้านจริง 5 ใบ · มีบล็อกทีมงาน

**คำสั่งถัดไปตัวจริง — เลือกหนึ่งใน 2 อย่าง**
```
① W-24 Gallery พร้อมคำบรรยาย  ← โจทย์ล่าสุดจากลูกค้า · แผนครบใน GALLERY-BRIEF.md
② เปลี่ยนภาพ AI ที่เหลือบนหน้าแรก + /services/
```
🔑 **ผมแนะนำ ① ก่อน** — Gallery ใช้รูปจริงชุดเดียวกัน ⇒ **ทำ Gallery แล้วจะได้รูปที่พร้อมไปแทนภาพ AI ในข้อ ② ด้วย** · ทำสลับกันจะเสียแรงคัดรูปสองรอบ

**ภาพ AI ที่ยังเหลือ (ข้อ ②)**
```
components/Hero.tsx        → /images/hero/taitamd-gemini-hero.jpg   (ภาพ AI ห้องที่ไม่มีอยู่จริง)
components/Campaigns.tsx   → promo-hair-spa-gemini-v2.jpg  ← 🔴 ภาพห้องสระผม+คน สื่อว่าเป็นร้านเรา
                           → promo-spa-ritual-gemini-v3.jpg (ภาพนิ่ง ไม่ได้สื่อสถานที่ ความเร่งต่ำกว่า)
```

**ทำไปแล้วครึ่งทางตรงไหน** — ใช้รูปจริงแล้ว 5 ใบจาก 24 · **เปิดดูด้วยตาจริงแค่ 8 ใบ · อีก 16 ใบยังไม่เคยเห็น**
⚠️ **หน้า Signature แก้รอบสองแล้ว** (`b233e3e`) — เดิมมีภาพซ้ำ 2 คู่ ตอนนี้ 7 สลอต 7 ภาพไม่ซ้ำ
🔑 **บทเรียนที่ต้องใช้ต่อใน Gallery**: `reception-tropical` กับ `reception-wide` เป็น *คนละไฟล์แต่มุมเดียวกัน* ⇒ **การนับว่า "ไม่ซ้ำไฟล์" ไม่พอ ต้องดูว่าซ้ำ *มุม* ไหม**

**🔴 ของที่อยู่ในหัวแต่ยังไม่มีในไฟล์ — ส่วนนี้สำคัญที่สุด**

```
รูปต้นทางทั้ง 24 ใบ : /Users/komphet.me/Downloads/Taitam-D Shop/
เสียงครูนก 4 คลิป   : /Users/komphet.me/Downloads/WhatsApp Audio 2569-08-12 at 23.04.49.opus
                      /Users/komphet.me/Downloads/WhatsApp Audio 2569-08-13 at 02.49.20.opus
                      /Users/komphet.me/Downloads/WhatsApp Audio 2569-08-13 at 13.06.46.opus
                      /Users/komphet.me/Downloads/WhatsApp Audio 2569-08-13 at 16.26.23.opus
                      ⇒ ถอดความครบแล้วใน OWNER-BRIEF-2026-08-13.md ไม่ต้องถอดซ้ำ
```
⚠️ **ไฟล์พวกนี้อยู่นอกรีโป** — ถ้าเครื่องถูกล้าง ต้องขอครูนกใหม่ทั้งชุด · เธอบอกว่าต้นฉบับอยู่ใน **Google Drive** (ชุดใน Downloads เป็นสำเนาที่โหลดกลับจาก Facebook คาป 2048px)

**สคริปต์ที่ใช้จริง กู้มาไว้ในรีโปแล้ว** (เดิมอยู่ใน scratchpad ที่จะหายไปพร้อม session):
- `scripts/prep-shop-photos.py` — ปรับแสง/ลด noise/ย่อ · มีตารางว่า **ไฟล์ไหน → ชื่ออะไร → ขนาดเท่าไร**
- `scripts/transcribe-voice-notes.py` — ถอดเสียงด้วย Gemini (เครื่องไม่มี whisper ตอนนั้น)

**การจับคู่รูปที่ตัดสินไปแล้ว** (อยู่ใน `prep-shop-photos.py` ด้วย):
```
772204702… → treatment-room-couples  ← 🥇 ห้องนวดคู่ ผ้าไหมลายไทย = อัตลักษณ์ที่ครูนกพูดถึง
773867203… → reception-tropical      (แนวนอน สะอาดสุด)
773218417… → reception-wide
772466111… → salon-lavender
772466122… → team-board              ← บอร์ดทีม + ถ้วยรางวัล
```
⚠️ **แนวนอนมีแค่ 4 ใบใน 24** — ที่เหลือเป็นแนวตั้งจากมือถือ

**รออะไรจากใคร**
```
เจ้าของ · `treatwell/` 14 ใบ เราถ่ายเองหรือของ Treatwell     ⏳ ไม่มีวันหมดอายุ · บล็อก W-18
เจ้าของ · `founder-860x1260.jpg` ลบได้ไหม (รูปบุคคล)        ⏳ ไม่มีวันหมดอายุ · บล็อก W-18
ครูนก   · ต้นฉบับจาก Google Drive (คมกว่าที่มี)              ⏳ ไม่บล็อก — Kom สั่งใช้ของที่มีไปก่อน
```

**✅ มติที่เคาะแล้ว ห้ามถามซ้ำ** (Kom ตอบตรงในห้อง 13 ส.ค. · `WS` รับรองซ้ำ)
```
① รูปร้านจริงแทนภาพ AI    ⇒ เจ้าของแบรนด์ + LAW (D-W22) ชี้ทางเดียวกัน ⇒ ไม่มีอะไรให้เคาะเพิ่ม
② UNESCO                  ⇒ ขึ้นเลย ไม่ต้องรอ LAW
③ บอร์ดทีม                ⇒ ขึ้นทั้งรูปและข้อความ
④ ใช้รูปที่มีไปก่อน        ⇒ ขอต้นฉบับทีหลัง
⑤ `Powered by Taitam-D Academy London`
   🔴 **Kom สั่งให้ขึ้นเลยรวมบรรทัดนี้ และมันขึ้น production ไปแล้ว**
   ⚠️ `WS` เคยเสนอให้เว้นไว้เติมวันที่ 16 — **ข้อเสนอนั้นตกไปแล้ว เจ้าของทับ**
   ⛔ **ห้ามห้องใหม่ไปลบบรรทัดนี้ออกเพราะเห็นกฎ "ห้ามผูกชื่อ Academy ก่อน 16 ส.ค."**
```

**Verify — รันก่อนเชื่อบล็อกนี้**
```bash
curl -s https://taitam-d.com/signature/ | grep -c 'Heritage Massage'     # 2
curl -s https://taitam-d.com/signature/ | grep -c 'images/signature/'    # 0 = ภาพ AI หมดแล้ว
curl -s https://taitam-d.com/signature/ | grep -ci 'from £69'            # 0
ls "/Users/komphet.me/Downloads/Taitam-D Shop" | wc -l                   # 24 = รูปต้นทางยังอยู่
npm run check:assets                                                     # 19 known orphans, 0 new
```
⛔ **ห้ามแตะ 19 ไฟล์กำพร้าที่เหลือ** — `WS` สั่งห้ามเก็บกวาดในรอบเปลี่ยนบัญชี

---

## เสร็จแล้ว

| # | งาน | ปิดเมื่อ | หลักฐาน |
|---|---|---|---|
| W-22 | หน้า `/signature/` ไม่มีภาพ | 2026-08-12 | **ปิดโดยไม่ต้องรอสิ่งที่ใบบอกว่าต้องรอ** — ใบตั้งไว้ว่า *hold รอไฟล์จาก `SIG` + รอถ้อยคำ `A-1`* · ของจริง: **komphet สั่งตรงในห้องให้ generate ภาพเอง** ⇒ สร้าง 6 ใบด้วย Gemini · เปิดดูทุกเฟรมก่อนใช้ · ย่อ/แปลงก่อน commit (787 KB ทั้งชุด · hero 186 KB) · และ **`LAW` ให้ถ้อยคำมาแล้ว ตรงกับที่ผมขึ้นไปก่อนหน้าทุกตัวอักษร** (`Illustrative image — not a photograph of our team or of a client.`) ⇒ ติด 3 ใบที่มีบุคคล · 🔑 **แถวนี้ค้างอยู่ 1 วันทั้งที่งานเสร็จแล้ว** — คำตอบมาถึงในห้อง ไม่ได้มาถึงในคิว ⇒ *`status` บันทึกว่า "ยังไม่มีใครมาปิด" ไม่ได้บันทึกว่า "ยังไม่มีใครทำ"* |
| W-19 | ด่านใน CI ที่จับไฟล์กำพร้าใน `public/` | 2026-08-12 | สร้าง [`scripts/check-orphan-assets.mjs`](../../scripts/check-orphan-assets.mjs) + step ใน [`nextjs.yml`](../../.github/workflows/nextjs.yml) **ก่อน build** (ให้ deploy หยุด ไม่ใช่ขึ้นแล้วค่อยรู้) + `npm run check:assets` · 🔑 **ใช้ baseline ไม่ใช่ล้มทั้งหมด** — มี 30 ไฟล์อยู่ก่อนแล้ว ถ้าให้ล้มหมดคือเลือกระหว่าง *บล็อกทุก deploy* กับ *ไม่ใส่ด่านเลย* ⇒ ล็อกหนี้เดิมไว้ใน `scripts/orphan-assets-baseline.txt` **ล้มเฉพาะของใหม่** ⇒ ด่านมีผลทันทีโดยไม่บล็อกใคร และ**หนี้กลายเป็นตัวเลขที่นับได้** · ⚠️ **baseline ที่ค้างก็ล้มด้วย** — ไฟล์ที่ถูกแก้/ลบแล้วต้องถูกถอดออกจาก baseline ไม่งั้นมันจะกลายเป็นการนิรโทษกรรมถาวรที่ไม่มีใครมาตัด · 🔑 **มี self-test บังคับ**: ตัวสแกนต้องเห็นไฟล์ที่รู้อยู่แล้วว่าถูกใช้ 4 ไฟล์ **ถ้ามองไม่เห็นแม้ใบเดียว = ล้มทันทีพร้อมข้อความว่า "แก้ตัวสแกน ไม่ใช่แก้ไฟล์"** — เพราะตัวสแกนที่มองผิดที่ รายงานว่าสะอาดด้วยความมั่นใจเท่ากับตัวที่ทำงานถูก · 🔴 **`docs/` `README.md` `CLAUDE.md` ถูกกันออกจากขอบเขตโดยตั้งใจ** — หัวข้อ *Image credits* ใน `README` เคยทำให้รูป `stock/` 5 ใบดูเหมือนถูกใช้ ⇒ **เอกสารที่บอกว่า "ภาพนี้มาจากไหน" กลายเป็นหลักฐานว่า "ภาพนี้ถูกใช้อยู่"** · **allowlist มีรายการเดียว** (`llms.txt`) เพราะไอคอนทุกตัวถูกอ้างจริงใน `layout.tsx` ⇒ ทุกรายการใน allowlist คือรูโหว่ของด่าน · ✅ **ทดสอบ 4 ทางก่อนเชื่อ ไม่ได้เดาว่ามันทำงาน**: ปกติ→ผ่าน (exit 0) · วางไฟล์กำพร้าปลอม→**exit 1** · ลบไฟล์ที่อยู่ใน baseline→**exit 1** (baseline ค้าง) · บีบให้ตัวสแกนมองผิดที่→**self-test ล้ม** · verify: build ✓ · lint ✓ · CI เขียวหลัง push |
| W-20 | หน้า Landing ของ Signature + ราคา `£69 / 60 นาที` | 2026-08-11 | **ขึ้นก่อนโฆษณาตามลำดับที่ปลอดภัย** — `SIG` ยังไม่ยิง · สร้าง [`app/signature/page.tsx`](../../app/signature/page.tsx) · เนื้อหา **ลอกจากที่เจ้าของอนุมัติทุกตัวอักษร ไม่แต่งเอง** (H1 `Taitam-D Signature Thai Massage` · บรรทัดรอง · รายการ blend 7 อย่าง) · 🔑 **เก็บราคาเป็นสตริงเดียวผูกกับชื่อสินค้าใน `lib/site.ts`** ไม่ใช่ตัวเลขลอย — เพราะ **`£69` มีอยู่แล้วบนหน้าแรกในความหมาย "Hair Spa ผมยาวปานกลาง"** ⇒ เลขลอยจะไปนั่งข้างเลขที่ไม่เกี่ยวกันโดยไม่มีอะไรบอกว่าอันไหนของอะไร · ⛔ เขียนคอมเมนต์กำกับว่า **ห้าม render เป็น "from £69"** · **ไม่ผูกชื่อ Academy** ตามกฎก่อน 16 ส.ค. · เติมแถวใน [`sitemap.ts`](../../app/sitemap.ts) พร้อม trailing slash (บทเรียน `W-5` — sitemap เป็นลิสต์มือ **build ไม่เตือนถ้าลืม**) · verify: build ✓ 12 หน้า · lint ✓ · **visual gate ทั้ง desktop และ mobile** (ราคาอยู่เหนือ fold ทั้งสองขนาด) · console ไม่มี error · **production**: `/signature/` **200** · ราคาตรงตัว · `from £69` = **0** · sitemap มีแถวใหม่ · 7 หน้า 200 |
| W-21 | เติมชื่อนิติบุคคลใน footer (`Q-WS-080`) | 2026-08-11 | ถ้อยคำจาก `LAW` **ลอกมาทั้งประโยค ไม่ถอดความ** (เขาวัด Companies House เอง: `11141046` · Active · England and Wales) · 🔑 **ไม่ใช่แค่เติม footer — `legalName` เดิมเก็บ *ชื่อทางการค้า* ไว้** ⇒ JSON-LD ประกาศบริษัทที่ไม่มีอยู่จริงในชื่อนั้นมาตลอด · แยกเป็น `name` (แบรนด์ คงเดิม) + `legalName` (บริษัทจดทะเบียน) + `companyNumber` + `jurisdiction` + `registeredOffice` · ⛔ ไม่ใส่ `vatID` (ไม่มีเลข VAT) · ⚠️ **ที่อยู่สองอันต่างกันโดยถูกต้อง**: ร้าน `72-74` · สำนักงานจดทะเบียน `72` ⇒ **แยกป้ายกำกับ ห้ามรวมเป็นค่าเดียว** เพราะผู้อ่านต้องรู้ว่าส่งหนังสือตามกฎหมายไปที่ไหน · 🔴 **เป็นข้อบังคับตามกฎหมาย ไม่ใช่ความเรียบร้อย** — Companies (Trading Disclosures) Regulations 2015 reg 25–26 มีโทษปรับ ⇒ เว็บอยู่ในสภาพผิดกฎหมายมาก่อนที่ Google จะประกาศอะไร · verify: **อ่านจาก production แบบตัด markup** ยืนยันข้อความเต็มปรากฏบน `/` · `/signature/` · `/contact/` · `"legalName":"TAITAM-D MANAGEMENT LTD"` ใน JSON-LD |
| W-17 | เบอร์โทรในเว็บ ≠ เบอร์ที่ `Q-MKT-002` ระบุ | 2026-08-10 | **ปิดโดยไม่แก้โค้ด — เว็บถูกอยู่แล้วตั้งแต่ต้น** · เจ้าของเคาะผ่าน Kom: **`+442072780906` (020) ถูกยกเลิกไปแล้ว · เบอร์บนเว็บคือเบอร์ล่าสุด** ([D-W21](DECISIONS-BOARD.md)) · วัดแล้ว: เบอร์ 020 อยู่ใน repo **0 จุด** (ที่ grep เจอคือบันทึกที่ตั้งคำถามเรื่องนี้เอง) · อ่านสดจาก production ยืนยัน `tel:07564292247` 4 จุด · `"telephone":"+447564292247"` ใน JSON-LD · 🔑 **บทเรียน: การไม่ทำตามใบคือสิ่งที่ทำให้จับได้** — ถ้าใส่ตามที่ใบสั่ง เว็บจะประกาศเบอร์ที่ตายแล้ว **โดยที่ทุกคนคิดว่าแก้ให้ตรงกันแล้ว** · งานจริงอยู่นอก repo (Ads sitelink · GBP · Treatwell · Facebook) ⇒ ออกใบ `Q-SHOP-001` ถึง `WS` ให้ route ไป `MKT` |
| W-16 | `Q-MKT-002` — `Organization` JSON-LD + `sameAs` | 2026-08-10 | 🔑 **โจทย์กลับด้าน: schema มีอยู่แล้ว** (`@graph` เต็มใน [`StructuredData.tsx`](../../components/StructuredData.tsx)) ⇒ งานจริงคือตรวจว่ามันประกาศอะไร · **`sameAs` เดิม 3 รายการตกกติกาของใบเองทั้งหมด**: `facebook` ชี้ **เพจ Academy** · `instagram` ยังไม่มี business account · `googleMaps` เป็น URL ค้นหา ไม่ใช่ GBP ⇒ เหลือรายการเดียวที่ยืนยันแล้ว · **ยืนยันตัวตนเพจเอง**: `og:title` = `Taitam-D Beauty&Spa Centre \| London` vs เพจ Academy = `Taitam-D Beauty Academy \| London` · ⚠️ **ที่ใหญ่กว่า schema**: `SITE.social.facebook` ถูกอ่านโดย [`FollowUs.tsx`](../../components/FollowUs.tsx) ⇒ **ปุ่ม Facebook ใน footer ทุกหน้าส่งลูกค้าไปเพจ Academy มาตลอด** · 🔑 `grep "SITE\.social\.facebook"` ได้ **1 hit** เพราะ `FollowUs` ทำ `const s = SITE.social` แล้วเรียก `s.facebook` — **grep ชื่อเต็มมองไม่เห็น alias** จับได้เพราะอ่าน `href` จาก DOM จริง · `addressLocality` `King's Cross`→`London` (เขต ≠ เมือง) · **ไม่ทำ 3 ข้อพร้อมเหตุผลในใบ**: `@id` คง `#business` (ใบเองสั่งว่า `@id` ต้องคงที่) · `telephone` ขัดกัน → แยกเป็น W-17 · `foundingDate` อยู่บน production ก่อนแล้ว การลบชนกฎเหล็กข้อ 4 + `D-W1` · verify: build ✓ 11 หน้า · lint ✓ · visual gate 3301 ✓ (2 หน้า · console ไม่มี error · `JSON.parse()` ในเบราว์เซอร์ผ่าน) · **production หลัง deploy**: `sameAs` เหลือ 1 · `ld+json` 1 ก้อน/หน้า ไม่มี Organization ซ้อน · เพจ Academy บน `/contact/` เหลือ **0** |
| W-15 | หน้า `/cookies` บรรยาย Academy ผิด ("sets no cookies of its own") | 2026-08-09 | **วัดเอง**: `curl -I https://academy.taitam-d.com/en` → `set-cookie: NEXT_LOCALE=en` ตั้งแต่ยังไม่ล็อกอิน ⇒ ถ้อยคำเดิมเท็จ · แก้เป็น **"ไม่มีคุกกี้วิเคราะห์หรือโฆษณา — มีแต่คุกกี้จำเป็น เช่น จำภาษา และคงสถานะล็อกอินถ้าคุณสมัคร ซึ่งไม่ต้องขอความยินยอม"** ตาม [D-W16](DECISIONS-BOARD.md) (เจ้าของให้สิทธิ์ข้ามด่าน Legal · Legal แก้ทีหลังได้) · **ไม่แตะตารางทะเบียนคุกกี้ของเราเอง** ⇒ กฎเหล็กข้อ 2 ไม่ถูกกระทบ · verify: build ✓ 11 หน้า · lint ✓ · visual gate ✓ อ่านย่อหน้าจริงเต็ม ๆ บนหน้า `/cookies` · 6 หน้า 200 |
| W-13 | URL ของเว็บเก่าที่ Google ยัง index | 2026-08-09 | **ปิดด้วยการเคาะว่าไม่ทำ** — 404 ถูกต้องแล้ว และ 301 ที่น่าทำ (`/courses/…`) **ทำจากรีโปนี้ไม่ได้** ต้องทำที่ Hostinger ฝั่งโดเมนเก่า ⇒ ไม่ใช่งานห้องนี้ · เหตุผลเต็มและเงื่อนไขกลับมาทบทวนอยู่ที่ [D-W18](DECISIONS-BOARD.md) |
| W-14 | ลิงก์ Academy ชี้คนละโดเมนกับที่หน้านโยบายคุกกี้เราระบุ | 2026-08-09 | **เจ้าของเคาะ**: ปลายทาง = `academy.taitam-d.com` · โดเมน WordPress เก่า **เราถือเองอยู่บน Hostinger** (Marketing เช็ค WHOIS: จด 11 ก.ค. 2023 · หมดอายุ 2027) ไม่ใช่ทรัพย์สินหลุดมือ **และมีแผนยกเลิกทั้งหมด** ⇒ ลิงก์เดิมจะพังแน่นอนในอนาคต ไม่ใช่แค่ชี้ผิดชื่อ · แก้ **ค่าเดียว** `SITE.academy` ใน [`lib/site.ts`](../../lib/site.ts) แล้วครอบคลุมทั้ง 9 จุด (navbar · แถบล่างมือถือ · footer · `/story/` · `AcademyPromo`) — **ผลของการรวมศูนย์ค่าไว้ที่เดียวตามที่ `CLAUDE.md` วางไว้** · แก้ `README.md` + `CLAUDE.md` ที่ยังเขียนโดเมนเก่าด้วย · verify: build ✓ · lint ✓ · ปลายทางจริง `307 → /en` แล้ว **200** · ผลลัพธ์ที่ build ออกมามีลิงก์ใหม่ **20 จุด เหลือโดเมนเก่า 0** · visual gate ✓ 6 หน้า 200 · ตรวจ `<a>` ในหน้าจริง: ชี้ `academy.taitam-d.com` **6 ลิงก์** + Facebook เพจ 1 (ตั้งใจไม่แตะ เป็นที่อยู่ทรัพยากร) |
| W-12 | เว็บใช้ชื่อสถาบันผิด (`Beauty Academy` → `Taitam-D Academy London`) | 2026-08-09 | เจ้าของยืนยันว่าเป็นหน่วยเดียวกันและชื่อถูกอยู่บนโลโก้ ⇒ [D-W14](DECISIONS-BOARD.md) · แก้ 4 จุด (หัวข้อการ์ด · ปุ่ม · บรรทัดตำแหน่ง · ลิงก์ท้ายเว็บ) · **ไม่แตะ**: ป้ายนำทางที่เขียนแค่ `Academy` และ URL เฟซบุ๊กที่มีคำว่า BeautyAcademy อยู่ในตัวลิงก์ · verify: build ✓ 11 หน้า · lint ✓ · `grep "Beauty Academy"` ในซอร์สเหลือ **0** · visual gate ✓ **เห็นเต็มหน้าจริง** ทั้งการ์ด Academy และท้ายเว็บ 🔧 ท่าที่ใช้ได้กับ pane ที่ถ่ายภาพไม่ติดตอนเลื่อน: **ตั้ง viewport สูง (1280×2200) แล้วโหลดใหม่ + บังคับ `opacity:1`/`transform:none` ให้ Reveal** ได้ภาพเต็มหน้าในช็อตเดียว ดีกว่าซ่อน section ทีละอัน · ⚠️ เจอโดยบังเอิญ: รูปในร้านบนหน้านั้นมีป้ายผนังเขียน `TAITAM-D ACADEMY LONDON` ยืนยันชื่อซ้ำ |
| W-7 · W-9 | ยกเวอร์ชัน action ทั้งชุดใน workflow + ตั้ง `node-version` ให้ตรงเครื่องที่ verify | 2026-08-09 | **แก้ให้ตรงเหตุจริง ไม่ใช่ตามที่คิวเขียนไว้ตอนแรก**: annotation "Node.js 20 is deprecated" มาจาก runtime ของตัว action เอง ไม่ใช่ `node-version` (คิวเดิมผมเขียนผิดเอง แก้ก่อนลงมือ) · ยก `checkout v4→v7` · `setup-node v4→v7` · `cache v4→v6` · `configure-pages v5→v6` · `upload-pages-artifact v3→v5` · `deploy-pages v4→v5` · **W-9 ทำพ่วง**: `node-version 20→22` ให้ตรงกับเครื่องที่รัน gate (เหตุผลเดียวกับ [D-W8](DECISIONS-BOARD.md) — gate ที่รันคนละ runtime กับ deploy คือ gate ครึ่งใบ) · **ยืนยันจาก log จริง ไม่ใช่จากไฟล์ที่แก้**: `node: v22.23.1` · ค้นคำว่า "Node 20 is being deprecated" ในล็อกได้ **0 ครั้ง** (เดิมมีทุก run) · deploy success · production 200 ครบ 6 หน้า · title และชื่อผู้ก่อตั้งยังถูกต้อง · ⚠️ ตรงกันแค่ระดับ **major** (เครื่อง 22.14.0 · CI 22.23.1) — จงใจ ไม่ pin ถึง patch เพราะจะค้างทันทีที่เครื่องอัปเดต |
| W-8 | `npm audit` 10 รายการ (9 high) | 2026-08-09 | **ปิดด้วยการเคาะว่าไม่ทำ ไม่ใช่การเลื่อน** — ดู [D-W13](DECISIONS-BOARD.md) พร้อมเงื่อนไข 3 ข้อที่จะทำให้กลับมาทบทวน |
| W-11 | เว็บสะกดนามสกุลผู้ก่อตั้งผิด (`Phomnongsan` → `Pomnongsan`) | 2026-08-09 | ครูนกเคาะเองตาม [D-W10](DECISIONS-BOARD.md) · แก้ 3 จุดใน [`Story.tsx`](../../components/Story.tsx) — ท้ายคำพูด · หัวข้อ · **`alt` ของรูป** (จุดที่ตาไม่เห็นแต่ screen reader กับ Google อ่าน) · ตรวจทั้ง repo แล้วไม่มีที่อื่นอีก · verify: build ✓ 11 หน้า · lint ✓ · output จริงนับได้ `Thanyarat Pomnongsan` 6 ครั้ง เหลือ `Phomnongsan` 0 · visual gate ✓ **เห็นบล็อกผู้ก่อตั้งเต็ม ๆ ด้วยตา** (ชื่อ · ตำแหน่ง · คำพูด ถูกทุกจุด) 🔧 หมายเหตุเครื่องมือ: pane คืนภาพเปล่าทุกครั้งที่หน้าถูกเลื่อน ⇒ ต้องซ่อน section ก่อนหน้าด้วย JS ให้บล็อกขึ้นมาอยู่บนสุดถึงจะถ่ายติด (แก้ในเบราว์เซอร์อย่างเดียว ไม่แตะซอร์ส) |
| W-10 | ชื่อแบรนด์บนเว็บไม่ตรง `D-71` สองแบบพร้อมกัน | 2026-08-09 | ห้อง Concept ยืนยันว่า **`D-71` เคาะแล้ว: ยัติภังค์ ASCII + `D` ตัวใหญ่ตัวเดียว ⇒ `Taitam-D`** และแยกทำได้เลยไม่ต้องรอเรื่องชื่อคน · **เจอสองปัญหาไม่ใช่หนึ่ง**: (1) `TaiTam-D` (T ใหญ่สองตัว) **22 จุดใน 9 ไฟล์** — รวมข้อความ WhatsApp สำเร็จรูปที่ลูกค้าได้รับจริง ไม่ใช่แค่ข้อความบนหน้าจอ · (2) 🔑 **ที่ห้อง Concept ยังไม่เห็น** — `lib/site.ts` ใช้ **U+2011 non-breaking hyphen** ไม่ใช่ ASCII hyphen ใน 3 จุด ซึ่งไหลเข้า **`<title>` · OG · JSON-LD `legalName`** ⇒ สตริงแบรนด์ที่ Google อ่านไม่ตรงกับที่ไหนเลย · ⚠️ **U+2011 อาจตั้งใจใส่เพื่อกัน brand ตัดบรรทัด** — ถ้าจะเอา no-break กลับมา ใช้ CSS (`white-space: nowrap`) แทนอักขระ อย่าย้อนตัวอักษร · verify: build ✓ 11 หน้า · lint ✓ · ตรวจ output จริง: `<title>` และ `legalName` เป็น ASCII แล้ว · WhatsApp deep link เป็น `Hi%20Taitam-D` · visual gate 3301 ✓ 6 หน้า 200 · console ไม่มี error · **ไม่แตะการสะกดชื่อครูนก** (`Phomnongsan` คงเดิม ตรวจ diff ยืนยันแล้ว) |
| W-3 | ย้าย lint จาก `next lint` ไป ESLint CLI | 2026-08-09 | `next lint` deprecated จะถูกถอดใน Next 16 · ทำมือไม่ใช้ codemod (`@next/codemod@canary` เป็น tag ที่ขยับได้ตลอด ไม่อยากให้ deploy path ขึ้นกับของที่ไม่ pin) · `eslint-config-next` 15.5 **ยังไม่มี flat entry** ⇒ ใช้ `FlatCompat` ครอบ preset เดิมสองตัว ⇒ **เปลี่ยนตัวรัน ไม่เปลี่ยนกฎ** · เติม `@eslint/eslintrc` เป็น devDependency ตรง ๆ (เดิมมีแค่ transitive ผ่าน eslint) · 🔑 **พิสูจน์ว่า `next build` ยัง lint ให้อยู่จริง** ด้วยการใส่ probe ผิดกฎแล้วดูว่า build จับได้ ก่อนลบ probe ทิ้ง — ไม่ได้เดาจากเอกสาร · verify: build ✓ 11 หน้า · lint ✓ · visual gate 3301 ✓ (6 หน้า 200 · /cookies/ render เต็ม · console ไม่มี error) |
| W-6 | lockfile สองใบ ⇒ ของที่ verify ในเครื่อง ≠ ของที่ CI deploy | 2026-08-09 | เจอตอนสำรวจก่อนทำ W-3 (จะเพิ่ม devDependency แล้วมันจะลงแค่ lockfile ใบเดียว) · **วัดจริง**: CI `next 15.5.12`/`react 19.2.4`/`eslint 9.39.2` vs เครื่อง `15.5.22`/`19.2.8`/`9.39.5` · ตาม [D-W8](DECISIONS-BOARD.md) ลบ `pnpm-lock.yaml` แล้ว `npm ci` ⇒ เครื่องตรงกับ CI ทุกตัว · verify: `npm run build` ✓ 11 หน้า · `npm run lint` ✓ · visual gate 3301 ✓ (6 หน้า 200 ครบ · หน้า /services/ render เต็ม · cookie banner ทำงาน · console ไม่มี error) — **รอบนี้คือ gate แรกที่รันบน dependency ชุดเดียวกับที่ขึ้นเว็บจริง** |
| W-5 | `sitemap.xml` ลิสต์ URL แบบไม่มี trailing slash ทั้งที่เว็บ serve แบบมี ⇒ ทุก URL เป็น redirect | 2026-08-09 | request จากห้อง Marketing Lead (audit production) · **วัดซ้ำเองก่อนเชื่อ**: `/services` `/story` `/contact` `/book` `/cookies` ตอบ 301 ไป `…/` ทั้งหมด มีแต่ `/` ที่ 200 ตรง ⇒ **5 URL ไม่ใช่ 4 (Marketing ตกหล่น `/cookies`)** · ต้นเหตุ: [`app/sitemap.ts`](../../app/sitemap.ts) เขียนแบบไม่มี slash ขณะที่ `next.config.mjs` ตั้ง `trailingSlash: true` และ canonical บนหน้าก็เป็นแบบมี slash · verify: `npm run build` ✓ 11 หน้า · `npm run lint` ✓ no warnings · visual gate 3301 ✓ (หน้าแรก render ครบ · cookie banner ทำงาน · console ไม่มี error) · ยืนยันผลจริงจาก sitemap ที่ build ออกมา slash ครบทั้ง 5 |
| W-0 | รับโอนความรับผิดชอบเว็บไซต์จากห้อง Marketing Lead + เข้า Gumon Lore + วางกติกา Lead/Executor | 2026-08-09 | Marketing ยืนยันหยุดแก้ repo · `lore-add` สำเร็จ (14 โปรเจกต์) · `CLAUDE.md` มีกฎ 4 ข้อ + delta กติกา |
| W-2 | commit `CLAUDE.md` + `docs/plans/` เข้า repo | 2026-08-09 | verify เขียวก่อน push ตาม D-W4/D-W5 (`npm run build` ✓ 11 หน้า · `npm run lint` ✓ no warnings) · stage ราย path ตาม §11.17 |
| W-4 | ลบ workflow ที่ deploy ซ้ำ — เหลือ `nextjs.yml` ตัวเดียว | 2026-08-09 | ลบ `.github/workflows/pages.yml` ตาม D-W6 · ยืนยันหลัง push ว่าเหลือ **run เดียว** ต่อ push และเว็บ live ยังปกติ |
| W-1 | รับจัดสรร port band + สร้าง `.claude/launch.json` | 2026-08-09 | ได้ **3300–3399** จากห้อง Local Dev Lead (บันทึกใน `gumon-localdev/registry.json → devServerPorts`) — ⚠️ *ชื่อห้องและ path ณ วันนั้น · ปัจจุบันคือ **Gumon Workspace Lead** และไฟล์อยู่ที่ `gumon-workspace/machines/<id>/registry.json` (rename 2026-08-09) · **ไม่แก้ข้อความเดิมเพราะเป็นบันทึกว่าเกิดอะไรขึ้นตอนนั้น** ไม่ใช่คำแนะนำให้เดินตาม* · เติม `distDir` ให้ `next.config.mjs` รองรับ `NEXT_DIST_DIR` (ไม่ตั้ง env = `.next` เหมือนเดิม) · เติม `.next-*` ใน `.gitignore` (`.next` ไม่ match `.next-claude`) · **พิสูจน์แล้ว**: `preview_start` ขึ้น 3300 · หน้าแรก render ครบ · cookie banner ทำงาน · หยุด server + คืนไฟล์ + ยืนยันพอร์ตปิดด้วย `lsof` |

---

## ของที่ยังไม่มีในคิว (สำรวจแล้ว ณ 2026-08-09)

- **Meta Pixel — ปิดเรื่องแล้ว ไม่ลงคิว** · เจ้าของเคาะ "ยังไม่ยิง Meta ads" และ Marketing ยกเลิก
  คำขอเอง ⇒ **ห้ามแตะ tag layer** ดู [D-W7](DECISIONS-BOARD.md) · ผลประเมินเก็บไว้ใน [`HANDOFF.md §3`](HANDOFF.md)
- **`phone_click` / `whatsapp_click` ไม่ใช่งาน** — พิสูจน์แล้วว่าทำงานถูก ห้ามแก้ ดู [`HANDOFF.md §3`](HANDOFF.md)
- **Academy launch 16 ส.ค.** — ยังไม่มีทราฟฟิกมาฝั่งเว็บร้านตามข้อตกลง ⇒ ยังไม่มีงานเว็บ
- **PMax รันอยู่** ชี้ที่ apex `https://taitam-d.com/` ⇒ งานที่กระทบ **น้ำหนักหน้าแรกและ LCP** ให้ถือว่า priority สูงเสมอ
