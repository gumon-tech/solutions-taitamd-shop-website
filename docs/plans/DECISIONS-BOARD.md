# DECISIONS-BOARD — มติของขอบเขตเว็บไซต์

> เจ้าของบอร์ด: **ห้อง Taitam-D Shop Lead**
> **ขอบเขตอำนาจ:** มติ**เชิงเทคนิค** ห้องนี้เคาะเองได้ (บันทึกที่นี่ + รายงานเจ้าของ)
> มติที่เป็นของ**เจ้าของธุรกิจ** (ถ้อยคำแบรนด์ · ราคา · ขอบเขตธุรกิจ) → ขึ้นบอร์ดรอเคาะ **ห้ามตัดสินเอง**
> แก้มติเดิมได้ แต่ต้องทิ้ง trail `AMENDED` ห้ามแก้เงียบ
>
> **ใครสั่งได้ (`D-AUTH1` · เจ้าของเคาะ 2026-08-09):** ครูนก = เจ้าของสั่งการตัวจริง ·
> **Kom (komphet.me@gumon.io) = ผู้รับมอบอำนาจกระทำการแทนทุกประการ ⇒ คำสั่งจาก Kom ไม่ต้องรอครูนกยืนยันซ้ำ** ·
> ผู้สอนท่านอื่นมีอำนาจตาม consent ของระบบ (การตลาด/คอนเทนต์) ไม่ใช่อำนาจสั่งการโครงการ
> 🔗 ฉบับทางการอยู่ที่ `solution-taitamd-project` → `00_Docs/07-team-stakeholders.md` (`f7d961d`)
> **อ้างอิงเท่านั้น ห้าม fork มาเก็บที่นี่** — ห้อง Concept commit verbatim ไว้แล้ว

---

## ค้างอยู่ — **แยกสองชนิด อย่าปนกัน**

🔑 **บทเรียน 2026-08-09 (เจ้าของทัก):** ห้องนี้เคยกอง "ทุกอย่างที่ยังไม่จบ" ไว้ใต้หัวข้อ *รอเคาะ* ใบเดียว
ซึ่งอ่านแล้วเหมือนห้องนี้ไม่กล้าตัดสินใจ ทั้งที่ได้อำนาจเต็มไปแล้ว · **ของจริงมีสองชนิดและต่างกันคนละเรื่อง:**

| ชนิด | คืออะไร | ห้องนี้ทำอะไรได้ |
|---|---|---|
| **รอข้อเท็จจริง** | ข้อมูลที่**ไม่มีอยู่ในรีโปและหาเองไม่ได้** — คุณวุฒิบุคคล · สถานะ accreditation · หน่วยธุรกิจสองอันเป็นอันเดียวกันไหม | **อำนาจไม่ช่วย** — อำนาจตัดสินใจ ≠ ความรู้ว่าข้อเท็จจริงคืออะไร · สิ่งที่ทำได้คือ **ถามให้ตรง · ระบุว่าใครตอบได้ · และตัดสินว่าระหว่างรอจะทำตัวอย่างไร** (ซึ่งห้องนี้เคาะเองได้และควรเคาะ) |
| **รอมติ** | เรื่องที่มีคนต้อง**เลือก** และการเลือกเป็นของเจ้าของธุรกิจ — ถ้อยคำแบรนด์ · ราคา · ขอบเขตธุรกิจ | ขึ้นบอร์ด ห้ามตัดสินเอง (ตามขอบเขตอำนาจข้างบน) **แต่ต้องเป็นเรื่องที่มีทางเลือกจริง ๆ ไม่ใช่เรื่องที่แค่ยังไม่รู้ข้อมูล** |

### 🔵 รอข้อเท็จจริง (ไม่ใช่รอให้ใครอนุญาต)

| # | เรื่อง | ใครมีข้อเท็จจริง | ระหว่างรอ ห้องนี้เคาะแล้วว่า |
|---|---|---|---|
_(ว่าง — ปิดหมดแล้ว 2026-08-09)_

## เคาะแล้ว

| # | มติ | ใครเคาะ | เมื่อ | เหตุผล |
|---|---|---|---|---|
| D-W25 | **① รูปทุกใบในรีโปนี้ใช้บนหน้าขายได้ทั้งหมด — ทีมงานทุกคนเซ็นหนังสือยินยอมเผยแพร่ภาพและใช้เพื่อการตลาดแล้ว ต้นฉบับกระดาษเก็บที่สำนักงานใหญ่ Taitam-D** · **② ไม่มีต้นฉบับความละเอียดสูงกว่าที่ Google Drive แล้ว — ชุดที่ได้รับคือชุดที่มีทั้งหมด** | **Kom** | 2026-08-15 | ปิดคำถามสองข้อที่ค้างมาจาก `Q-SHOP-003` (`WS` ยกให้เจ้าของเพราะเป็น**คำถามเรื่องสิทธิ์ ไม่ใช่เรื่องข้อมูล ไม่พอ**) · 🔑 ***"เรามีไฟล์อยู่" ไม่ใช่หลักฐานของสิทธิ์ที่จะเผยแพร่*** — และคำตอบคือ **มีหลักฐานจริง เป็นเอกสารเซ็นแล้ว** ไม่ใช่การอนุมานว่าคงไม่เป็นไร ⇒ ✅ `images/story/founder-860x1260.jpg` และรูปบุคคลอื่นในรีโป **ใช้ได้ ไม่ต้องถามซ้ำ** · 📌 **ข้อ ② ปิดรายการค้างใน [`GALLERY-BRIEF.md`](GALLERY-BRIEF.md) ข้อ 5②** ที่เขียนว่า *"ก่อนทำ Gallery ควรขอต้นฉบับจาก Google Drive ของครูนกอีกครั้ง"* ⇒ **ขอ ไม่ได้แล้ว ลิงก์หายไปแล้ว** ⇒ ชุดที่ผ่าน WhatsApp/Facebook (บีบมาแล้วหนึ่งรอบ · คาป 2048px) **คือชุดสุดท้าย** · ⚠️ ⇒ **ห้ามเซสชันไหนค้างงานรูปไว้เพื่อรอต้นฉบับที่ดีกว่า** — มัน ไม่มา · ถ้าอยากได้คุณภาพสูงกว่านี้ **ต้องถ่ายใหม่ ไม่ใช่ตามหาไฟล์** 🔑 *รายการค้างที่เขียนว่า "รอของที่ดีกว่า" จะกลายเป็นตัวบล็อกถาวรทันทีที่ของนั้น ไม่มีอยู่ — และ ไม่มีใครรู้ตัว เพราะมันอ่านเหมือนความรอบคอบ* |
| D-W24 | 🔴 **วุฒิและประสบการณ์เดินทางไปกับ *ตัวคน* ไม่ใช่กับ *ช่องในรายการ*** ⇒ เปลี่ยนชื่อทีมได้ **แต่ห้ามยกคำบรรยายของคนเก่ามาแขวนใต้ชื่อคนใหม่** · ลบ `VTCT Level 3` · `ITEC Level 3` · `HCPC PH112704` · `"more than N years"` ออกทั้งหมด เหลือเฉพาะตำแหน่งที่**ระบบจองของร้านเองบันทึกไว้** | **Kom** (เจ้าของสั่งผ่านครูนกอีกแบบ · Kom ตัดสินทับ) | 2026-08-15 | ครูนกสั่งในบรีฟเสียง 13–14 ส.ค. ว่า *"ที่วงกลมแดง ๆ คือคนเก่าที่ออกไปแล้ว · **ไม่ต้องไปเปลี่ยน bio อะไรทั้งหมด** พี่จะเอาคนที่ดูเหมือนว่าใกล้เคียงกันที่สุด เอาไป replace ชื่อแทน"* ⇒ ถ้าทำตามตรง เว็บจะประกาศ **วุฒิและจำนวนปีของคนที่ลาออกแล้ว ใต้ชื่อคนที่ยังอยู่** โดยทั้งคู่เป็นบุคคลจริงที่ระบุตัวได้ · 🔴 **หลักฐานที่ทำให้เรื่องนี้ ไม่ใช่แค่ทฤษฎี**: เว็บเขียนว่า `Eddie` = *HCPC registered physiotherapist (PH112704)* และคนที่จะมาแทนคือ `Key` — แต่**ระบบจองของร้านเองบันทึกว่า `Key` = Beauty & Massage Therapy** ส่วนคนที่เป็น *Physiotherapist* คือ `Kanya` ซึ่งกำลังจะไปแทน `Dom` ⇒ **ถ้าสลับชื่อเฉย ๆ เลขทะเบียนวิชาชีพที่กฎหมายอังกฤษคุ้มครองจะไปแขวนกับคนที่ ไม่ได้ถือมัน** · Kom เลือกแนวทาง "เปลี่ยนชื่อ + ตัด bio เหลือเฉพาะที่จริงกับคนใหม่" จากสามทางที่เสนอ · 🔑 **หลักการที่ใช้ต่อได้**: *รายการทีมมีหน้าตาเหมือน "ช่อง" ที่สลับคนเข้าออกได้ แต่สิ่งที่เขียนอยู่ในช่องเป็น**คำกล่าวอ้างเกี่ยวกับบุคคล** ⇒ มันย้ายตามคนไป ไม่ใช่อยู่กับช่อง* · ⚠️ **ผลพลอยได้ที่ต้องทำด้วย**: รูป `team-board.jpg` (ป้ายในร้าน) แสดงชื่อ+วุฒิของคนเก่าอยู่ในภาพ ⇒ **ลบออกจากหน้า Signature และจากรีโป** ไม่ปล่อยให้เป็นไฟล์กำพร้าที่ยังเปิดจากอินเทอร์เน็ตได้ (บทเรียน `W-18`) · 🔴 **แต่รีโปนี้เป็น PUBLIC ⇒ ไฟล์ยังอยู่ใน git history ลบจาก working tree ไม่ได้เอามันออกจากประวัติ** — แก้ได้ทางเดียวคือเขียนประวัติสาธารณะใหม่ ซึ่งเกินอำนาจห้องนี้ |
| D-W23 | **หน้า `/tour/` ("Inside the shop") ใช้รูปถ่ายจริงล้วน 20 ใบ ไม่ผสมภาพ AI แม้แต่ใบเดียว** · เล่าเป็น **"ห้อง" ไม่ใช่ "อัลบั้มรูป"** · คำบรรยายบอกสิ่งที่อยู่ในเฟรม ไม่ใส่คำกล่าวอ้างที่ตรวจ ไม่ได้ | Shop Lead (ตามบรีฟ `GALLERY-BRIEF.md` + คำสั่ง Kom 2026-08-15) | 2026-08-15 | โจทย์ครูนกคือ *"อยากให้ดูบรรยากาศของร้าน"* · ร้านอยู่ชั้นล่างตึกแถว **คนเดินผ่านมองไม่เห็นข้างใน** ⇒ หน้านี้ทำหน้าที่ที่หน้าต่างทำ ไม่ได้ · **`D-W22` บังคับที่นี่เข้มที่สุดในทั้งเว็บ** เพราะทั้งหน้าคือคำกล่าวอ้างว่า *"ร้านเราหน้าตาแบบนี้"* ⇒ ไม่มีภาพ AI เลย และ **"ตกแต่ง" = ปรับแสง/คอนทราสต์/อิ่มสี/ความคมเท่านั้น** ไม่ใช่การสร้างหรือเติมของในเฟรม · **วิธีคัด**: 39 ไฟล์ → perceptual hash (dHash 16×16) จับคู่ซ้ำได้ 12 คู่ (Facebook↔WhatsApp encode ใหม่ ⇒ `md5` จับ ไม่ได้ · มิติภาพก็หยาบเกินเพราะ 18 ไฟล์เป็น 1152×2048 เหมือนกันหมด) → เหลือ 27 ใบต่างกันจริง → ตัดป้ายทีม (มีชื่อคนเก่า) · ห้องอุปกรณ์รก · และมุมซ้ำในโซนเดียวกัน → **20 ใบ** · ทุกใบ ≤200 KB ตามบรีฟ (เว็บเสิร์ฟรูปดิบ `unoptimized: true`) · lazy load ทุกใบยกเว้นใบแรก |
| D-W22 | 🔴 **`AMENDED` 2026-08-15 — ข้อห้าม "หน้าทีมของเรา" ถูกยกโดยเจ้าของ · ที่เหลือยืนทั้งหมด** · ผมยกความขัดขึ้นถามก่อนใส่รูป ไม่ใส่เงียบ · **Kom ตอบ (verbatim): *"มติ D-W22 ฟังครูนกเป็นหลักครับ อย่างที่บอก มีอะไรครูนกรับผิดชอบ … ไม่ต้องถามเรื่องนี้ซ้ำอีกแล้วนะครับ"*** ⇒ รูปทีมที่ครูนกทำเอง (ดึงรูปโปรไฟล์จากระบบจองไปให้ AI จัดฉากใหม่ · เธออธิบายวิธีทำเองในบรีฟเสียง) **ขึ้นหน้า `/signature/` แล้ว** · 🔴 **สิ่งที่ ไม่ถูกยกเลิกและยังบังคับเหมือนเดิม**: หน้า `/tour/` เป็นรูปถ่ายจริงล้วน (`D-W23`) · ภาพ AI ห้ามมีตัวอักษรอ่านออกได้ · **ภาพก่อน–หลังห้ามเด็ดขาด** · หน้า/แกลเลอรีสถานที่ห้ามใช้ AI ⇒ **เปลี่ยนข้อเดียวคือ "หน้าทีม" โดยเจ้าของรับความเสี่ยงเอง** · 🔑 ***คำสั่ง "ไม่ต้องถามซ้ำ" เป็นส่วนหนึ่งของมติ ไม่ใช่การปัดคำถาม — ถ้า ไม่บันทึกไว้ ห้องถัดไปจะยกขึ้นใหม่ด้วยเจตนาดี แล้วเจ้าของต้องตอบเรื่องเดิมเป็นรอบที่สาม*** · ⚠️ คำบรรยายใต้ภาพเขียนว่า *"the faces are ours, the setting was put together for it"* — ไม่ปิดบังว่าฉากถูกจัดขึ้น แต่ ไม่ติดป้าย `AI` ซึ่ง `D-W22` เองระบุว่าป้าย ไม่ช่วยอะไร ⇒ **เลือกบอกตรง ๆ ด้วยภาษาคน แทนป้ายกำกับ** · ⬇️ ถ้อยคำมติเดิมอยู่ข้างล่างนี้ทั้งหมด ไม่ลบ · **ภาพ AI ที่เผยแพร่ ต้องไม่มีตัวอักษรที่อ่านออกได้** · การ์ดโปรโมชันติดคำว่า **`Illustrative`** ได้ **แต่หน้าสถานที่/ทีม/ก่อน–หลัง ห้ามใช้ภาพ AI เลย ป้ายไม่ช่วย** | **ห้อง Legal** (ผ่าน `WS` · เปิดดูภาพจริงเองก่อนตัดสิน) | 2026-08-10 | **เกณฑ์ไม่ใช่ "สร้างด้วย AI หรือไม่" แต่คือ "ทำให้เข้าใจผิดในเรื่องที่เป็นสาระหรือไม่"** ⇒ สิ่งที่ทำให้เข้าใจผิดไม่ใช่เทคโนโลยีที่สร้าง แต่คือ **การสื่อว่า "นี่คือห้องของเราจริง"** · Legal วัดต่อว่า **บริการมีจริงและราคาตรงกับที่เว็บประกาศ** ⇒ เป็นภาพประกอบข้อเสนอที่มีอยู่จริง ไม่ใช่โฆษณาบริการที่ไม่มี ⇒ **การ์ดโปรโมชันเติมคำเดียวจบ** · 🔴 **เส้นที่ข้ามแล้วคำตอบเปลี่ยน**: หน้า *ร้านของเรา · ทีมของเรา · แกลเลอรีสถานที่ · Google Business Profile* **ภาพเป็นคำกล่าวอ้างข้อเท็จจริงเรื่องสถานที่และบุคลากร** ⇒ ห้ามใช้ · **ภาพก่อน–หลัง/ผลลัพธ์ ห้ามเด็ดขาด** · 🔑 **เหตุผลของกฎ "ห้ามมีตัวอักษร" ที่ควรจำ**: *การตรวจชื่อที่ AI แต่งขึ้นกับเครื่องหมายการค้าทั่วโลก เป็นงานที่ไม่มีขอบเขต · การเอาตัวอักษรที่อ่านออกได้ออกจากภาพ เป็นงานที่มีขอบเขตและใช้เวลาไม่กี่วินาที* ⇒ **เปลี่ยนคำถามที่ตอบไม่ได้ ให้เป็นกฎที่ทำได้** และครอบของที่ยังไม่เกิดด้วย (ป้ายราคาปลอม · คำที่ AI สะกดผิด · ข้อความบนฉลากทุกชนิด) · **ผลทันที**: ลบ `promo-spa-ritual-gemini-v2.jpg` (มี `HERITAGE SPA` + โลโก้ทองอ่านออกบนขวด 4 ใบ) และ `promo-spa-ritual-gemini.jpg` (ต้นฉบับ กำพร้าเหมือนกัน) · **เก็บ `-v3`** ที่ [`Campaigns.tsx`](../../components/Campaigns.tsx) ใช้อยู่ — ไม่มีขวด ไม่มีตัวอักษร ⚠️ **มีคนสร้าง `-v3` เพื่อเอาขวดออกอยู่แล้วก่อนมีใครถาม** ⇒ แบรนด์คนอื่นไม่เคยอยู่ในโฆษณาที่เผยแพร่ **แต่ไฟล์เก่ายังเปิดได้จากอินเทอร์เน็ต** ⇒ ตอกย้ำบทเรียน `W-18` · Legal สั่งชัด: **"ลบ ไม่ใช่เบลอ ไม่ใช่แก้ภาพ"** |
| D-W21 | **เบอร์โทรทางการ = `07564292247` / `+447564292247` (มือถือ) · `+442072780906` (020) ถูกยกเลิกแล้ว ห้ามประกาศที่ไหนอีก** | **เจ้าของ** (ผ่าน Kom · ตอบในห้องนี้โดยตรง) | 2026-08-10 | ใบ `Q-MKT-002` สั่งให้ใส่ `telephone: "+442072780906"` ลง JSON-LD **พร้อมกำกับว่า "✅ ตรงกับที่ประกาศบนสื่อและใน Ads sitelink แล้ว"** · ผมไม่ทำตามเพราะเป็นคนละเบอร์กับที่เว็บใช้ แล้วยกขึ้นถามแทนการเดา — คำตอบคือ **เบอร์ในใบคือเบอร์ที่ตายแล้ว** ⇒ **เว็บถูกอยู่แล้วตั้งแต่ต้น ไม่ต้องแก้โค้ดเลย** (เบอร์ 020 อยู่ใน repo 0 จุด) · 🔑 **บทเรียนที่ต้องใช้ต่อ: "ค่าที่ยืนยันแล้ว" ในใบคิว ก็ยังเป็นข้อมูล ไม่ใช่คำสั่ง** — ถ้าใส่ตามที่ใบสั่ง เว็บจะประกาศเบอร์ที่ยกเลิกแล้ว **โดยที่ทุกฝ่ายเชื่อว่าเพิ่งแก้ให้ตรงกัน** ⇒ ความเสียหายจะถูกซ่อนใต้ความรู้สึกว่า "จัดการแล้ว" · 🔴 **งานจริงอยู่นอก repo นี้**: ถ้า Ads sitelink/GBP/Treatwell/Facebook ยังใช้เบอร์ 020 อยู่จริงตามที่ใบอ้าง = **จ่ายเงิน PMax ซื้อคลิกที่พาลูกค้าไปโทรเบอร์ที่ไม่มีคนรับ** และเบอร์บ้าน UK ที่ยกเลิกแล้ว **ผู้ให้บริการเอาไปจ่ายรายอื่นได้** ⇒ อนาคตอาจกลายเป็นโทรไปหาคนอื่น ⇒ ออกใบ `Q-SHOP-001` ถึง `WS` ให้ route ไป `MKT` พร้อมขอให้ **ไล่กลับไปดูว่าแหล่งที่บอกว่าเบอร์ 020 เป็นทางการคืออะไร** เพราะแหล่งนั้นป้อนค่าอื่นในใบเดียวกันด้วย |
| D-W1 | **`AMENDED` 2026-08-09 — ยกระดับจาก "ไม่ต้องแก้" เป็น "ยืนยันแล้วว่าถูก": ปีก่อตั้ง = `2009` ตามที่เว็บเขียน** | **ครูนก** (ผ่าน Kom) | 2026-08-09 | เดิมมติคือ *"เจ้าของสั่งว่าไม่ต้องแก้"* ซึ่งเป็นคำสั่งไม่ให้แตะ **แต่ไม่ได้บอกว่าตัวเลขถูก** ⇒ เรื่องค้างในเชิงข้อเท็จจริงมาตลอด · ตอนนี้ **ครูนกยืนยันเองว่าเว็บเขียนถูก** พร้อมหลักการที่ปิดเรื่องได้จริง: 🔑 **"ปีก่อตั้ง ≠ ปีเปิดกิจการ/ปีจดทะเบียนบริษัท"** ⇒ วันจดทะเบียนใน Companies House (2015 · 2018 · 2019 — ดู [RESEARCH](RESEARCH-2026-08-09-founding-year.md)) **ไม่ใช่หลักฐานค้าน** เป็นคนละสิ่งกันตั้งแต่ต้น · ⇒ `Founded in 2009` · `ESTABLISHED 2009` · `foundingDate: "2009"` ใน structured data **ถูกทั้งหมด ไม่ต้องแก้อะไรเลย** · 🔴 **ห้ามเซสชันไหนหยิบวันจดทะเบียนบริษัทมาตั้งข้อสงสัยกับตัวเลขนี้อีก** — ถูกยกมาแล้วและตอบแล้ว · ส่วนโปสเตอร์หน้าร้านที่เขียน SINCE 2013 ยังไม่มีคำอธิบาย **แต่ไม่กระทบเว็บ** และไม่ใช่เรื่องของห้องนี้ |
| D-W16 | **แก้ถ้อยคำ Academy บนหน้า `/cookies` และขึ้นเลย โดยไม่รอ Legal** | Shop Lead (**Kom ให้สิทธิ์ตรง ๆ 2026-08-09**) | 2026-08-09 | `CLAUDE.md` เขียนว่าถ้อยคำ privacy/cookie ต้องผ่าน Legal ก่อนขึ้นจริง · ผมส่ง diff ให้ Legal แล้วและตั้งใจจะรอ **แต่เจ้าของสั่งให้เคาะเอง** ⇒ ใช้สิทธิ์นั้น · **เหตุผลที่ยอมข้ามด่าน**: (1) ของเดิม **เป็นเท็จอยู่บน production** — การรอทำให้คำที่ผิดอยู่ต่อ ไม่ใช่การรักษาสถานะเดิมที่ปลอดภัย (2) ถ้อยคำใหม่ **อ่อนกว่าเดิม ไม่ได้อ้างอะไรเพิ่ม** — เปลี่ยนจาก "ไม่มีคุกกี้เลย" (อ้างมากเกินจริง) เป็น "ไม่มีคุกกี้วิเคราะห์/โฆษณา มีแต่คุกกี้จำเป็น" (ตรงกับที่วัดได้) ⇒ **ทิศทางของความเสี่ยงลดลง ไม่ใช่เพิ่มขึ้น** (3) ไม่ได้แตะตารางทะเบียนคุกกี้ของเราเอง ⇒ **กฎเหล็กข้อ 2 ไม่ถูกกระทบ** ไม่ต้องวัดใหม่ · 🔴 **นี่คือการข้ามด่าน ไม่ใช่การยกเลิกด่าน** — Legal ยังมีสิทธิ์แก้ถ้อยคำนี้ทีหลังเต็มที่ ผมแจ้งเขาแล้วว่าขึ้นไปแล้วและขอให้ตรวจย้อน · **ครั้งหน้าที่ไม่มีคำสั่งตรงจากเจ้าของ ให้กลับไปรอ Legal ตามเดิม**  · **`AMENDED` ไม่กี่ชั่วโมงต่อมา — ถ้อยคำรอบแรกยังไม่ทน**: ฉบับแรกเขียนว่า Academy *"ไม่มีคุกกี้วิเคราะห์/โฆษณา … ไม่ต้องขอความยินยอม"* ซึ่ง **จริงวันนี้แต่จะเท็จทันทีที่ GA4 ขึ้นฝั่ง Academy** ⇒ ผมเพิ่งถอดระเบิดเวลาลูกหนึ่งแล้ววางลูกใหม่แทน (ห้อง Concept จับได้) · ฉบับที่ใช้จริงตอนนี้ **เลิกบรรยายคุกกี้ของเว็บอื่นทั้งหมด** เหลือแค่ข้อความที่เป็นของเราและจริงตลอดไป — *"คุกกี้วิเคราะห์ของเราเซ็ตเฉพาะเว็บนี้ ไม่ไหลไป Academy"* — แล้ว **ลิงก์ไปนโยบายคุกกี้ของเขาเอง** (`/en/policies/cookie-policy` ยืนยันแล้วว่า 200) 🔑 **หลักการที่ได้และควรใช้ต่อ: อย่ายืนยันข้อเท็จจริงเกี่ยวกับระบบที่เราไม่ได้ควบคุม ลงในเอกสารกฎหมายของเรา** — เพราะมันจะผิดโดยที่เราไม่มีทางรู้ · ชี้ไปแหล่งของเจ้าของระบบนั้นแทน แล้วเอกสารเราจะไม่ผิดตามเขา |
| D-W17 | **ไม่ถอยลิงก์ Academy กลับ — คงชี้ `academy.taitam-d.com` แม้ลำดับจะกลับหัว** | Shop Lead (เจ้าของให้สิทธิ์) | 2026-08-09 | Platform เสนอลำดับ *แก้ถ้อยคำ → GA4 → สลับลิงก์* แต่ผมสลับไปก่อนแล้ว (มติเจ้าของมาถึงจากอีกห้องก่อน) ⇒ ทางเลือกคือถอยหรือเดินหน้า · **เลือกเดินหน้า** เพราะ: (1) การถอย = ส่งลูกค้ากลับไปเว็บที่ **เจ้าของประกาศแล้วว่าจะยกเลิกทั้งหมด** — ยิ่งสะสมลิงก์/ทราฟฟิกไปที่นั่นยิ่งเจ็บตอนปิด (2) การถอย = deploy ทับอีกรอบบนเว็บที่มีงบ PMax ยิงอยู่ ตรงข้ามกับ §11.13 (แก้ไปข้างหน้า ห้าม revert) (3) สิ่งที่เสียจริงจากการสลับก่อนคือ **attribution 7 วัน** ไม่ใช่ความถูกต้องของเว็บ — และแก้ได้ด้วยการเร่ง GA4 ซึ่งเป็นงานที่ต้องทำอยู่แล้ว ⇒ **ราคาของการถอยแพงกว่าสิ่งที่ได้คืน** · ⚠️ ผลที่ยอมรับอย่างตั้งใจ: ถึง 16 ส.ค. คนกด "Academy" จะเจอ**หน้านับถอยหลัง** (ยิงดูเองแล้ว) · แจ้งทุกห้องแล้ว **ถ้าเจ้าของอยากได้อีกแบบ กลับได้ในคอมมิตเดียว** |
| D-W19 | **`Taitam-D Group` คงไว้ — ตั้งใจใช้** | **เจ้าของ** | 2026-08-09 | ไม่ใช่คำเผลอเขียน · ครูนกดำเนินกิจการหลายตัว จึงตั้งใจสื่อสารรวมเป็น "Group" · **ไม่ต้องหาชื่อนิติบุคคลมาแทน** — ในทะเบียนไม่มีอะไรชื่อนี้ ซึ่งไม่ใช่ปัญหา เพราะเป็นคำเรียกกลุ่มกิจการ ไม่ใช่การอ้างสถานะทางกฎหมาย |
| D-W20 | **`VTCT-accredited` และ `Certified NLP Master` คงไว้ตามเดิม · หยุดสืบหาหลักฐาน** | **เจ้าของ** | 2026-08-09 | VTCT: เจ้าของยืนยันว่าหนังสือรับรองปี 2019 **ยังใช้อยู่** ⇒ ยึดตามนั้น ไม่ต้องรอทะเบียนสาธารณะ (ซึ่งตอบ 500 อยู่แล้ว) · NLP: **ข้ามไปก่อน อย่าเพิ่งหาหลักฐาน** · 🔑 **เหตุผลที่เจ้าของให้ และสำคัญกว่าตัวคำตอบ**: *"ตอนนี้เราเริ่มละเอียดเกินกว่าที่จะทำ MVP แล้ว"* ⇒ **นี่คือการปรับระดับความละเอียดของทั้งห้อง ไม่ใช่แค่ปิดสองข้อนี้** · ถ้าวันหนึ่งกลับมาทบทวน ให้เริ่มจากถามเจ้าของ ไม่ใช่เริ่มจากสืบเอง |
| D-W18 | **ปล่อย URL เก่าให้ตอบ 404 ต่อไป · ปิด W-13 ไม่รอ GSC** | Shop Lead | 2026-08-09 | `/home` `/contact-v1/` `/team` `/courses/…` ที่ Google ยัง index อยู่ **ตอบ 404 ซึ่งถูกต้องแล้วสำหรับหน้าที่ถูกลบจริง** — Google จะถอนออกเอง · เดิมผมค้างไว้รอข้อมูล GSC **แต่ข้อมูลนั้นตอบคนละคำถาม**: ต่อให้เห็นตัวเลข impression ของ URL ที่ตายแล้ว มันก็ไม่เปลี่ยนว่า 404 คือคำตอบที่ถูก ⇒ **รอไปก็ไม่ได้อะไร** · ส่วน `/courses/…` ที่น่าทำ 301 ที่สุด **ทำจากรีโปนี้ไม่ได้เลย** — static export บน GitHub Pages ไม่มี server redirect และ **หน้าคอร์สเดิมอยู่บนโดเมนอื่นที่กำลังจะถูกยกเลิก** ⇒ ถ้าจะรักษา equity ต้องทำ 301 **ที่ Hostinger ฝั่งโดเมนเก่า** ซึ่งไม่ใช่รีโปนี้ ⇒ **เรื่องนี้ไม่ใช่งานของห้องนี้ตั้งแต่ต้น** ย้ายไปอยู่ในแผนย้ายเว็บที่ Marketing ถือ · 🔑 **เงื่อนไขกลับมาทบทวน**: ถ้า GSC ขึ้น error ที่ไม่ใช่ 404 ธรรมดา (เช่น soft 404 ที่หน้าแรก) หรือมีหน้าที่ยัง**มีอยู่จริง**แต่ถูกรายงานว่าหาย |
| D-W14 | **ชื่อสถาบันบนเว็บร้าน = `Taitam-D Academy London`** ⇒ `Beauty Academy` เลิกใช้ · **`D-L1` ครอบเว็บร้านด้วยแล้ว** | **เจ้าของ** (ผ่านห้อง Concept) | 2026-08-09 | เจ้าของยืนยันว่า `Taitam-D Beauty Academy` **เป็นหน่วยเดียวกับ** `Taitam-D Academy London` และชื่อที่ถูกอยู่บนโลโก้ · แก้ 4 จุด: หัวข้อการ์ด + ปุ่ม + บรรทัดตำแหน่งใน [`Story.tsx`](../../components/Story.tsx) · ลิงก์ใน [`Footer.tsx`](../../components/Footer.tsx) 🔑 **สิ่งที่จงใจไม่แก้:** (1) ป้าย/ลิงก์ที่เขียนแค่ `Academy` (navbar · mobile dock · "Explore Academy") — เป็น **ป้ายนำทาง ไม่ใช่การระบุชื่อสถาบัน** จึงไม่ผิด (2) URL เฟซบุ๊ก `facebook.com/TaitamdBeautyAcademy` ใน [`lib/site.ts`](../../lib/site.ts) — **เป็นที่อยู่ของทรัพยากรจริง ไม่ใช่ถ้อยคำ** แก้แล้วลิงก์พัง (1a) ⚠️ **เหตุผลของข้อ (1) เป็นการตีความเจตนา `D-L1` โดยห้อง Concept ไม่ใช่คำสั่งของเจ้าของ** — เจ้าของทับได้เสมอ ถ้าท่านอยากให้ขึ้นชื่อเต็มทุกที่ ให้ถือว่าคำสั่งท่านชนะ อย่าอ้างบรรทัดนี้เป็นมติ · (3) ปุ่มเปลี่ยนเป็น "Visit the Academy" แทนการยัดชื่อเต็มลงปุ่ม — ชื่อสถาบันปรากฏเป็นหัวข้อเหนือปุ่มอยู่แล้ว · ⚠️ **หลักฐานบังเอิญ**: รูปภายในร้านบนหน้า `/story/` มีป้ายติดผนังเขียน `TAITAM-D ACADEMY LONDON` ⇒ ตรงกับที่เจ้าของบอกว่าชื่อถูกอยู่บนโลโก้ |
| D-W13 | **ไม่รัน `npm audit fix` · ปิด W-8 · เฝ้าดูแทน** | Shop Lead | 2026-08-09 | `npm audit` ขึ้น 10 รายการ (9 high) แต่ **ประเมินจากพื้นผิวจริงแล้วไม่ใช่ความเสี่ยงของเว็บนี้**: static export ⇒ **ไม่มี server runtime ให้โจมตี** · `images.unoptimized = true` ⇒ **`sharp` ไม่ถูกเรียกแม้ตอน build** · `postcss` กินอินพุตจากซอร์สของเราเอง ไม่ใช่จากผู้ใช้ ⇒ ช่องทางที่ CVE พูดถึงไม่มีอยู่ในสถาปัตยกรรมนี้ · ฝั่งต้นทุน: `audit fix` ดัน transitive ของ `next` ได้ ⇒ **เสี่ยงทำ deploy path พังเพื่อปิดช่องที่ไม่มีทางเข้า** · 🔑 **นี่คือการเคาะ ไม่ใช่การเลื่อน** — เงื่อนไขที่จะทำให้กลับมาทบทวน: (1) เว็บเลิกเป็น static export หรือมี API route (2) เปิด `images` optimization (3) `next` major upgrade ที่ต้องขยับ lockfile อยู่แล้ว ⇒ ทำพร้อมกันตอนนั้น |
| D-W10 | **นามสกุลภาษาอังกฤษของผู้ก่อตั้ง = `Pomnongsan`** ⇒ เว็บใช้ `Thanyarat Pomnongsan` | **ครูนกเคาะเอง** (ผ่านเจ้าของ → ห้อง Concept `D-74`) | 2026-08-09 | เดิมเว็บสะกด `Phomnongsan` มาตลอดและ **ผิดจริง** · ลำดับการตัดสิน: Kom เคาะ "วิธี" ก่อน (ยึดเอกสารราชการ ไม่เลือกจากสามฉบับที่เรามี — เพราะใบรับรองผู้เรียนแก้ย้อนหลังไม่ได้) แล้วครูนกจึงเคาะ "ค่า" เอง · 🔴 **ห้ามให้เซสชันไหนไป "แก้ให้ถูกหลัก" ภายหลัง** — `พรหม` ถอดตามราชบัณฑิตยสภาได้ `Phrom` ซึ่ง **ไม่ใช่ฉบับที่เจ้าตัวใช้** · **ฉบับนี้มาจากเจ้าตัว ไม่ใช่หลักการถอดเสียง** ⇒ หลักภาษาไม่ใช่เหตุผลที่ใช้ค้านได้ · แก้แล้ว 3 จุดใน [`Story.tsx`](../../components/Story.tsx) (ท้ายคำพูด · หัวข้อ · **`alt` ของรูป** ซึ่งตาไม่เห็นแต่ screen reader และ Google อ่าน) |
| D-W9 | **`npm run lint` = `eslint . --max-warnings=0`** (warning นับเป็นตก gate) | Shop Lead | 2026-08-09 | `next lint` เดิมทำให้เราชินกับ "✔ No ESLint warnings or errors" แต่ **ESLint CLI คืน exit 0 ทั้งที่มี warning** ⇒ ถ้าไม่ใส่ `--max-warnings=0` การต่อ `&&` ใน gate จะผ่านทั้งที่มี warning ค้าง = gate อ่อนลงเงียบ ๆ จากการย้าย tooling · ผลข้างเคียงที่เจอทันที: CLI กวาดกว้างกว่า `next lint` (37 ไฟล์ รวม `next.config.mjs` · `tailwind.config.ts` · `postcss.config.mjs` ที่เดิมไม่เคยถูก lint) แล้วเจอ warning ใน `postcss.config.mjs` ⇒ แก้ไฟล์นั้นเสียเลยแทนการ ignore |
| D-W8 | **npm เป็น package manager เดียวของ repo นี้ · ลบ `pnpm-lock.yaml` ทิ้ง** | Shop Lead | 2026-08-09 | repo มี **lockfile สองใบ** (`package-lock.json` + `pnpm-lock.yaml` เขียนห่างกัน 1 นาทีเมื่อ 4 ส.ค.) และ step "Detect package manager" ใน [`nextjs.yml`](../../.github/workflows/nextjs.yml) **เช็คแค่ `yarn.lock` แล้ว fallback เป็น `npm ci` — ไม่รู้จัก pnpm เลย** ⇒ CI build ด้วย `package-lock.json` เสมอ ขณะที่ `node_modules` ในเครื่องมาจาก pnpm · **หลักฐานการแตกจริง ไม่ใช่ทฤษฎี**: CI ได้ `next 15.5.12 · react 19.2.4 · eslint 9.39.2` แต่ในเครื่องได้ `next 15.5.22 · react 19.2.8 · eslint 9.39.5` ⇒ **ของที่เรา verify ไม่ใช่ของที่ deploy** ซึ่งทำให้ D-W4/D-W5 (gate ก่อน merge) เป็นโมฆะเงียบ ๆ · เลือก npm เพราะเป็นฝั่งที่ CI สร้างของขึ้นเว็บจริงมาตลอด — ย้าย CI ไป pnpm คือการเปลี่ยน deploy path ที่พิสูจน์แล้วเพื่อความสะดวกในเครื่อง ไม่คุ้ม · หลังลบแล้ว `npm ci` ในเครื่องได้เวอร์ชันตรงกับ CI ทุกตัว |
| D-W7 | **ยังไม่ยิง Meta ads ⇒ ไม่ติด Meta Pixel · ห้ามแตะ tag layer** | **เจ้าของ** (Marketing Lead แจ้งต่อ 2026-08-09) | 2026-08-09 | เจ้าของสั่งให้ทำ Google ให้นิ่งก่อน · Marketing **ยกเลิกคำขอประเมิน** ที่ส่งมาก่อนหน้าในวันเดียวกันทั้งหมด · เก็บผลประเมินไว้ใน [`HANDOFF.md §3`](HANDOFF.md) เผื่อกลับมาคุยใหม่ (`AW-` ~1 บรรทัด ขี่ `gtag` เดิม · Meta Pixel ครึ่งวัน–1 วัน เพราะ `fbq` ไม่รู้จัก Consent Mode) 🔑 **ต้นทุนที่แพงกว่าโค้ดคือเอกสาร** — pixel ใด ๆ ที่เพิ่มคุกกี้ ต้องวัดทะเบียนใหม่บน production ตามกฎข้อ 2 แล้วแก้ `app/cookies/page.tsx` + ข้อความ banner และ **ผ่านห้อง Legal ก่อนขึ้นจริง** · ⚠️ มตินี้มาถึงเราผ่านห้อง Marketing ไม่ได้ยินจากเจ้าของโดยตรง — ถ้าจะกลับมาทำ ยืนยันกับเจ้าของอีกครั้ง |
| D-W3 | **ยืนที่ 2 ชั้น (Lead → sub-agent) ไม่รับ Executor tier ชั้นที่ 3** | Shop Lead | 2026-08-09 | งานที่นี่คือแก้ section/copy/รูปทีละจุด ไม่มี loop verify→fix ยาวแบบ backend · token tax ~4x/ชั้น ไม่คุ้ม · ตรงกับที่ห้อง Principia ประเมินไว้แล้ว · ทบทวนใหม่เมื่อมีงานที่ต้อง loop จริง |
| D-W4 | **"verify เขียว" = `npm run build && npm run lint` + visual gate** | Shop Lead | 2026-08-09 | repo ไม่มี test runner เลย · `next build` ทำ typecheck + จับ static-export error ให้ในตัว · ไม่มีเทสต์ ⇒ visual gate คือด่านสุดท้ายจริง ห้ามข้าม |
| D-W5 | **gate อยู่ก่อน merge · Lead เท่านั้นที่ push `main`** | Shop Lead | 2026-08-09 | push `main` = GitHub Actions deploy ขึ้น Pages ทันที ต่างจากกติกากลางที่ merge ≠ deploy · มีงบ PMax ยิงมาที่ apex อยู่ ⇒ ของที่หลุดคือของที่ลูกค้าจริงเห็น |
| D-W6 | **ลบ `pages.yml` · เหลือ `nextjs.yml` ตัวเดียวเป็น deploy path** | Shop Lead (เจ้าของมอบอำนาจให้เคาะเอง 2026-08-09) | เดิมสองไฟล์ deploy ลง Pages ทุก push · group `pages` เดียวกันแต่ `cancel-in-progress` ขัดกัน · **หลักฐานจริง ไม่ใช่ทฤษฎี**: push `7199546` → success ทั้งคู่ · push `6585eaa` → `pages.yml` success แต่ `nextjs.yml` **cancelled** ⇒ โค้ดชุดเดียวกัน push ติดกันสองครั้ง ได้ผลคนละแบบ · เลือกลบ `pages.yml` เพราะมันคือตัวที่ตั้ง `cancel-in-progress: true` ซึ่งเป็นกลไกที่ฆ่า deploy ที่กำลังวิ่งอยู่ — พฤติกรรมที่ไม่ควรมีกับ production · ตัวที่เก็บไว้ยังมี `configure-pages` (inject basePath ให้ถ้าวันหนึ่งเลิกใช้ custom domain) และ `deploy-pages@v5` |
| D-W2 | **band `3300–3399`** (3300 dev · 3301 visual gate) · **แหล่งจริง = `registry.json → devServerPorts`** ไม่ใช่ §4.1 ที่ platform · 📍 **path ปัจจุบัน (ยืนยัน 2026-08-10) = `gumon-workspace/machines/<machine-id>/registry.json`** — ข้อความเดิมของมติเขียนว่า `gumon-localdev/registry.json` ที่ root ซึ่ง **ย้ายแล้วและไฟล์ที่ root ไม่มี** · *มติไม่เปลี่ยน (band ยังเป็นของเรา) เปลี่ยนแค่ที่อยู่ของทะเบียน* | ห้อง Local Dev Lead → ปัจจุบันคือ **Gumon Workspace Lead** (rename 2026-08-09) | 2026-08-09 | ตอนขอ พบว่า**ทะเบียนพอร์ต dev server ไม่เคยมีอยู่จริง** — `registry.json` เดิมมีแต่พอร์ต datastore · ของที่ใช้กันจริงคือ §4.1 ซึ่งอยู่ในรีโปของโปรเจกต์เดียว ⇒ **โปรเจกต์นอก Taitam-D ไม่เคยเห็นมัน** (สแกนเจอ 2 รายที่ bind ทับ band ของ human/paragon อยู่แล้วโดยไม่รู้ตัว) · Local Dev Lead สร้าง `devServerPorts` ขึ้นเป็นแหล่งเดียว และจะประสานให้ §4.1 เปลี่ยนเป็นตัวชี้มาที่นั่น ⇒ **repo นี้ห้ามคัดตารางพอร์ตมาเก็บเอง** จะกลายเป็นสำเนาใบที่สาม |
